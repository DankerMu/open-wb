# CONTEXT — 领域语言与限界上下文

> 术语单一来源。一个词一个含义；与本表冲突的措辞按缺陷修正。行为基准是
> `resource/workbuddy-live-demo.html`（demo 能点出来的行为就是需求），架构基准是 `docs/architecture/system.md`（`PLAN.md` 保留为建设方案与阶段账本）。

## 项目本质

内网多用户 AI Agent Web 服务。三个自研/组装组件：**app-server**（业务后端）、**web SPA**、
**kbservice**（知识库）；agent 执行由 **omp fork**（另仓，冻结 v18.0.10）以每会话子进程承担。

## 领域术语

| 术语 | 含义 | 边界说明 |
|---|---|---|
| 工作空间 workspace | 一等实体；一棵多根目录树 = 空间根目录（沙箱内）+ 若干挂载 | 不是"项目"的别名；会话可挂在空间下 |
| 挂载 mount | 空间内的一个树根：本地磁盘 / SFTP / NFS / SMB，有只读与在线状态 | 空间根目录是不可卸载的特殊根 |
| 沙箱 sandbox | 服务端每账号根目录（demo 形态 `/data/workbuddy/<user>`）；越界写一律拒绝并入审计 | 是文件系统边界，不是进程隔离 |
| 场景 scene | 会话页三分类：日常办公/代码开发/创意设计，决定默认专家与工具面 | 不绑定知识库（用户已确认现逻辑） |
| 专家 expert | 预置人设卡片（提示词+工具面），可加入会话 | 与"技能"不同：技能是能力开关 |
| 知识库 KB | **检索层不是文件夹**：agentic 检索后只把命中切片带入上下文，绝不整篇载入 | 要整读文件走"工作空间引用" |
| 切片 chunk | 文档经模板化切分的检索单元；12 种模板对应 RAGFlow chunk_method | |
| agentic 检索 | 改写→召回→充分性判定→不足则二轮→仅切片入上下文 | 与单轮向量检索区分 |
| 可见范围 scope | KB 权限四档：仅自己/本部门/项目组/全员；共享一律只读 | 检索共享库记审计 |
| 项目组 group | 跨部门共享单元，只影响 KB 可见范围，不改沙箱边界 | 若 SSO 自带组概念则改同步（开放问题） |
| 连接器 connector | MCP 连接器，仅显式配置不自动发现 | 上游的 SaaS 连接器已裁掉 |
| 审计 audit | 权限事件/共享 KB 检索/账号操作的只追加记录，按账号隔离 | |
| host tool | omp RPC 的宿主工具机制；KB 检索经 app-server 转发实现 | 凭证只在 app-server，不进 omp 环境 |
| 模型代理 model-proxy | app-server 内的 OpenAI 兼容端点，omp 访问模型的唯一通道 | omp 环境零网关凭证（ADR-0008）；kb-service 嵌入调用不经它 |

## 限界上下文

- **会话编排**（server）：会话/场景/专家/omp 子进程生命周期。
- **文件与沙箱**（server）：工作空间/挂载/沙箱边界/白名单。
- **知识库**（kbservice）：摄取/切片/检索/agentic 环——不感知租户，只收已过滤输入。
- **账号与治理**（server）：SSO/项目组/可见范围/审计——多租户逻辑全在这里。

## 不变量（违反即缺陷）

1. 共享知识库对非所有者永远只读。
2. 进入模型上下文的 KB 内容只能是命中切片+出处，不能是整篇文档。
3. 沙箱外写/删一律拒绝且必入审计。
4. kb-service 凭证与模型网关密钥不得出现在 omp 子进程可读环境。
5. `app-reference/` 内容不进任何产物。

## 开放术语/问题

- SSO 已定 OIDC、项目组默认自建（ADR-0007）；剩余开放：IdP 是否带组声明，带则改同步——待身份源调研，P3 前关闭。
