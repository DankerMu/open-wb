# Changelog

## [0.1.0] - 2026-08-28

- Imported from stellarlink-skills (`design`) into the my-agents monorepo as part of the `stellarlink` pack.
- Renamed to `visual-design` to avoid colliding with Claude Code's built-in `design` canvas skill and with software-architecture `design` skills.
- Flagged `disable-model-invocation: true`; nothing in the pack invokes it mid-run.
