Change surface (size-guard 800-line limit moved fixtures into `server/test/fake-omp-helpers.ts` and shared HTTP/launcher helpers into `server-startup-helpers.ts`): `server/test/support/fake-omp.mjs` (call-proxy relay only), `server/test/fake-omp.test.ts`, `server/test/server-startup-order.test.ts`, `server/test/server-startup-helpers.ts` (preload generator), `server/test/listener-shutdown.test.ts` (switch to the shared preload, behaviour unchanged).

Must preserve:
- #88 fake upstream contract unchanged (no text-without-tool escape hatch; `model-proxy` 假上游夹具契约 and `server/test/fake-upstream*.test.ts` green). The relay must work against the fixture as-is.
- Other fake-omp scenarios (default, crash, crash-after-deltas, error, extension-ui, hang-*, probe, chunked, interleaved, no-ready, missing-session) unchanged.
- Existing call-proxy stub tests in `server/test/fake-omp.test.ts`: fragmented-UTF-8 byte-boundary case (~:243-278, content-only single-round success) and failure modes (missing config, `providers: []`, HTTP 500, EOF before [DONE]) keep their assertions and no-secret checks.
- Production code untouched; `server.ts:272` guard untouched.
- Existing startup-order cases and #227 listener tests keep their assertions.

Relay design (fake-omp call-proxy):
- Round 1 POST `[user]`. Parse SSE `delta.content` (byte-safe, existing decoder) and `delta.tool_calls[]` fragments keyed by `index` (first fragment carries `id`, `function.name`; `function.arguments` concatenated).
- If round 1 has tool calls: for each, emit `tool_execution_start`/`tool_execution_end` with the same fields as the default scenario but `toolCallId` = upstream id (e.g. `call_<uuid>`) and `toolName` = upstream name; tool result is a fixed string (e.g. `workbuddy-smoke`). Round 2 POST `[user, {role:"assistant", content:<round-1 text or null>, tool_calls:[{id,type:"function",function:{name,arguments}}]}, {role:"tool", tool_call_id:id, content:<result>}]`. Round 2 content → `text_delta`.
- Success requires nonempty text in the answering round. Round 1 empty (no content, no tool calls), incomplete tool call or non-JSON arguments, round 2 without content, or round 2 carrying any tool calls (would need a third round) → `failTurn` path with `stopReason:"error"`. No loop beyond two rounds.
- Round 1 with content and no tool calls → single-round success (keeps stub tests).

Governing invariants: (1) a call-proxy turn succeeds only if the upstream produced observable content through the proxy; (2) SIGTERM observed during the pending models.yml write never publishes `server_started` nor `server_start_failed`, exits `{code:0, signal:null}`, closes listener then DB exactly once and frees the port.

Sibling surfaces: fake-omp `completeTurn`/`failTurn` (~:295-335); default-scenario tool frame shape; model proxy passthrough (`server/src/model-proxy/index.ts:433-435` builds `baseUrl/chat/completions`); omp launcher env (`server/src/sessions/omp/process.ts:81-82`: `PI_CODING_AGENT_DIR=<OMP_STATE_DIR>/agent`, `WORKBUDDY_MODEL_TOKEN`); `writeManagedModelsYml` (`server/src/model-proxy/models-yml.ts`, named `writeFile` import → preload uses `syncBuiltinESMExports` as the #227 hook does); helpers `compileServerEntry`/`startCompiledServer` (`orderTrace` → `listener-close`/`db-close` trace, `requireHook`)/`reserveWildcardPort`; existing `login`/`createSession`/`prompt` and `hangEofLauncher` pattern in `server-startup-order.test.ts` (~:547-600) and `captureSpawnHook` (~:621-637).

Seams under test: fake-omp child over real JSONL/HTTP; compiled production entry with real listen, real models.yml, real token registry and proxy; compiled entry with a call-through `--require` preload and real SIGTERM.

Required evidence:
- E1 fake-omp contract (fake-omp.test.ts): upstream = in-process #88 `start()` behind a test-owned HTTP forwarder that records each request body and forwards it unchanged; models.yml points at the forwarder, bearer = fixture apiKey. Assert: `tool_execution_start`/`end` for `bash` with args `{"command":"echo workbuddy-smoke"}` and the upstream call id; exactly two recorded requests; request 2 messages = `[user, assistant with tool_calls (same id/name/arguments), tool with tool_call_id = that id]`; ≥3 `text_delta` concatenating exactly to `你好，这是 WorkBuddy 的第一条流式回复。`; `message_end.stopReason` not `error`, terminal `agent_end`. RED against the pre-change relay (shown by implementer). Empty-200 stub (role-only delta + [DONE]) → `stopReason:"error"`, no fabricated text. Second-round tool-only stub → `stopReason:"error"`.
- E2 compiled-entry full proxy turn (server-startup-order.test.ts): #88 `start({apiKey:<unique 64-char sentinel>})` as `MODEL_UPSTREAM_BASE_URL`/`MODEL_UPSTREAM_API_KEY`; `OMP_BIN` = a generated launcher (like `hangEofLauncher`) running fake-omp `--scenario call-proxy` that first writes its `WORKBUDDY_MODEL_TOKEN` env value to a test-owned file whose absolute path is embedded in the launcher text (the omp spawn env is rebuilt from scratch, `process.ts:77-89`, so it cannot come via env). login → create session → prompt; poll `/api/sessions/:id/messages` with a deadline (no sleeps as ordering) until the assistant message is terminal. Assert: persisted assistant text equals the fixture text, turn done, a done `bash` step; models.yml contains `WORKBUDDY_MODEL_TOKEN` name and neither the sentinel nor the captured bearer; complete stdout/stderr contain neither; SIGTERM then clean exit.
- E3 held write (#210, server-startup-order.test.ts): shared preload in call-through mode gates `writeFile` for the models.yml path: writes a `write-entered` marker, installs a `process.on('SIGTERM')` listener (call-through mode only) that writes a `signal-observed` marker, and releases the real write only after the marker exists (polling on a timer). Determinism argument: all SIGTERM listeners (preload's and entry's, server.ts:193-194) run synchronously in one `process.emit`, and the held write resumes only on a later macrotask, so `owned.signalReceived = true` (server.ts:300) precedes write resumption and the guard at :272. Test: start with `orderTrace` + the preload; wait for `write-entered`; SIGTERM; wait exit. Assert: no `server_started` in stdout, no `server_start_failed` in stderr (stderr only the known SQLite ExperimentalWarning), exit `{code:0, signal:null}`, models.yml present with valid content (write settled), order trace exactly `listener-close\ndb-close\n`, port bindable by a successor listener.
- E3 mutation: temporarily delete/neutralize the guard at `server/src/server.ts:272-274`, run the E3 test and show it red (either `server_started` on stdout or `server_start_failed` + exit 1 — both valid, not pinned), restore, prove with `git diff --exit-code server/src`.
- Shared preload keeps #227 throw-mode semantics (listener-shutdown.test.ts assertions unchanged, no SIGTERM listener in throw mode).

Non-goals: changing the #88 fixture, production code, CI workflow, or live specs other than the call-proxy scenario; real-omp smoke (#105 path).

Review focus: relay faithfully reassembles fragmented tool_calls and never fakes success; E2 really goes through the compiled entry's models.yml, token registry and proxy; E3 barrier deterministic; shared preload keeps #227 semantics; no secret leakage; no jscpd duplication (reuse helpers).
