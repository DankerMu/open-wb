## 1. Contract and baseline
- [x] 1.1 Expanded fixture first-round PASS;136 existing source/test/judge identities frozen, LSP option/class consumers checked, user-approved synchronous contract recorded. Parent fourteen-case compiled baseline has two valid controls GREEN and twelve unsupported-return/getter semantic counterexamples RED; all owned native exits observed with the repaired containment runner.
- [x] 1.2 Public REDs recovered from original tool artifacts3178 (fulfilled event sink, missing owned fault) and3182 (error thenable, source fault without contract fault); artifact3176 was a harness timeout and is excluded. Existing synchronous containment scenarios preserved.
## 2. Synchronous containment
- [x] 2.1 Shared return guard at event/error call sites, options/assembly JSDoc; eight permanent regressions plus fourteen protected parent cases cover the approved return contract. Only the first event/error verticals had writer semantic RED before their guard edits; later cases first ran GREEN, disclosed rather than retroactively claimed RED.
- [x] 2.2 One-notification ownership, non-recursion, native/token retirement, no awaiting invalid work and caller DB ownership verified. Existing fault-test helpers moved unchanged into the existing shared helper file.
## 3. Acceptance and delivery
- [x] 3.1 Parent fourteen-case real compiled/native acceptance GREEN; five isolated compiled mutants rejected (missing event guard, missing error guard, missing rejection consumer, native-only recognition, awaiting pending return), each restored GREEN; five separate stability trials GREEN. Scoped static/types/build pass; full server63files1146tests,92.14% statements/88.10% branches; anti-drift0clones and strict validation pass. Baseline136 protected inputs changed only in the five authorized existing source/test files.
- [x] 3.2 Expanded three-seat review completed; one coverage-only fix pass, fresh round2 clean on4cbe3555db52083f07674f8d9a7829c24f1d64aa; CI35807494969 all8success; sourcePR216 merged asf5c9a5b53829a44066fa8f839f602feefc29c77b. Canonical promotion/archive is the separate follow-up branch. Queue continuation remains gated on that archive PR's own CI and merge; this source-completion checkbox does not claim archive CI already ran.

## Core risk mapping
Selected entry/API→1.2/2.1 faithful sink return seam; schema/types→2.1 structural thenable versus ordinary void values; concurrency/state→2.1 pending/rejected work and one fault transition; resources→2.2 native/token cleanup; compatibility→2.2 existing sync throws and recorder values; errors→2.1/2.2 rejection consumption/non-recursion; tests/evidence→1.1/3.1 semantic RED versus setup/cleanup failure; release/docs→3.2 explicit options/canonical contract and same-headCI. Not selected config (no keys), file IO (no new production IO), auth/secrets (no auth or credential policy changes; native cleanup still audited).

## Domain risk mapping
Selected auth/session lifecycle (faulted pump/native ownership,2.2); process/child-environment isolation (real native retirement and no detached process-level rejection,3.1); offline deployability (zero new dependencies,3.1). Not selected tenant/sandbox enforcement, SQLite migration/catalog changes, server/web HTTP-envelope changes, browser runtime/navigation/persistence or cross-service network protocol. Existing DB remains caller-owned; no schema or permission changes.

## Ownership and execution
One writer/current checkout/no worktree/no nesting. Allowed source: server/src/sessions/supervisor.ts; JSDoc-only sink changes in sessions/index.ts and app.ts. Tests: existing server/test/session-supervisor-faults.test.ts stays behaviorally unchanged; move the new synchronous-sink cases and their private observers into server/test/session-supervisor-sinks.test.ts because normal formatting exceeds the 800-line file cap. Reuse existing support helpers; no new production helper or lint configuration change. The three intentional thenable constructions may carry local biome-ignore lint/suspicious/noThenProperty comments explaining their negative-fixture purpose: both literal and defineProperty constructions were rejected by Biome; computed-name evasion is forbidden. All other source/tests/config/fixture and /tmp/open-wb-issue204-evidence are read-only to the writer. Writer may run only focused `npm exec --workspace server -- vitest run --no-coverage test/session-supervisor-faults.test.ts test/session-supervisor-sinks.test.ts`; skip formatter/linter/types/build/fullsuite/git. Parent owns verification and fixture updates. Reviewers read-only/no validation/nesting.
Parent anti-drift found two clones after the test split. Authorized test-only scope extension: move assistantIdFor, assertRetainedFaultOnShutdown and containsMessage into the existing session-supervisor-helpers.ts, import them from both tests, and preserve their implementations/assertions. Original fault scenarios remain unchanged; helper relocation replaces the earlier byte-identical-file expectation. No duplicate-threshold or discovery changes.

## Oracle integrity
Initial parent event-rejection probe observed the genuine Node detached rejection but then its runner attempted a group signal after leader exit and got EPERM; that attempt is not counted as a clean containment baseline. Originals/logs are retained. Revised runner signals a group only while its own leader is verified live; an uncaughtExceptionMonitor does not swallow Node fatal handling and stops only the probe-owned native ChildProcess handles. IPC identifies native children; non-destructive liveness verifies exit. The repaired core baseline was expanded before candidate authorization to fourteen cases: two GREEN/twelve semantic RED, all native exits confirmed, no cleanup error or timeout. Every expanded case reran against the current oracle fingerprint; earlier core logs remain historical rather than substitute evidence. Same-user local separation is not an OS enclave; independent same-head CI remains mandatory.

## Verification deviations and limits
Raw historical writer GREEN outputs were not saved; parent fresh focused/full-suite output supplies final proof. The initial test split exceeded the file cap and then duplicated helpers; the final dedicated test file and shared helper extraction preserve all original scenarios. Three local noThenProperty ignores document intentionally invalid test values; no production rule/config/discovery/threshold changed. Parent required the implementer to remove test-owned rejection handlers before acceptance so rejection-consumption tests no longer mask a missing guard. Evidence and raw commands live at /tmp/open-wb-issue204-evidence; source PR carries durable command results and review/CI identities. No claim covers arbitrary detached work created inside invalid callbacks, hostile non-returning then methods, or browser SSE behavior.

## Review round 1 coverage repair
All three reviewers found no production defect. Parent confirmed the permanent callable-thenable case did not detect a lost receiver (frozen original test with that isolated mutation exited0), although the protected native probe already covered it. Fix pass1 replaces it with receiver-bound bare rejected work constructed inside the sink callback; the guard is the only rejection consumer. Exact internal error-message equality assertions were replaced by fault count/type/identity assertions. The repaired test rejects the lost-receiver mutation with a real unhandled rejection (exit1), restored guard exits0 and a separate stability run exits0. An initial repair created rejection before asynchronous setup and failed even with the restored guard; that authoring failure and the missing-tsconfig setup failure are preserved, excluded from qualification. No production change.
