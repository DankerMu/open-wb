## Context
jscpd 5.0.16 (lockfile) merges `cli.exit_code.or(config.exit_code)`; config accepts camelCase `exitCode` (`#[serde(alias = "exit-code")]`). Exit order at `src/main.rs` tail: threshold reporter failure → exit 1 with `ERROR: jscpd found too many duplicates (…) over threshold (…)`; then, independently, any clone + `exit_code` set → exit(code) with no threshold text.

Change surface: `.jscpd.json`; `scripts/test-guardrails.sh` (new self-proof cases).
Must preserve: command literal `npx jscpd --config .jscpd.json` in Makefile `anti-drift`, CI `anti-drift` job step, `package.json` `dupes`; `scripts/test-ci-harness.sh` literal/mutation oracle; `threshold: 3`, `pattern`, `ignore`, `reporters` unchanged; `constraints.yaml` 3%/block and AGENTS mirrors unchanged; existing guardrail cases unchanged; `test-guardrails.sh` still chains into `test-ci-harness.sh` only when all cases pass.
Must add/change: remove `exitCode`; gate fails iff duplicated-line percentage > 3 (exactly 3.0 passes, per `actual > threshold`).
Governing invariant: the only way `npx jscpd --config .jscpd.json` exits non-zero on duplication is the 3% threshold reporter, and every entry point shares that single config.
Sibling surfaces: Makefile `anti-drift` recipe; CI `anti-drift` job; `npm run dupes`; `scripts/test-ci-harness.sh` (pins literal only); `constraints.yaml:85-89` and `AGENTS.md:22/86/106/145` (declared contract, already 3%); none else reads `.jscpd.json` (grep).
Seams under test: the real jscpd binary's exit code and output on controlled fixture trees.
Invocation (orchestrator-probed on jscpd 5.0.16, scratch only): from repo root run `node_modules/.bin/jscpd --config .jscpd.json "$tmp/<fixture>"` (repo-local binary, never bare `npx` from a tmp cwd → no network). With a positional path, `pattern` resolves against the scanned path, so only the fixture is analysed (probe: `Files analyzed 3`, not the repo). Fixture layout: `$tmp/<fixture>/server/{a,b,c}.ts` — `a.ts`/`b.ts` share one ≥11-line duplicated block, `c.ts` is unique filler sized to set the ratio. Probe results with `exitCode` removed: 11/424 lines (2.59%) → exit 0; 11/174 lines (6.32%) → exit 1 with `ERROR: jscpd found too many duplicates (6.3%) over threshold (3.0%)`; the gate uses the duplicated-lines percentage. With HEAD config the 2.59% fixture exits 1 without threshold text.
Derived-config exception: the permanent mutation case writes `$tmp/exitcode.json` = real `.jscpd.json` + `"exitCode": 1` at runtime (generated from the real file, never hand-copied), proving the below-threshold fixture really contains a detected clone and that re-adding `exitCode` would be caught.
Required evidence:
- Before (HEAD config): 1 small clone (≈0.04%) → exit 1 without threshold text (RED reproduction).
- After: 0 clones → 0; ≈0.04% → 0; ≈2.8% → 0; ≈3.2% → non-zero; ≈5% → non-zero with `too many duplicates` + `over threshold`; percentages computed as duplicated lines / total lines from jscpd's own report, not console rounding.
- Permanent guardrail cases: real config accepts the below-threshold fixture; real config rejects the above-threshold fixture with `too many duplicates` + `over threshold`; the runtime-derived `exitCode` config rejects the same below-threshold fixture (proves a clone is detected there and a re-added `exitCode` is caught).
- Stop/escalate: if after deleting `exitCode` an above-threshold fixture exits 0 or lacks the threshold text, stop and report a second root cause; do NOT add a `threshold` reporter, touch other keys, or change the command literal (issue out-of-scope).
- `make anti-drift` exit 0 on the real repo; `make test-guardrails` exit 0.
Non-goals: changing the 3% threshold or constraints policy; jscpd upgrade/downgrade; changing `pattern`/`ignore`; any `continue-on-error`, step deletion, or command-literal change; product code.
Review focus: exit semantics really come from the threshold reporter; fixtures run against the real config (not a hand-rolled copy that could drift); guardrail fixtures are hermetic (tmp dir, cleaned by existing trap) and deterministic; no gate weakening anywhere.
