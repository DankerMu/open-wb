# Tasks

## 1. Fixture and baseline
- [x] 1.1 Record #105 merged baseline, existing guardrail GREEN, exact current contracts and blind spots; expanded fixture reviewer PASS and strict validation0.

## 2. Atomic harness implementation
- [x] 2.1 Add valid tracked md/csv/png and independent files.hurl covering full owner/create/conflict/tree/traversal/audit/preview/foreign404/logout chain; verify on real HTTP including empty cookie state.
- [x] 2.2 Append files.hurl to exact Make recipe and document caller copy; preserve all existing variables/lifecycle contracts, verified by guardrails.
- [x] 2.3 Provision both existing CI modes via shared helper before launches; update exact source/runtime oracle and negative mutations, copy failure propagates, no product/Playwright/uidjob changes.

## 3. Main verification and delivery
- [x] 3.1 Build real server/web, use pinned real omp/fake upstream and caller-owned state; run four-file make smoke twice without restart/reseed plus standalone files.hurl; decode PNG, exact preview bytes and no live auth sessions.
- [x] 3.2 Qualify missing/corrupt fixture and request-response/security assertion failure classes with bounded semantic RED/restoredGREEN in disposable evidence paths; explicitly retain an identical old sandbox.reject event while suppressing the current request's new audit and prove RED, then restore GREEN; preserve raw command/results.
- [x] 3.3 Guardrails576PASS/0FAIL and static/drift gates exit0; frozen d79a5740 passed three expanded static seats without findings, round1clean/zero fixpasses; CI35957055888 all8success including smoke/ui-walk. PR253 merged7e00c26; selectively archive this child with current #105-promoted canonical requirements preserved.

## Risk packs
- Selected Public API / CLI / script entry:2.1/2.2/3.1 exact Hurl/Make contract.
- Selected Config / project setup:2.3/3.3 CI caller environment and provisioning.
- Selected File IO / path safety / overwrite:2.1/2.3/3.2 tracked fixture target, no destructive tree replacement.
- Not selected Schema / columns / units / field names: no product schema or migration changes; existing network fields consumed.
- Selected Auth / permissions / secrets:2.1/3.1/3.2 tenant404/traversal audit/logout; preserve cleanenv and sanitized CIlogs.
- Selected Concurrency / shared state / ordering:2.3/3.1 same-state repeatability, audit ordering and prelaunch copy.
- Selected Resource limits / large input / discovery:2.2/3.3 exact four-file discovery, existing zero globalretry/bounded chat poll and process lifecycle unchanged.
- Selected Legacy compatibility / examples:2.2/3.3 public/auth/chat/UI and smoke-live unchanged.
- Selected Error handling / rollback / partial outputs:2.3/3.2 copy/request/assertion errors failclosed; existing cleanup controls retained.
- Selected Release / packaging / dependency compatibility:3.1/3.3 existing pinned omp/Hurl/Playwright used, no new deps/actions.
- Selected Documentation / migration notes:2.2/3.3 caller setup and selective archive; AGENTS/control-plane expansion excluded.

## Evidence before frozen review
- Native Hurl8.0.1 / Node24.13.1 / verified omp18.0.10 + real compiled server + loopback fake upstream: final same-state make smoke twice each4files/38requests pass; standalone files16requests pass; zero live auth rows.
- Old identical audit retained while an owned-DB BEFORE INSERT trigger suppresses only new sandbox.reject: failure specifically at pre/post body inequality; trigger removed and restored run passes. Missing Markdown fixture and corrupted preview bytes each fail their intended assertion, restore passes. No product source mutation.
- PNG independently decoded by Pillow as1×1 8-bit grayscale;67bytes. Both actual shared-helper modes completedexit0, copied all3exactfixturebytes; unchanged Chromium journey1passed.
- make test-guardrails exit0: CI oracle576PASS/0FAIL (baseline563/0); lint/typecheck/anti-drift exit0,zero clones. Existing preview-test lint notices unchanged.
- Pre-review compatibility repairs: Hurl8 lacks status-in predicate, singleton JSONPath results need no first/post-count filter, and captured sha256 is not string-renderable. After three cycles Main paused; user explicitly authorized one more cycle, completed with body-string pre/post comparison without weakening freshness. Fresh acceptance state excludes failed-debug login residue.
- Additional pre-review corrections: PNG payload matched its grayscale IHDR; escaped embedded Python literals repaired. Earlier negative oracle results during SyntaxError are not qualification evidence; final576PASS/0FAIL reruns baseline/positive/negative controls nonvacuously.
