## 1. Contract and baseline
- [x] 1.1 Compact fixture first-round review PASS, user-approved min−1 correction recorded on issue91 and all134 existing source/test/judge inputs frozen. Parent independent array reference passes five public cases; off-by-one, epoch-mixing, completed-turn and input-alias bad candidates fail semantically, restore GREEN and dedicated stability passes. Missing production module remains a SETUP absence, not fabricated prior behavior.
- [x] 1.2 First public tracer was written and run before production file creation; missing import was retained as SETUP. Parent's independent callable wrong models supplied semantic boundary RED before candidate authorization, not a fabricated prior-production baseline. Writer implemented the full real ring after the tracer, so most subsequent cases were already GREEN; this vertical-TDD deviation is disclosed rather than relabeled.
## 2. Pure ring implementation
- [x] 2.1 Fixed1000 circular retention, epoch-local IDs and ordered successor replay implemented. User-approved1001/min−1 and1002 true-gap boundaries, canonical grammar/epoch/empty/tail behavior and instance isolation are covered; no shift or retained-buffer copy on insertion.
- [x] 2.2 Active/completed/missing/evicted turn starts, cursor precedence and immutable input/result-array behavior covered. A real payload-alias counterexample failed before repair; correcting an invalid attempt to mutate a frozen owned payload is retained in the writer report. Green checkpoint6ffad73 preceded cleanup: one flat ChatEvent snapshot replaces per-variant field mirroring; replay iteration starts at the requested sequence, with no predicate closure or unrelated-slot scan. Two implementation-identity assertions removed; observable mutation/isolation assertions preserved.
## 3. Acceptance and delivery
- [x] 3.1 Parent compiled API passes five exact cases,6400 interleaved pushes/4046 differential checks over multiple wraps, and final line-terminator grammar checks. Four bad candidates from the actual compiled module fail semantically; restoration and dedicated public/holdout stability pass. Server62files/1138tests pass (92.03% statements/88.02% branches; ring98.52%/95%); Biome/types/build/anti-drift zero-clones/strict all exit0. All134 existing source/test/judge identities remain unchanged; no threshold/discovery changes.
- [x] 3.2 PR212 merged after compact correctness+test-evidence first-round PASS, no fix pass, head1592676e12b2fff11c92f03473dc3990abcb372a and CI35797673906 eight successful jobs. Prepare the separate added-requirement promotion/archive and parent4.1 update; its independent PR/CI gate must pass before queue advancement. Existing pure mapper requirements remain unchanged.

## Core risk mapping
Selected: entry/API→2.1 constructor/push/since; schema/units→2.1 canonical epoch/seq and numeric ChatEvent; concurrency/state→2.2 deterministic active-turn/read transitions, no actual concurrency; resources→2.1 fixed1000 O(1) insertion; compatibility→2.1 reuse event union and approved min−1; errors→2.1 malformed cursor becomes gap; test/evidence→1.2/3.1 exact ordered payloads and qualified mutants; docs/release→3.2 canonical append and same-headCI. Not selected: config/environment (no new key), filesystem/IO (pure), auth/secrets (no auth/credential surface).

## Domain risk mapping
Selected: auth/session lifecycle only for isolated in-memory epoch/turn identity (2.1/2.2); server/web HTTP-envelope compatibility only for future cursor/event payload protocol (2.1; no route mounted); offline deployability (zero dependencies,3.1). Not selected: tenant/sandbox enforcement, process/child-environment, SQLite migration/catalog, browser runtime/navigation/persistence, cross-service network boundary. No expanded trigger from project-profile is touched.

## Ownership and verification
One implementer/current checkout/no worktree/no nesting; production/test scope is ring-buffer.ts and session-ring-buffer.test.ts only. Parent owns fixture, protected examples/holdout/qualification and final acceptance. Writer skips project build/type/lint/format/fullsuite/git; may run only focused Vitest during vertical TDD. Reviewers skip all validation. Parent focused command `npm exec --workspace server -- vitest run --no-coverage test/session-ring-buffer.test.ts`; final `npm run test --workspace server`, scoped Biome, server types/build, `make anti-drift`, strict OpenSpec. Evidence root `/tmp/open-wb-issue91-evidence`. Same-user local isolation is an authority convention, not an OS enclave; independent CI remains mandatory.

## Verification deviations
The initial snapshot cleanup's generic Object.freeze inference failed TypeScript; a single documented discriminated-union spread assertion repaired typing without reintroducing a second payload schema, runtime fallback or an `any` cast. Final typecheck and all behavioral gates passed. Missing-module SETUP, independently qualified semantic wrong models, writer-discovered alias RED, already-GREEN later cases and the frozen-object test correction are distinct in `/tmp/open-wb-issue91-evidence`; no claim that all cases went RED against prior production code. The exact-array permanent expectation was shortened to contiguous IDs plus literal endpoint/midpoint payloads; the independent parent oracle still checks every retained record's payload.
