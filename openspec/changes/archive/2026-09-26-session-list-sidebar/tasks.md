## Risk Packs
- Legacy compatibility — selected: list semantics, fences, locator names → 1.3, 1.6, 2.4.
- Accessibility — selected: landmark placement, overlay close + focus return → 1.2, 2.2, 2.3.
- Concurrency / state ownership — selected: list state stays in ChatPage; slot node lifecycle (report/clear, narrow/wide swap, StrictMode) → 1.1, 1.3, 2.2, 2.4.
- Documentation — selected: spa-shell + chat-web main/parent deltas, parent proposal #7, checklist CH-15/CH-07 → 1.8, 1.9, 2.6.
- Public API, Config, Schema, File IO/path safety, Auth/secrets, Resource limits, Error handling, Release — not selected: frontend layout only; no API/config/storage-key change (`workbuddy-sidebar` rules preserved, 2.2).

## 1. Implementation
- [x] 1.1 `web/src/lib/sidebar-slot.tsx` per design D1 (setter/value split contexts, `useSidebarSlot(node)` layout-effect report + clear, `useSidebarSlotContent()`, `SidebarNavigateContext`/`useSidebarNavigate()`); mount `SidebarSlotProvider` in `AppShell` as its own component with stable `children` (state must not live in `AppShell`, or ChatPage re-renders with it). Outside a provider: `useSidebarSlot` is a no-op, `useSidebarSlotContent()` returns null, `useSidebarNavigate()` returns undefined. Only a small list-area child inside `Sidebar` reads the value, so each report re-renders that child, not the whole sidebar.
- [x] 1.2 `web/src/routes/shell/sidebar.tsx` + `sidebar.css`: list-area `div.sidebar-main` between `主导航` and `AuthFooter` rendering the slot node, not rendered when collapsed; overlay wraps it in `SidebarNavigateContext` with `onNavigate`; styles per D4 with exactly one scroll container for the list (`新建会话` pinned above it; drop the extra `overflow-y`/`height:100%` layer); narrow `.sidebar nav`, `.sidebar nav ul` and overlay `nav` rules (`sidebar.css:45-60,263-265`) to the main nav only (overlay variant keeps Drawer scroll behavior).
- [x] 1.3 `web/src/features/chat/conversation-view.tsx` / `page.tsx`: extract `SessionNav` (existing nav/button/alerts/entries, no `.chat-sidebar` wrapper; calls `useSidebarNavigate()` after select/new per D5); `ChatPage` reports it with `useSidebarSlot(node)`; `ConversationView` renders only the main column. Watch the 800-line size guard (`page.tsx` 726 lines): put `SessionNav` in its own file if needed.
- [x] 1.4 `web/src/features/chat/chat.css`: D3 card row (≥761 nowrap without `overflow: hidden`, ≤760 wrap; card title/desc stay inside the narrowest card), D6 single-column grid, delete `.chat-sidebar` and the `≤760` list-stack rules; list styles adapted for the sidebar (D4).
- [x] 1.5 `web/e2e/ui-walk.spec.ts` (+ helpers in `web/e2e/ui-walk-layout.ts`; spec file is 710/800 lines): E1 layout step (1440×900 and 1024×768 via `withViewport` on desktop-light; 390×844 on mobile-dark); desktop placement check (list inside `aside[aria-label="侧栏"]` after `主导航`, not in `main`). At 390, `walkHeldDialogue` and the `expectRunningPrefix`/`expectCompletedPair` helpers (`:641-645`, `:704-708`) must follow open → act or read list status → close (Escape or the select closing it) before any `main` query, because an open overlay aria-hides the app root; reuse `inspectSidebar` (`ui-walk-layout.ts:199-208`, export it first). Assert the overlay closes after select/new.
- [x] 1.6 `web/e2e/ui-shots.mjs`: confirm `createDoneSession` (first cell 1440-light, desktop) still finds `新建会话` / list status in the sidebar; other cells only `goto` + `waitDoneConversation` and need no change unless they touch the list.
- [x] 1.7 jsdom: slot host in the existing flat helper `web/test/chat-page-lifecycle-support.tsx` (repo convention `*-support.tsx`, no `web/test/support/`); update `chat-page-lifecycle.test.tsx`, `topbar.test.tsx` (T7), chat-composer CSS-text tests, `routes.test.tsx` and any other failing chat-page/ownership suites; add sidebar + app-shell-responsive cases for E2 (incl. the non-interactive-click-does-not-close case: `yieldMacrotask()` before `pressPointer`, as `web/test/ui-dialog.test.tsx:526-529` does, plus a positive control in the same test where pressing the backdrop does close) and the E5b CSS assertions.
- [x] 1.8 `docs/acceptance/demo-parity-checklist.md`: CH-15 behavior + status (sidebar placement = S1e item; grouping stays S1c not-applicable); CH-07 evidence column cites the new ui-walk step; SH-02 (`:99`) drops the outdated 会话列 wording; CH-04 adds the ≥761 single-row card rule and its evidence; line refs stay pinned per header `:11` (new evidence cited as `path:行（@<merge SHA>）` + test/step name).
- [x] 1.9 Spec deltas in this change; parent deltas `openspec/changes/s1e-frontend-parity/specs/{spa-shell,chat-web}/spec.md` synced (spa-shell list sentence + scenario edits + new scenario; chat-web three identical clauses); parent `proposal.md` deviation #7 (orchestrator, done). Archive order: this change before s1e; if s1e archives first, rebase the MODIFIED blocks on the new main before archiving.

## 2. Verification
- [x] 2.1 E1 RED: new ui-walk layout step run against the pre-change build with both projects in normal order (desktop-light then mobile-dark) fails as expected at 1024 and 390 (1440 passes); record failing assertion lines.
- [x] 2.2 E2 jsdom shell placement / collapsed / overlay (incl. non-interactive click keeps it open) / storage cases pass; E5b CSS assertions pass.
- [x] 2.3 E3 local ui-walk (CI env recipe, `bash .github/scripts/ci-compiled-server.sh ui-walk`) exit 0, both projects.
- [x] 2.4 E4 existing chat semantics suites pass unchanged in assertions (only harness/host changes).
- [x] 2.5 E5 local `make ui-shots` against a compiled server (fresh DB, fixture sandbox, verified omp, fake upstream) → `截图 60/60，失败 0`; screenshots handed to the orchestrator for visual review.
- [x] 2.6 `make lint`, `make typecheck`, `make test`, `make anti-drift` exit 0; `openspec validate session-list-sidebar --strict --no-interactive` and `openspec validate s1e-frontend-parity --strict --no-interactive` pass.
