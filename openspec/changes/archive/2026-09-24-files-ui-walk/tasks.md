## 1. Fixture
- [x] 1.1 Expanded fixture review PASS and strict validation0 before editing journey.
## 2. Journey
- [x] 2.1 Add files segment through real UI with ready workspace list, create/select smoke-fixture and actual three-file root readiness.
- [x] 2.2 Assert exact Markdown rendered/source and CSV header/data; create absent root walk-out and prove same ws/selected workspace/files/directory after reload.
- [x] 2.3 Preserve existing chat/theme/logout/auth console oracle, caller ownership and unchanged time/retry/config limits.
## 3. Qualification and delivery
- [x] 3.1 Main wrong-owned-Markdown semantic RED, restore full make ui-walk GREEN, real screenshots and zero unexpected browser errors; select and fresh-create branches exercised across local/CI.
- [x] 3.2 Lint/type/drift/fixture checks and exact-head CI pass; frozen expanded review, automatic merge and selective archive.

Local evidence: Node24.13.1, real omp18.0.10, compiled server/web and owned loopback fake-upstream. Wrong owned Markdown heading caused make ui-walk exit2 at the exact preview h1 assertion (5.8s); restoring tracked bytes produced full journey1passed (4.8s), exit0, including existing-workspace selection, directory creation and unchanged exact2unauthorizedME oracle. Fresh creation executed before the negative preview failure; fresh full CI remains pending. Four real browser screenshots retained (rendered/source/CSV/reloaded directory), separate capture unexpectedErrors=[]; owned server/upstream stopped exit0. Lint/type/builds/anti-drift exit0; no product/config/fixture changes.

Final evidence: PR262 headc364a9580beb341b3bf4ae704a3003cbec51a612, CI35984311706 all9success, fresh UI job107583281945 full1passed6.2s; merged9ebb3e723a989435ae4b387acc5a268b64de2415. Expanded three-seat review findings=[],round1clean/0fixpasses. Local select branch and CI fresh-create branch both complete; prior pending CI note above is superseded. Full summary https://github.com/DankerMu/open-wb/pull/262#issuecomment-5812159602 . Caller-owned runtime and screenshot executable removed; four screenshots and logs retained.

## Risk packs
- Selected Public API / CLI / script entry:2.1/3.1 same make ui-walk entry.
- Not selected Config / project setup: unchanged config/workflow/fixtures.
- Selected File IO / path safety / overwrite:2.2/3.1 root directory owned sandbox only, no deletion oroverwrite in journey.
- Not selected Schema / columns / units / field names: no schema or persistedformat change.
- Selected Auth / permissions / secrets:2.3 exact two unauthorized ME and no broadened console suppression.
- Selected Concurrency / shared state / ordering:2.1/2.2 list/root readiness and same workspace reload; no sleeps.
- Selected Resource limits / large input / discovery:2.3 existing timeouts/retries/testdiscovery unchanged.
- Selected Legacy compatibility / examples:2.3 existing realchat and theme/logout flow intact.
- Selected Error handling / rollback / partial outputs:3.1 wrongfixture rejected, restore before acceptance, existing error oracle remainsactive.
- Not selected Release / packaging / dependency compatibility: no dependency/config/build changes.
- Selected Documentation / migration notes:3.2 preserve complete currentcanonicalUIrequirement and selective child promotion, no parentoverwrite.
