## Context and Decision
Project profile openspec/project-profile.md. Scope is one existing four-row test matrix, no production/API/config changes. Use a stable unique test secret constant near the matrix; all four inputs and message/stack exclusion assertions consume it. Keep 200 non-JSON, malformed 500 envelope, 500 non-JSON and network rejection status0, stable fallback assertions, and raw stack comparisons.
Must preserve all test rows and production behavior; no stack normalization, suppression, skipping, API edits or unrelated logout/field-validation marker changes.
Seam declared by #69: createApiClient().getInfo() through mocked fetch boundary, checking actual ApiError message and stack. Parent verifies from physical macOS checkout paths with/without /private. A temporary production-copy mutant leaking the marker into message or stack must fail the same four-case oracle; no mutant enters commits.
Risk packs: Public API/CLI, config/setup, file IO/path safety, schema, auth/permissions/secrets, concurrency, resource limits, legacy compatibility, error handling runtime, release/dependencies: not selected (fixture-only marker substitution, none of these implementations/contracts change). Documentation/migration notes selected: this verification spec records the oracle invariant; no migration. All project domain packs not selected: no runtime surface changes.
Evidence-related review exemption is conditional: Phase2 must show four scenarios preserved, path parity, both message/stack mutation discrimination and unchanged production/CI. If audit finds risk, escalate from none rather than skipping review.
