# Spec: ui-primitives

## MODIFIED Requirements

### Requirement: 设计 token 全集
`web/src/styles/tokens.css` SHALL 定义 demo（`resource/workbuddy-live-demo.html:19-188`）的调色板层（`--wb-palette-*`）与语义层（`--wb-brand-*`、`--wb-bg-*`、`--wb-text-*`、`--wb-border-*`、`--wb-status-*`、`--wb-shadow-*`、`--wb-icon-*`、`--wb-font*`、`--wb-mono`）全部 token，浅色在 `:root`、深色在 `[data-theme="dark"]`，值逐字取自 demo（"逐字"指经格式归一化——hex 小写、空白折叠、数字字面量规范化（`.10`→`0.1`）、折行合并——后相等，因 biome 会重排格式）；demo 引用但未定义的六个变量 SHALL 在此补齐为固定值并注明"demo 缺失、本仓补定"：`--wb-palette-black-60: rgba(0,0,0,.6)`、`--wb-palette-white-10: rgba(255,255,255,.1)`、`--wb-palette-white-20: rgba(255,255,255,.2)`、`--wb-palette-white-60: rgba(255,255,255,.6)`、`--wb-text-white: var(--wb-palette-white-100)`（以上两块同值）、`--wb-home-composer-chip-bg-hover`: 浅色 `var(--wb-palette-gray-3)`、深色 `var(--wb-bg-hover)`。每块的变量名集合 SHALL 恰为 demo 对应块名集合加这六个名，不得多出其它变量。文件头 SHALL 保留 token 来源说明（WorkBuddy 5.3.11 token 文件，经 demo；ATTRIBUTION.md §4）。`--wb-font-heading` 是唯一允许与 demo 值不同的变量：SHALL 去掉 `Poppins`、只列本地/系统字体栈，不得 `@import`/`<link>` 任何公网字体。仓内既有但 demo 无的 `--wb-home-bg`/`--wb-control-bg` SHALL 删除，消费者改用值相同的 demo token `--wb-home-bg-primary`/`--wb-color-bg-input`；`web/src/**/*.css` 引用的每个 `var(--wb-*)` SHALL 在 `tokens.css` 有定义（`web/test` 断言引用集合 ⊆ 定义集合）。feature 样式 SHALL 只引用语义层 token，不得直接引用 `--wb-palette-*` 或硬编码颜色（`web/test` 以 grep 断言 `web/src/features/**/*.css`、`web/src/features/**/*.tsx` 与 `web/src/routes/**` 无 `#[0-9a-fA-F]{3,8}`、`rgba?(`、`--wb-palette-`）。

#### Scenario: token 完整且分层
- WHEN 读取 `tokens.css` 并对照 demo `:root` 与 `[data-theme="dark"]` 两块
- THEN demo 定义的每个 `--wb-*` 变量名在对应主题块中存在且归一化后值相等（`--wb-font-heading` 期望为去掉 `Poppins,` 后的串）；六个补定变量在两块中都存在且等于固定值；每块名集合恰为 demo 名集合 ∪ 六个补定名；`web/src/**/*.css` 无未定义的 `var(--wb-*)` 引用；`web/src/features/**/*.css`、`web/src/features/**/*.tsx`、`web/src/routes/**` 中无硬编码颜色（含 `rgba(`）与调色板引用；构建产物无公网字体请求

### Requirement: 基元组件库
`web/src/ui/index.ts` SHALL 是基元层的唯一出口，feature 与 routes 只经它导入基元；截至本切片导出 `Icon`、`BrandMark`、`Button`、`Input`、`Switch`、`Tag`、`Chip`、`Dialog`、`ConfirmDialog`、`Drawer`、`Menu`（及 `MenuItem` 类型）、`Popover`、`Tooltip`、`SegmentedControl`、`ToastProvider`、`useToast`、`EmptyState`。基元组件 SHALL 用类名 + token 取色：`web/src/ui/**/*.css` 可引用语义 token 与 `--wb-palette-*`（基元层是调色板到组件的唯一映射边界，demo 组件规则直接引用调色板处照搬），不得出现 hex/`rgba(` 颜色字面量；`web/src/ui/**/*.tsx` 不得出现颜色字面量、`--wb-palette-` 引用与 `style={` 内联样式；每个组件根元素类名以 `ui-` 开头且不与既有 `.ui-button*`/`.ui-alert`/`.ui-muted`/`.ui-empty` 撞名（既有类与其消费者在迁移切片前不动）。`BrandMark` SHALL 只含自有 mark SVG（沿用现有对勾图形）与可选字标 `WorkBuddy`，`size` 可配，不含任何上游品牌资产。
`Button` SHALL 提供 variant `primary|secondary|ghost|danger`（默认 secondary）与 size `sm|md|lg|icon`（默认 md），类名 `ui-btn ui-btn--<variant> ui-btn--<size>`，默认 `type="button"`（调用方可覆盖），透传原生 button 属性；`loading` 时 SHALL `disabled` 且 `aria-busy="true"`、加类名 `ui-btn--loading`、渲染 `aria-hidden` 且绝对定位（脱离文档流）、按变体上色（primary/danger 用 `--wb-text-white`，深色 primary 用 `--wb-palette-black-90`）的旋转指示器，children 仍在 DOM 并占位（文字透明——由 `.ui-btn.ui-btn--loading` 置于 button.css 末尾实现，且所有 hover 色规则带 `:not(:disabled)`，任何变体/主题/hover 下 label 都不显示），按钮宽度与非 loading 态一致；`size="icon"` 为 32×32 方形，调用方须提供 `aria-label`。样式映射 demo:570-586。
`Input` SHALL 渲染 `<input class="ui-input">` 并透传原生属性；`variant="search"` SHALL 渲染 `ui-input ui-input--search` 容器（`Icon name="search"` + `<input type="search">`）。样式映射 demo:606-612。
`Switch` SHALL 基于 `@radix-ui/react-switch`（`Root` 类名 `ui-switch`、`Thumb` 类名 `ui-switch-thumb`），`role="switch"`、`aria-checked` 与 `data-state` 由 Radix 提供，`checked`/`defaultChecked`/`onCheckedChange`/`disabled` 透传；jsdom 内放进 `<form>` 或传 `form` 属性的测试须引入 `web/test/radix-platform.ts`（Radix 表单桥接在表单内构造 ResizeObserver）。样式映射 demo:614-619。
`Tag` SHALL 提供 tone `brand|success|warning|error|neutral`（默认 neutral），类名 `ui-tag ui-tag--<tone>`。样式映射 demo:599-604（含 demo:601 深色 brand 文字色覆盖）。
`Chip` SHALL 是 `<button type="button">`，`selected` 映射 `aria-pressed` 与类名 `ui-chip--selected`，`onSelect` 映射点击。样式只映射 demo:622-625 的 `.filter-chip`（欢迎页 `.quick-row .qk` 形态不属于本组件）。可聚焦组件（Button/Input/Switch/Chip）的 css SHALL 自带 `:focus-visible` 规则重申自身圆角，以压制全局 `:focus-visible { border-radius: 4px }`；Button/Input/Switch/Chip 的过渡 SHALL 在 `prefers-reduced-motion: reduce` 下禁用（各组件 css 自带媒体块）。五个组件的 props SHALL 允许 `ref`（基于 `ComponentProps`，`ref` 落到根元素；search 变体落到内层 `<input>`）。
`Dialog` SHALL 基于 `@radix-ui/react-dialog`：受控 `open`/`onOpenChange`，`title` 必填并经 `aria-labelledby` 关联，`description` 可选并经 `aria-describedby` 关联（无 description 时不产生 Radix 告警），`size` `sm|md`（默认 md；类名 `ui-dialog ui-dialog--<size>`，宽 400/520，`max-height: 82vh`），内容 portal 到 `document.body` 且组件显式设置 `aria-modal="true"`（Radix 不输出该属性）；打开时聚焦内容内首个可聚焦控件或 `initialFocus` 指向的元素，Tab/Shift+Tab 在内容内循环，Escape 与遮罩点击调用 `onOpenChange(false)`，`dismissible=false` 时二者均不关闭且不渲染右上 `关闭` 按钮；关闭后焦点 SHALL 回到 `returnFocus` 指向的元素；未传 `returnFocus` 且无 `trigger` 时回到打开瞬间的活动元素（组件经 `onOpenAutoFocus` 记录、经 `onCloseAutoFocus` 归还）；有 `trigger` 且未传 `returnFocus` 时交给 Radix 归还 trigger；标题为 `h2`（Radix `Title` 默认），标题内图标 `aria-hidden`。样式映射 demo:730-738。
`ConfirmDialog` SHALL 是 `role="alertdialog"` 变体（`returnFocus` 透传 Dialog 内核；可选 `children` 渲染在 body，作为 description 之外的补充内容）：`size="sm"`、无右上关闭按钮、遮罩点击不关闭、Escape 视为取消；footer 固定为 `取消`（`cancelText` 可改，`Button variant="ghost"`）与确认按钮（`confirmText`，`danger` 时 `variant="danger"` 否则 `primary`，`pending` 时 `loading`），确认按钮点击调用 `onConfirm`，取消调用 `onOpenChange(false)`；标题前渲染 `Icon`（`danger` → `triangle-alert`，否则 `info`）。样式映射 demo:1116-1128。
`Drawer` SHALL 基于同一 Radix Dialog：`side` `left|right`（默认 right，类名 `ui-drawer--<side>`、`data-side`），`width` 枚举 `288|420`（默认 420，类名 `ui-drawer--w<width>`；`max-width: 92vw`；左侧 288 供侧栏覆盖层），`title` 必填，右上 `关闭` 按钮、Escape 与遮罩关闭；不写内联 `style`。样式映射 demo:772-777、1129-1140。
`web/test/radix-platform.ts` SHALL 只在 jsdom 缺失时补 `ResizeObserver`、`Element.prototype.{hasPointerCapture,setPointerCapture,releasePointerCapture,scrollIntoView}` 四项，不覆盖已有实现、不补 `PointerEvent`（jsdom 29 已有）；渲染 Radix 覆盖层基元的测试 SHALL 引入它。Dialog/Drawer 的入场动画 SHALL 在 `prefers-reduced-motion: reduce` 下禁用。`Icon` 名集合 SHALL 含 `triangle-alert` 与 `info`。
`Menu` SHALL 基于 `@radix-ui/react-dropdown-menu`：props `trigger`（`Trigger asChild` 的单一元素）与 `items: MenuItem[]`（`MenuItem = { label; onSelect; danger?; icon?: IconName }`）；`Root modal={false}`（打开期间不给 body 加 `pointer-events: none`、不给页面其它元素加 `aria-hidden`，外点关闭且点击到达目标）、`Content loop`；content 类名 `ui-menu`、`role="menu"`、`aria-labelledby` 指向 trigger，项类名 `ui-menu-item`（`danger` 加 `ui-menu-item--danger`，`icon` 渲染 `Icon size={14}`）、`role="menuitem"`；trigger 由 Radix 加 `aria-haspopup="menu"`/`aria-expanded`；键盘打开后焦点落在首项，上下/Home/End 移动且首尾环绕，Enter/Space/点击触发该项 `onSelect` 并关闭，Escape/外点关闭，关闭后焦点回 trigger；组件不写任何键盘或焦点处理代码。样式映射 demo:761-770（z-index 1600）。
`Popover` SHALL 基于 `@radix-ui/react-popover`：props `trigger`、`children`、可选受控 `open`/`onOpenChange`（未传时非受控）、`contentRole`/`contentLabel`；content 类名 `ui-popover`，默认保持 Radix 的 `role="dialog"`，`contentRole`/`contentLabel` 仅在传入时展开为 `role`/`aria-label`（不得以 `undefined` 覆盖默认角色）；打开聚焦 content 内首个可聚焦元素，Escape/外点关闭，关闭后焦点回 trigger；content 带 Radix 输出的 `data-side`/`data-align`（视口翻转由 ui-walk/ui-shots 取证）。样式映射 demo:779-780（z-index 1300，宽 300px）。
`Tooltip` SHALL 基于 `@radix-ui/react-tooltip`：props `label: string`、`children`（`Trigger asChild` 的单一元素）、`side`（默认 `top`）；每个实例自带 `Provider delayDuration={300} disableHoverableContent`（离开 trigger 即隐藏，不建悬停宽限区）；trigger 聚焦时立即显示、指针悬停 300ms 后显示，blur/离开/Escape 隐藏；可见 content 类名 `ui-tooltip`、本身即 `role="tooltip"`（Content 不传 `aria-label`，故 Radix 不渲染隐藏副本）且带 `data-side`，显示期间 trigger `aria-describedby` 指向它。demo 无 tooltip 规则，样式以 token 类比定义（z-index 1700，深色主题反转底/字色）。
`SegmentedControl` SHALL 基于 `@radix-ui/react-radio-group`：props `value`、`onValueChange`、`options: { value; label }[]`、`label`（映射 root `aria-label`）；root 类名 `ui-seg`、`role="radiogroup"`；每项类名 `ui-seg-item`、`<button type="button" role="radio">`、`aria-checked` 与 `data-state="checked|unchecked"` 由 Radix 提供，选中态样式挂 `[data-state="checked"]`（深色主题反转底/字色）；点击与方向键（首尾环绕）切换均调用 `onValueChange(下一值)`；Tab 进入组时焦点落在选中项（Radix roving focus：初始根元素 `tabIndex=0`、各项 `-1`，根元素获焦后选中项 `0`）；表单内渲染不抛错。样式映射 demo:558-562（`#fff`/`#111` 以 `--wb-text-white`/`--wb-palette-black-90` 替代）。
Menu/Popover/Tooltip 的入场动画与 Menu 项过渡 SHALL 在 `prefers-reduced-motion: reduce` 下禁用；`SegmentedControl` 项 css SHALL 自带 `:focus-visible` 圆角重申。`web/test/ui-support.ts` SHALL 提供 `yieldMacrotask`/`pressPointer`/`ruleBody` 三个 helper 供覆盖层基元测试共用（`ui-dialog.test.tsx` 改为导入，不再本地定义）。
`ToastProvider`/`useToast` SHALL 基于 `@radix-ui/react-toast`：`ToastProvider` 内含 Radix `Provider duration={2400} label="通知"` 与 `Viewport`（类名 `ui-toast-viewport`，`role="region"` 可访问名 `通知`），并持有通知队列——`useToast().show({ type: "success"|"error"|"info", message })` 追加一条，同时最多 3 条（超出丢最旧），Radix 计时到期（2.4s，hover/focus 暂停）、Escape 或滑动关闭时从队列移除；队列从非空变空时 SHALL 以递增的 `key` 重挂 Radix `Provider`+`Viewport`（应用子树在 Radix Provider 之外、不随之重挂），使 Radix 留在 Provider 内的暂停标记归零——否则悬停/聚焦中关掉最后一条后，下一条永不自动关闭；每条渲染为 `Toast.Root type="background"`（Radix 以 `role="status" aria-live="polite"` 的隐藏播报区宣读）、类名 `ui-toast ui-toast--<type>`、类型图标（success `circle-check`、error `triangle-alert`、info `info`，`aria-hidden`）与 `Toast.Description` 类名 `ui-toast-message`；组件不自设任何定时器；`useToast` 在 `ToastProvider` 外调用 SHALL 抛出含 `ToastProvider` 字样的错误。`ToastProvider` SHALL 在 `web/src/main.tsx` 包裹 `RouterProvider`（应用根唯一一处），jsdom 页面级 fixture `web/test/render-app-router.tsx` SHALL 同样包裹；feature 只经 `useToast()` 触发。样式映射 demo:753-758、1044-1052（z-index 2000，顶部 52px 居中；离场动画不移植）。
`EmptyState` SHALL 渲染 `div.ui-empty-state`：可选 `icon`（`IconName`，容器 `ui-empty-state-icon` 64px 圆角 20，图标经 css 放大到 28px）、必填 `title`（`p.ui-empty-state-title`）、可选 `description`（`p.ui-empty-state-desc`）与可选 `children`（操作区，置于最后）；类名与既有 `.ui-empty` 零交集。样式映射 demo:724-728。
`web/src/features/**` 与 `web/src/routes/**` 的 `.ts/.tsx` SHALL 不直接 import `@radix-ui/*`（静态与动态 import 均以 grep 守卫断言；`web/src/ui/**` 为唯一允许处）。`Icon` 名集合 SHALL 含 `circle-check`。
既有手写 `<dialog>` 的迁移 SHALL 分两刀且行为等价：auth 侧（`web/src/features/auth/footer.tsx`）的退出确认 SHALL 由 `ConfirmDialog` 渲染（`danger`、`confirmText="退出"`、`cancelText` 在 pending 时为 `关闭` 否则 `取消`、`pending` 透传为确认按钮 loading、pending 提示行经 `children`），feature 不再持有 `<dialog>`/焦点循环/键盘处理代码，`returnFocus` SHALL 在关闭时刻按 trigger 是否禁用归还到 trigger 或 `aside` 内 `a[aria-current=page]`（两支各有断言）；退出语义（标题 `退出登录？`、说明文案、恰一次 `POST /api/auth/logout`、同 tick 双击去重、pending 中关闭不取消请求、失败回滚并显示 `alert`、卸载中止请求）SHALL 与迁移前一致；`styles.css` 的 `.logout-dialog*` 规则 SHALL 随之删除。jsdom 侧因 Radix portal 与 `hideOthers` 的定位改法 SHALL 为：打开期间取页面元素用 `{ hidden: true }`、原生 `HTMLDialogElement.open`/`cancel` 事件改为 `aria-modal` 断言与 Escape keydown、焦点归还断言经 `waitFor`；渲染该 footer 并打开对话框的测试 SHALL 引入 `web/test/radix-platform.ts`。files 侧（`web/src/features/files/{dialogs,page,tree}.tsx`）SHALL 同样只经基元：`新建工作空间`/`新建文件夹` 两个表单对话框由 `Dialog` 渲染（`busy` 为请求进行中；遮罩点击关闭为与 demo 对齐的新增取消入口，与 `取消` 同义，其余行为等价），`＋` 创建菜单由 `Menu` 渲染，工作空间切换器由受控 `Popover`（`contentLabel` `工作空间切换器`，默认 `role="dialog"`）渲染；`web/src/lib/dialog.ts`、`web/test/dialog.test.tsx` 与已无使用者的 `web/test/dialog-platform.ts` SHALL 删除，`web/src` SHALL 不再含 `<dialog`、`showModal`、手写 `role="menu"`。
`ATTRIBUTION.md` §3 的 Radix 条目 SHALL 列出当前已安装的 Radix 包。

`Dialog` 与 `ConfirmDialog` SHALL 在忙碌期间把焦点留在模态内：`Dialog` 接受可选 `busy`（默认 false），`ConfirmDialog` 以 `pending` 作为 `busy`；`busy` 由 false 变 true 的那次提交之后，若活动元素是内容内已禁用的控件，或活动元素为 `document.body`/空，焦点 SHALL 移到内容内首个未禁用的可聚焦控件（`ConfirmDialog` 在 children 无可聚焦元素时即取消按钮；auth 退出在 pending 时把该按钮文案设为 `关闭`）；活动元素是内容内未禁用的控件或在内容外的其他元素时不移动；`busy` 保持为 true 的后续渲染不再移动焦点。`Button` `loading` 的原生禁用语义不变。真实浏览器中，确认按钮被原生禁用引发 focus fixup 后，Tab/Shift+Tab 仍停在模态内，背景 SHALL NOT 经键盘获焦。
#### Scenario: 出口与品牌
- WHEN 在 jsdom 渲染 `<BrandMark />` 与 `<BrandMark wordmark />`，并静态 grep `web/src/ui/**/*.tsx`
- THEN `BrandMark` 渲染 svg，仅 `wordmark` 时出现文本 `WorkBuddy`；`web/src/ui/**/*.tsx` 无 `style={`；`Icon`/`BrandMark`/`Button`/`Input`/`Switch`/`Tag`/`Chip` 可从 `web/src/ui/index.ts` 导入

#### Scenario: 表单级基元可访问且状态可断言
- WHEN 在 jsdom 渲染 `<Button>保存</Button>`、`<Button loading>保存</Button>`、`<Button size="icon" aria-label="发送" />`、`<Input aria-label="账号" />`、`<Input variant="search" placeholder="搜索" />`、`<Switch aria-label="深色" checked={false} onCheckedChange={fn} />`、`<Tag tone="error">失败</Tag>`、`<Chip selected onSelect={fn}>日常</Chip>` 并点击可交互者
- THEN 默认按钮 `role=button`、`type="button"`、类名 `ui-btn ui-btn--secondary ui-btn--md`；loading 按钮 `disabled`、`aria-busy="true"`、类名含 `ui-btn--loading`、文本 `保存` 仍在 DOM、含 `aria-hidden` 指示器、点击不触发 `onClick`，且 `button.css` 中指示器为 `position: absolute`、最后一个规则块选择器为 `.ui-btn.ui-btn--loading` 且含 `color: transparent`、每个 `:hover` 选择器含 `:not(:disabled)`；`tag.css` 含 `[data-theme="dark"] .ui-tag--brand`；button/input/switch/chip 四个 css 各含 `:focus-visible` 规则（含 `border-radius`）与 `prefers-reduced-motion: reduce` 块；`<Input ref={r} />` 渲染后 `r.current` 为 `HTMLInputElement`；icon 按钮可访问名 `发送`、类名含 `ui-btn--icon`；`Input` 为 `role=textbox` 类名 `ui-input`，search 变体为 `role=searchbox` 且容器 `.ui-input--search` 含 svg；`Switch` 为 `role=switch` `aria-checked="false"`，点击后 `fn(true)`，`disabled` 时点击不触发；`Tag` 类名 `ui-tag ui-tag--error`；`Chip` `aria-pressed="true"`、类名含 `ui-chip--selected`、点击触发 `fn` 一次

#### Scenario: 基元层取色边界
- WHEN 静态扫描 `web/src/features/**/*.{css,tsx}`、`web/src/routes/**`、`web/src/ui/**/*.{css,tsx}`
- THEN 所有路径无 `#[0-9a-fA-F]{3,8}` 与 `rgba?(` 字面量；features/routes 与 `web/src/ui/**/*.tsx` 无 `--wb-palette-`；`web/src/ui/**/*.css` 引用的 `--wb-palette-*` 与 `--wb-*` 均在 `tokens.css` 有定义

#### Scenario: 模态基元可访问且关闭策略可断言
- WHEN 在 jsdom（引入 `radix-platform.ts`）渲染受控 `<Dialog open title="新建" description="说明" footer={…}>`（body 内含 input 与按钮）、`<Dialog open dismissible={false} …>`、`<Dialog open initialFocus={ref} …>`、`<ConfirmDialog open title="退出登录？" description="…" confirmText="退出" danger onConfirm={fn} …>`、`<Drawer open title="导航" side="left" width={288}>`，并触发 Tab/Shift+Tab、Escape、遮罩点击（完整 pointerdown→click 序列，让出一个宏任务后）、按钮点击与 `open` 翻转
- THEN `dialog` 位于 `document.body` 直接子树、`aria-modal="true"`、`aria-labelledby` 指向标题、`aria-describedby` 指向说明；默认焦点落在内容内首个可聚焦控件，`initialFocus` 时落在指定元素；最后一个控件上 Tab → 第一个控件，第一个上 Shift+Tab → 最后一个；Escape 与遮罩点击各调用 `onOpenChange(false)` 一次，`dismissible=false` 时均不调用且无 `关闭` 按钮；`open` 翻回 false 后焦点回到打开前的按钮，传 `returnFocus` 时回到其指向元素（含同时传 `trigger` 的情形），有 `trigger` 且未传 `returnFocus` 时回到 trigger；`ConfirmDialog` 标题为 `heading`；无 description 的 Dialog/Drawer 不触发 `console.warn`/`console.error`；`ConfirmDialog` 为 `alertdialog`、`取消` 点击 → `onOpenChange(false)`、`退出` 点击 → `fn` 一次、确认按钮类名含 `ui-btn--danger`、遮罩点击不关闭、Escape 关闭、无 `关闭` 按钮、`pending` 时确认按钮 `aria-busy="true"`；`Drawer` 类名含 `ui-drawer--left ui-drawer--w288`、`data-side="left"`，默认为 `ui-drawer--right ui-drawer--w420`，`关闭` 点击 → `onOpenChange(false)`；`dialog.css` 含 `.ui-drawer--w288`/`--w420`/`max-width: 92vw`/`.ui-dialog--sm`、reduced-motion 块与 `ui-drawer-in-left` 关键帧；`radix-platform.ts` 只补缺；`Dialog`/`ConfirmDialog`/`Drawer` 可从 `web/src/ui/index.ts` 导入

#### Scenario: 弹出与分段基元可访问且键盘可断言
- WHEN 在 jsdom（引入 `radix-platform.ts`）渲染 `<Menu trigger={<Button>新建</Button>} items={[{label:"新建文件夹",onSelect:fn,icon:"folder"},{label:"新建工作空间",onSelect:fn2},{label:"删除",onSelect:fn3,danger:true}]} />`（页面另有按钮 `其他`）、`<Popover trigger={…} contentRole="dialog" contentLabel="工作空间切换器">`（content 内首个元素为 input）、无 `contentRole` 的 `Popover`、受控 `Popover open`、`<Tooltip label="展开侧栏" side="right"><Button …/></Tooltip>`、`<SegmentedControl label="主题" value="light" options={[light 浅色, dark 深色, system 跟随系统]} onValueChange={fn} />`，并触发 Enter/方向键/Home/End/Escape、指针按下与点击、外点序列（让出宏任务后）、`focus()`/`blur()`/`pointerMove`/`pointerLeave`、`open` 翻转
- THEN `Menu` 关闭态无 `menu` 且 trigger `aria-haspopup="menu"` `aria-expanded="false"`；Enter 打开后 `menu` 类名含 `ui-menu`、位于 `document.body` portal 内、`aria-labelledby` 为 trigger id、三个 `menuitem`、首项获焦；ArrowDown/End/Home/ArrowUp 依次落到第二/末/首/末项；焦点项 Enter → 该项回调一次、菜单关闭、焦点回 trigger；Escape 关闭且无回调；pointerdown 打开后点击项 → 回调一次并关闭；`danger` 项类名含 `ui-menu-item--danger`、`icon` 项含 `aria-hidden` svg；打开期间 `document.body.style.pointerEvents` 为空、按钮 `其他` 无需 `hidden:true` 可查到、外点序列后关闭；`Popover` 无 `contentRole` 时为 `dialog` 且无 `aria-label`，传 `contentRole="dialog" contentLabel="工作空间切换器"` 时 `getByRole("dialog",{name:"工作空间切换器"})` 命中、`contentRole="group"` 时为 `group`；打开后 input 获焦，Escape 关闭并归还 trigger，外点关闭、内点不关闭；受控 `open` 下 Escape 调用 `onOpenChange(false)` 一次且仍渲染，`open=false` 后消失；content 带 `data-side`/`data-align`；`Tooltip` 初始无 `tooltip`，`focus()` 后 `role="tooltip"` 元素文本为 label、类名含 `ui-tooltip` 且 trigger `aria-describedby` 等于其 id，其 `data-side="right"`（默认 `top`），`blur()`/Escape 后消失且 `aria-describedby` 移除，`pointerMove` 后 150ms 仍不显示、350ms 时已显示、`pointerLeave` 后消失；`SegmentedControl` 为 `radiogroup` 名 `主题`、三个 `radio` 名正确、选中项 `aria-checked="true"` `data-state="checked"`、初始根元素 `tabIndex=0` 且三项 `-1`、根元素 `focus()` 后焦点落在选中项且其 `tabIndex=0`，点击 `深色` → `fn("dark")` 一次且 rerender 前 `aria-checked` 不变，选中项上 ArrowRight → `fn("dark")`、首项上 ArrowLeft → `fn("system")`，`<form>` 内渲染 `radio` 仍为 3；四个 css 各含 reduced-motion 块，`.ui-menu`/`.ui-popover`/`.ui-tooltip` 分别含 `z-index: 1600/1300/1700`，`menu.css` 含 `.ui-menu-item[data-highlighted]`，`segmented-control.css` 含 `[data-theme="dark"] .ui-seg-item[data-state="checked"]` 与 `.ui-seg-item:focus-visible`，`tooltip.css` 含 `[data-theme="dark"] .ui-tooltip`，`ui.css` 含四个 `@import`；`Menu`/`Popover`/`Tooltip`/`SegmentedControl` 可从 `web/src/ui/index.ts` 导入

#### Scenario: 通知与空态可断言
- WHEN 在 jsdom（fake timers）于 `<ToastProvider>` 内渲染调用 `useToast()` 的探针，依次 `show` success/error/info 与连续四条消息，推进计时、悬停/聚焦、按 Escape；在 Provider 外渲染同一探针；渲染 `<EmptyState icon="folder" title="空目录" description="拖入文件或新建"><button>新建</button></EmptyState>` 与无 icon/description 的 `<EmptyState title="暂无" />`；并静态读取 `web/src/main.tsx`、`web/test/render-app-router.tsx`、`web/src/features/**`、`web/src/routes/**`、`toast.css`、`empty-state.css`
- THEN 无 toast 时 `region` 名 `通知` 存在且无 `status`；`show` 后 `.ui-toast.ui-toast--<type>` 含消息文本与该类型图标的 `aria-hidden` svg（`lucide-circle-check`/`lucide-triangle-alert`/`lucide-info`），且存在 `role="status" aria-live="polite"` 元素；推进 2399ms 仍在、再 1ms 消失；四条连发只剩后三条，再推进 2400ms 全部消失；悬停（`pointermove`，`pointerleave` 派发在 `通知` region 上恢复）或聚焦（`focusin`/`focusout`）期间推进 5000ms 仍在，离开后 2400ms 消失；Escape 关闭；悬停或聚焦中用 Escape 关掉最后一条后再 `show`，推进 2400ms 新 toast 仍消失，且 Provider 内的应用子树探针未重挂（挂载计数为 1）；Provider 外调用抛含 `ToastProvider` 的错误；`main.tsx` 与 `render-app-router.tsx` 均以 `<ToastProvider>` 包裹 `<RouterProvider`；`EmptyState` 根类名 `ui-empty-state`、图标容器内 svg `aria-hidden`、标题/说明为 `p` 且文本正确、`button` 为根的最后子元素，无 icon/description 时对应节点不存在；features/routes 无 `@radix-ui` import 命中；`toast.css` `.ui-toast-viewport` 含 `z-index: 2000` 与 `top: 52px`、含 reduced-motion 块与三类型图标色规则；`empty-state.css` 含 `.ui-empty-state-icon .ui-icon` `width: 28px`；`ToastProvider`/`useToast`/`EmptyState` 可从 `web/src/ui/index.ts` 导入

#### Scenario: 退出确认迁移不回归
- WHEN 在 jsdom（引入 `radix-platform.ts`）渲染已登录应用并点击侧栏 `退出登录`，分别执行：点击 `取消`；按 Escape；点击 `退出` 后按 Escape（logout 挂起）；同 tick 双击 `退出`；确认后卸载；logout 返回 403 后再次打开并成功；并静态读取 `footer.tsx`、`styles.css`
- THEN `alertdialog` 标题 `heading` `退出登录？`、说明文案存在、`aria-modal="true"`、初始焦点在 `取消`；`取消`/Escape 关闭且 0 次 logout，Escape 后焦点回 trigger；pending 中 `关闭` 按钮存在、Escape 关闭后请求仍在（恰 1 次）且页面 heading 仍在、焦点回到 `aside` 内 `aria-current="page"` 的链接（trigger 已禁用），204 后回登录页；双击恰 1 次 logout、确认按钮 disabled、trigger disabled；卸载后请求 signal aborted 且无 `console.error`；403 后对话框关闭、footer `alert` 显示错误、重试可成功；`footer.tsx` 不含 `lib/dialog`/`<dialog`/`trapDialogFocus` 且含 `ConfirmDialog`，`styles.css` 不含 `.logout-dialog`；CI `make ui-walk` 退出段（定位已为页面范围，不改）通过，经 `render-app-router` 打开退出对话框的既有 `chat-page-lifecycle` 用例不改仍绿

#### Scenario: 忙碌期焦点留在模态内
- WHEN 在 jsdom（引入 `radix-platform.ts`）渲染受控 `<ConfirmDialog open confirmText="退出" …>`，先聚焦 `退出` 再以 `pending` 由 false 变 true 重渲染；另一轮先把焦点移到 `document.body`（`退出` 失焦）再翻转 `pending`；渲染受控 `<Dialog open busy={false} footer={<Button type="submit">创建</Button>} …>`（body 含 input），聚焦 input 后翻转 `busy`，再以聚焦的 `创建` 被禁用的形态翻转 `busy`；在 `ConfirmDialog` 焦点已被救回后令其失焦（活动元素为 `document.body`），再以 `pending` 仍为 true 重渲染
- THEN `ConfirmDialog` 两轮翻转后活动元素均为取消按钮；`Dialog` 中聚焦未禁用 input 时翻转不移动焦点，聚焦的提交按钮被禁用时焦点落到内容内首个未禁用的可聚焦控件；`pending` 保持 true 的重渲染不移动焦点（活动元素仍为 `document.body`）；真实浏览器由 `make ui-walk` 退出段证明（见 verification-harness）

#### Scenario: 既有对话框迁移不回归
- WHEN 退出确认、新建工作空间、新建文件夹、`＋` 创建菜单、工作空间切换器改用基元后运行既有 `web/test` 与 `make ui-walk`
- THEN 既有断言语义（`alertdialog` 标题 `退出登录？`、`取消`/`退出`、创建校验文案、切换器 `dialog` 名称 `工作空间切换器`、`＋ 新建工作空间`、`menuitem` `新建文件夹`）全部保持通过——定位器由 `main` 范围改为页面范围、原生 `HTMLDialogElement.open`/`cancel` 事件断言改为 Escape keydown；`web/src/lib/dialog.ts` 与 `dialog.test.tsx` 不再存在

#### Scenario: 组件可访问且样式只来自 token
- WHEN 在 jsdom 渲染每个组件并按键盘操作
- THEN `Dialog` 打开时焦点进入、Tab 循环留在框内、Escape 关闭并把焦点还给触发器；`Menu` 用箭头键移动、Enter 触发、Escape 关闭；`Toast` 在 `aria-live` 区域出现并在计时后移除；`SegmentedControl` 三项 `role="radio"` 且箭头键切换；`Tooltip` 聚焦触发器时出现且 `aria-describedby` 指向它；`Icon` 默认 `aria-hidden="true"`、传 `label` 时有可访问名；每个组件根元素类名以 `ui-` 开头，且源码（`web/src/ui/**/*.tsx`）grep 无 `style={`

### Requirement: 动效与图标
`Icon` SHALL 是单一组件：`name` 为本仓用到的 lucide 图标名联合类型，内部维护 name→组件映射、不逐个再导出 lucide 图标；size `12|14|16|18|20`（类名 `ui-icon-<size>`）；默认 `aria-hidden="true"`，传 `label` 时为 `role="img"` 且以 `label` 为可访问名。`web/src/ui/motion.css` SHALL 提供 demo 的 `wb-fadein`、`wb-pop`、`wb-pulse`、`wb-caret`、`wb-spin`、`wb-drawer-in` 关键帧与对应工具类（`ui-fadein`、`ui-pop`、`ui-pulse`、`ui-caret`、`ui-spin`），并在 `prefers-reduced-motion: reduce` 下把每个工具类置为 `animation: none` 与 `transition: none`；demo 未使用的 `wb-float`/`wb-shimmer` 不移植。图标 SHALL 全部经 `Icon` 取自 `lucide-react`（ISC，打包进产物，运行时零网络请求），`ATTRIBUTION.md` SHALL 新增 lucide（ISC）与 Radix UI Primitives（MIT）条目。

#### Scenario: 图标可访问、动效可禁用、归属登记
- WHEN 在 jsdom 渲染 `<Icon name="folder" />` 与 `<Icon name="folder" label="目录" size={12} />`，并静态读取 `motion.css` 与 `ATTRIBUTION.md`
- THEN 第一个 svg `aria-hidden="true"`；第二个 `role="img"`、可访问名 `目录`、类名含 `ui-icon-12`；`motion.css` 的 reduced-motion 块覆盖全部五个 `ui-*` 工具类；`ATTRIBUTION.md` 含 `lucide`/ISC 与 `Radix`/MIT 条目

#### Scenario: 图标离线、动效可禁用且归属登记
- WHEN `make ui-walk` 的 `desktop-light` project 在 journey 内统计 resourceType 为 `image|font|stylesheet|script` 的 `requestfailed`（导航/SSE 取消的 `net::ERR_ABORTED` 不计）与非 `baseURL` 源的请求，并在受控回合运行中对 `.ui-pulse` 元素先 `emulateMedia({reducedMotion:"reduce"})` 再恢复 `no-preference`；`web/test` 静态读取 `motion.css`
- THEN 全 journey 零静态资源 `requestfailed`、零跨源请求（图标离线可用）；reduce 下 `animationName` 为 `none`、恢复后非 `none`；`motion.css` 的 reduced-motion 块把每个 `ui-*` 动效类置为 `animation: none` 且 `transition: none`；`ATTRIBUTION.md` 含 `lucide`/ISC 与 `Radix`/MIT 条目
