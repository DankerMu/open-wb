## 1. Contract and baseline
- [x] 1.1 Expanded fixture PASS after one bounded-replay revision; strict exit0.140 source/test/config inputs frozen; protected pre-SSE compiled reference returns real authenticated404 vs required200. Local Node24.13.1/npm11.8.0/Darwinarm64 and environment allowlist recorded.
- [x] 1.2 LSP consumer references checked; #204 synchronous observer guard and #214 ring/cursor ownership preserved. Initial fixture corrected writefalse semantics before implementation, without changing user min−1/empty-id/snapshot decisions.

## 2. Replay and HTTP implementation
- [x] 2.1 Supervisor synchronous subscriptions consume the existing ring, latest frozen retained record and captured IDs; no second buffer/counter or ReturnType contract. No-observer/replay/gap/refresh/replacement boundaries remain covered.
- [x] 2.2 Actual assembled owner SSE200 with immediate real HTTP headers, lossless id/event/JSON frames, owner401/404 isolation, empty-id gap, injectable15s comment heartbeat and dual-client disconnect behavior implemented.
- [x] 2.3 Bounded initial replay drains without queueing live events; live false, throw/error, paused replay/gap and end-pending responses are isolated and owned through physical close. Late owner admission returns existing502 agent_unavailable + Connection:close. Parent found header-flush, raw-error, end-pending and late-admission defects beyond initially green inject tests; writer found gap-writefalse sibling defect. Protected counterexamples now pass; #204/#214 paths preserved.

## 3. Independent verification
- [x] 3.1 Protected compiled app + real HTTP/native probe passes canonical live/replay/gap/refresh/replacement,1000-record replay, controlled false/drain/cutoff/reconnect and20response cleanup; owned native exits observed. Separate real TCP held-preHandler/preClose probe returns502 with zero subscribers; injected-clock gap false probe verifies paused/resumed heartbeat and zero timers. Permanent inject/native TCP regressions cover the same selected lifecycle classes.
- [x] 3.2 Seven disposable compiled wrong candidates each semanticRED then restoredGREEN: missing empty-id, skipped replay, delayed headers, ignored gap backpressure, forgotten end-pending ownership, ignored raw-error close and late keep-alive response. Five dedicated stability trials pass. All8 protected #214 snapshot and14 #204 sink cases rerun against this production candidate, exit0.
- [x] 3.3 Parent scoped Biome/typecheck/build/server suite/anti-drift exit0:67files1203tests,91.25% statements/87.38% branches,0clones and size/naming pass. Three drift repair cycles removed unused exports and consolidated copied test setup/helpers without weakening inputs/assertions/gates. Architecture/ADR/parent tasks updated after positive smoke; generated README removed. Final strict validation and archived qualification-copy removal are recorded with the delivery evidence.

## 4. Delivery
- [ ] 4.1 Expanded three-seat review, bounded2fixpasses, same-head source CI/merge, separate preserving canonical archive PR/CI before92.

## Core risk mapping
Selected entry/API→2.2/3.1 rawroute/headers; schema/types→2.1 named owner DTO and existing event IDs; concurrency/state→2.1/2.3 replay/live/generation/drain; resources→2.3 timer/response/subscriber/native cleanup; compatibility→2.1/3.2 unchanged REST/ring/sinks/cursor; errors→2.3 writefalse/throw/error isolation; auth/secrets→2.2 ownerbeforeheaders/cookie; tests/evidence→1.1/3.1/3.2 realHTTP+inject and qualifiedoracle; release/docs→3.3/4.1 assembly and archive. Not selected config (no key), fileIO (no newproductionfilesystempath).

## Domain risk mapping
Selected tenant isolation→2.2 ownernegative; auth/session lifecycle→2.1/2.2/2.3 generations+cookie; process/child-environment isolation→2.3/3.1 cleanup/noenvchange; server/web HTTP-envelope→2.2 SSEwire; offline deployability→3.3 no dependency/networkaddition. Not selected SQLite migration/catalog (read/publishseam only), browser runtime/navigation/persistence (web later92/93/104), cross-service network (HTTP client transport only, no outboundservicecall).

## Scope and evidence limits
One implementer/currentcheckout/no newworktree/no nesting. Source allowed stream/sse.ts, supervisor.ts, index.ts; app.ts only for necessary injectable assembly plumbing; rest.ts only reuse of existing narrow owner/no-store helper if duplicate guard otherwise unavoidable. Ring-buffer.ts only if exposing its already-retained publication is needed, never changing push/since semantics. Existing sessions/runtime helpers and focused SSE/supervisor/assembly tests allowed. No store mutation/runtime/process/tokens/config/dependencies/CI/threshold changes; concrete ReturnType aliases prohibited, named owner exports required. Writer runs focused RED/GREEN only, no format/lint/build/full suites; parent owns protected /tmp/open-wb-issue103-evidence, acceptance and Git. Every source addition must be preceded by focused failing test. Protected scripts are not candidate-writable; local OSuser shared, independent CI required. #219 remains separate; do not silently absorb it.

## Evidence and deviations
Parent evidence: /tmp/open-wb-issue103-evidence; writer logs: /tmp/open-wb-issue103-writer. Initial parent oracle's empty create POST carried JSON content-type and failed500; fixed setup, then observed actual404 semanticRED. Outer watchdog failures were minimized to semantic2s header/close deadlines for the controlled cases, not a universal production shutdown promise. Initial writer subscribe-symbol RED is setup-only; assembled route404 is the public behavior RED. Later backpressure cases first ran GREEN and were qualified with explicit wrong candidates or actual found defects.

Fixture reviewer revival and one writer repair wake were interrupted by harness/model transport; fresh fixture revision review and the same in-place writer completed the respective work. Parent initially suggested503 for shutdown; owner-defined agent_unavailable is502, preserved unchanged. Writer's early gap test-double logs red-22/green-23 were invalidated because the underlying response could independently drain; corrected qualified red-23/green-25 records retained. Ordinary REST keep-alive shutdown latency predates103 and is separately tracked at#227; no global listener policy/config change is included. Existing#219 claim bookkeeping remains out of scope.

Qualification bundle SHA256: `0b5f37574e68b1270e37263e434936fbcbc4ced67f6732720847e5b295859722`. Final49 compiled files are byte-identical to the qualified/restored candidate after the last test-only refactor; all3 current-tree protected probes reran GREEN. Disposable qualification/diagnostic/initial-candidate copies were archived then removed; the protected pre-SSE reference and raw failed/restored logs remain. Native1000-record delivery and controlled raw-write backpressure are separately identified evidence, not a claim that every loopback write naturally blocked. No OS enclave or browser execution is claimed.
