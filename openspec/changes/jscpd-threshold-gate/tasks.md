## Risk Packs
- Config / project setup — selected: `.jscpd.json` is the single gate config → 1.1, 2.1.
- Public API / CLI / script entry — selected: three entry points share the command literal; literal must not change → 2.2 (literal grep + `scripts/test-ci-harness.sh` via `make test-guardrails`).
- Error handling — selected: failure must come from the threshold reporter with its diagnostic, not a bare exit → 1.2, 2.1.
- Legacy compatibility — selected: threshold/pattern/ignore/reporters and existing guardrail cases unchanged → 2.2.
- Documentation — selected: constraints.yaml/AGENTS.md already state ≤3%; verify no edit needed, PR body explains exitCode vs threshold semantics with source citations → 3.1.
- File IO / path safety — not selected: fixtures live in the script's existing `mktemp -d` dir removed by its trap; no repo files written → checked in 2.2 (`git status` clean).
- Schema, Auth/secrets, Concurrency, Resource limits, Release/packaging — not selected: no data, credentials, shared state, large input, or dependency change (jscpd stays 5.0.16).

## 1. Implementation
- [ ] 1.1 Remove `"exitCode": 1` from `.jscpd.json`; all other keys byte-identical.
- [ ] 1.2 Add guardrail cases to `scripts/test-guardrails.sh` using the design's invocation and layout (`node_modules/.bin/jscpd --config .jscpd.json "$tmp/<fixture>"` from repo root; `server/{a,b,c}.ts`): (a) real config accepts a below-threshold fixture (≈2–3%); (b) real config rejects an above-threshold fixture (≈5–7%) AND output contains `too many duplicates` and `over threshold`; (c) runtime-derived `$tmp/exitcode.json` (real config + `"exitCode": 1`, generated by script) rejects fixture (a). Fixtures and derived config only inside the existing `$tmp`; no network; deterministic; existing cases untouched.

## 2. Verification
- [ ] 2.1 Exit-code matrix (stop/escalate per design if an above-threshold case exits 0 or lacks threshold text) (disposable fixtures, not committed): HEAD config RED on ≈0.04%; new config 0 / ≈0.04% / ≈2.8% → 0, ≈3.2% / ≈5% → non-zero with threshold text; record actual percentages from jscpd output and the observed boundary.
- [ ] 2.2 Mutation: temporarily re-adding `"exitCode": 1` to the real `.jscpd.json` makes guardrail case (a) FAIL; restore byte-identical. `make anti-drift` exit 0; `make test-guardrails` exit 0; command literals in Makefile/CI/package.json unchanged; `git status` clean apart from the two intended files.

## 3. Delivery
- [ ] 3.1 PR body cites jscpd 5.0.16 `main.rs`/`cli.rs`/`options.rs` and cpd-reporter `threshold.rs` semantics and explains choosing key deletion over command/threshold changes.
