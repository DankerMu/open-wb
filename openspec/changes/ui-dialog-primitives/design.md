# Design: ui-dialog-primitives（#277）

Change surface: 新 `web/src/ui/{dialog,confirm-dialog,drawer}.tsx`、`web/src/ui/dialog.css`；`web/src/ui/ui.css`（+`@import "./dialog.css"`）；`web/src/ui/index.ts`（+3 导出）；`web/src/ui/icon.tsx`（+`triangle-alert`、`info`）；新 `web/test/radix-platform.ts`、`web/test/ui-dialog.test.tsx`；`web/test/ui-icon-brand.test.tsx`（名清单 +2）；`web/package.json`/`package-lock.json`；`ATTRIBUTION.md` §3。

Must preserve: `web/src/features/auth/footer.tsx:106-137` 与 `web/src/features/files/dialogs.tsx:66-81` 的手写 `<dialog>`、`web/src/lib/dialog.ts`、`web/test/dialog-platform.ts` 及其 8 个引入方逐字不动（迁移归 #280/#302）；`styles.css:447-480 .logout-dialog*` 与 `files.css:580-615 .files-dialog*` 规则不动，新类名 `ui-dialog*`/`ui-drawer*` 与之零交集；`web/src/ui/{button,input,switch,tag,chip}.*` 不动（ConfirmDialog 只消费 `Button`）；`Icon` 既有 25 个名与行为不变（只增不改）；`motion.css` 六个关键帧与五个工具类不动（Drawer 左侧入场关键帧写在 `dialog.css` 内）；`styles.css` `@import` 列表不变；`make ui-walk` 全 journey 不变（无页面消费者）。

Must add/change:
- 依赖 `@radix-ui/react-dialog@^1.1`（含 focus-scope/dismissable-layer/portal/presence/remove-scroll 等 16 个传递依赖，MIT，其中 `tslib` 为 0BSD 且仅 dev→prod 标记变化）。
- `Dialog`（`web/src/ui/dialog.tsx`）：`DialogProps = { open: boolean; onOpenChange: (open: boolean) => void; title: ReactNode; description?: ReactNode; size?: "sm"|"md"; initialFocus?: RefObject<HTMLElement | null>; returnFocus?: RefObject<HTMLElement | null>; dismissible?: boolean; footer?: ReactNode; trigger?: ReactElement; children?: ReactNode }`。调用方指定初始焦点只能走 `initialFocus`、不得用 React `autoFocus`（焦点已在容器内时 FocusScope 不派发 `onMountAutoFocus`，打开者记录会过期；JSDoc 写明）。焦点归还：Radix 模态 `DialogContentModal.onCloseAutoFocus` 先 `preventDefault` 再聚焦 `triggerRef`，无 `Dialog.Trigger` 时焦点落到 `body`——本组件在 Content 的 `onOpenAutoFocus` 里记录打开者（FocusScope 先派发该事件、再移动焦点，此时 `document.activeElement` 必是打开者；不能用 `[open]` effect 记录——子组件 effect 先于父组件跑，若 Content 与 `open=true` 同一次提交挂载，记录到的会是对话框内首个控件）：`onOpenAutoFocus={e => { openerRef.current = document.activeElement as HTMLElement | null; if (initialFocus) { e.preventDefault(); initialFocus.current?.focus(); } }}`（处理器始终挂载）。归还分支：`onCloseAutoFocus={e => { if (returnFocus || !trigger) { e.preventDefault(); (returnFocus?.current ?? openerRef.current)?.focus(); } }}`——有 `trigger` 且未传 `returnFocus` 时不 `preventDefault`，交给 Radix 归还 trigger（macOS Safari 点击按钮不聚焦，打开者可能是 `body`，此时 Radix 的 trigger 归还优于本组件）。`aria-modal="true"` 由本组件显式写在 Content 上（Radix 不输出该属性，只用 `hideOthers` 给 portal 外元素加 `aria-hidden`）。`aria-describedby`：有 `description` 时**不传**该 key（让 Radix 自己关联 `descriptionId`）；无 `description` 时传 `aria-describedby={undefined}` 消除 Radix 告警，即 `{...(description ? {} : { "aria-describedby": undefined })}`。结构：`<Root open onOpenChange>{trigger && <Trigger asChild>{trigger}</Trigger>}<Portal><Overlay class="ui-dialog-overlay"><Content class="ui-dialog ui-dialog--<size>" onOpenAutoFocus={记录打开者 + initialFocus 处理器（始终挂载）} onEscapeKeyDown={dismissible ? undefined : e => e.preventDefault()} onPointerDownOutside={dismissible ? undefined : e => e.preventDefault()} onCloseAutoFocus={归还焦点处理器} aria-modal="true" {...(description ? {} : { "aria-describedby": undefined })}><div class="ui-dialog-head"><Title class="ui-dialog-title">{title}</Title>{dismissible && <Close asChild><Button size="icon" variant="ghost" aria-label="关闭"><Icon name="x" /></Button></Close>}</div>{description && <Description class="ui-dialog-desc">{description}</Description>}<div class="ui-dialog-body">{children}</div>{footer && <div class="ui-dialog-foot">{footer}</div>}</Content></Overlay></Portal></Root>`。Overlay 作为居中容器（demo:730 backdrop flex center padding 24）——Radix Overlay 与 Content 同级也可，但把 Content 放进 Overlay 可直接复用 demo 布局；Radix 允许 Content 在 Overlay 内（官方示例之一）。默认焦点：Radix FocusScope 聚焦首个可聚焦元素（demo:1097 聚焦容器本身；本仓选首个控件，父 spec 已定）。`Title` 保持 Radix 默认的 `h2`（`web/e2e/ui-walk.spec.ts:101` 与 `settings-footer.test.tsx:584` 按 `heading "退出登录？"` 定位），标题内 `Icon` 为 `aria-hidden`。
- `ConfirmDialog`（`web/src/ui/confirm-dialog.tsx`）：`{ open; onOpenChange; title: ReactNode; description: ReactNode; cancelText?: string = "取消"; confirmText: string; danger?: boolean; pending?: boolean; onConfirm: () => void; returnFocus?: RefObject<HTMLElement | null>; children?: ReactNode }`（`children` 渲染在 body，供 #280 的 pending 提示行——Radix `Description` 是 `<p>`，不能再塞 `<p>`）（`returnFocus` 透传给 Dialog 内核；#280 迁移 `settings-footer.test.tsx:598-614` 那类「click 打开、未聚焦 trigger、断言焦点回 trigger」的用例即靠它）。渲染 `Dialog` 的内部件但 Content 加 `role="alertdialog"`（Radix `Dialog.Content` 把 `...props` 展开在默认 `role="dialog"` 之后，可覆盖；实现时以 RTL `getByRole("alertdialog")` 验证，若 Radix 版本不允许覆盖则改装 `@radix-ui/react-alert-dialog` 并记偏离）、`onPointerDownOutside` 一律 `preventDefault`（alertdialog 不因外点关闭）、Escape 允许（= 取消）、无右上关闭按钮、`size="sm"`。标题前 `Icon name={danger ? "triangle-alert" : "info"}`（demo:1118）。footer：`<Button variant="ghost" onClick={() => onOpenChange(false)}>{cancelText}</Button><Button variant={danger ? "danger" : "primary"} loading={pending} onClick={onConfirm}>{confirmText}</Button>`。为避免 `Dialog` 组件 props 膨胀，`dialog.tsx` 导出内部 `DialogFrame`（非 index 导出）供 `ConfirmDialog` 复用，或 `Dialog` 增加内部-only 的 `role`/`hideClose` 选项——实现者二选一，保持 index 只导出三个公开组件。
- `Drawer`（`web/src/ui/drawer.tsx`）：`{ open; onOpenChange; side?: "left"|"right" = "right"; width?: 288|420 = 420; title: ReactNode; footer?: ReactNode; children?: ReactNode }`。结构：`<Root><Portal><Overlay class="ui-drawer-overlay" /><Content class="ui-drawer ui-drawer--<side> ui-drawer--w<width>" data-side={side}><div class="ui-drawer-head"><Title class="ui-drawer-title">{title}</Title><Close asChild><Button size="icon" variant="ghost" aria-label="关闭"><Icon name="x" /></Button></Close></div><div class="ui-drawer-body">{children}</div>{footer && <div class="ui-drawer-foot">{footer}</div>}</Content></Portal></Root>`（Overlay 与 Content 同级，demo:772-773 两者独立定位）。无 description，无条件 `aria-describedby={undefined}`；`aria-modal="true"`；焦点归还同 Dialog：`onOpenAutoFocus` 记录打开者、`onCloseAutoFocus` 无 trigger 时 `preventDefault` 并归还（#283 侧栏覆盖层无 Radix trigger，依赖此路径）——Seam (8) 增一条 Drawer 焦点归还用例（`button.focus()` → open → close → `waitFor` 焦点回到 button）。
- `dialog.css`（映射 demo:730-738、772-777、1129-1140；颜色只用 token）：`.ui-dialog-overlay{position:fixed;inset:0;z-index:1400;display:flex;align-items:center;justify-content:center;padding:24px;background:var(--wb-overlay);animation:wb-fadein .15s ease}`；`.ui-dialog{display:flex;flex-direction:column;width:520px;max-width:100%;max-height:82vh;border-radius:14px;background:var(--wb-bg-primary);box-shadow:var(--wb-shadow-dialog);animation:wb-pop .2s ease}`；`.ui-dialog--sm{width:400px}`；`.ui-dialog-head{display:flex;align-items:center;gap:10px;padding:16px 20px 8px}`；`.ui-dialog-title{flex:1;min-width:0;display:flex;align-items:center;gap:8px;font-size:16px;font-weight:600}`；`.ui-dialog-desc{padding:0 20px;color:var(--wb-text-secondary);font-size:13.5px;line-height:22px}`（demo:1119 内联样式 token 化）；`.ui-dialog-body{padding:8px 20px 16px;overflow-y:auto}`；`.ui-dialog-foot{display:flex;justify-content:flex-end;gap:8px;padding:12px 20px 16px}`；关闭按钮沿用 `Button size="icon" variant="ghost"`（demo:735 的 28×28 由 `.ui-dialog-head .ui-btn--icon{width:28px;height:28px;border-radius:6px}` 收窄）。Drawer：`.ui-drawer-overlay{position:fixed;inset:0;z-index:1350;background:var(--wb-overlay);animation:wb-fadein .15s ease}`；`.ui-drawer{position:fixed;top:0;bottom:0;z-index:1360;display:flex;flex-direction:column;max-width:92vw;background:var(--wb-bg-primary);box-shadow:var(--wb-shadow-dialog)}`；`.ui-drawer--right{right:0;animation:wb-drawer-in .22s ease}`；`.ui-drawer--left{left:0;animation:ui-drawer-in-left .22s ease}` + `@keyframes ui-drawer-in-left{from{transform:translateX(-40px);opacity:0}to{transform:none;opacity:1}}`（本文件内，不动 motion.css）；`.ui-drawer--w420{width:420px}`；`.ui-drawer--w288{width:288px}`；`.ui-drawer-head{display:flex;align-items:center;gap:10px;padding:16px 18px;border-bottom:1px solid var(--wb-border-default);flex-shrink:0}`；`.ui-drawer-title{flex:1;font-size:15px;font-weight:600}`；`.ui-drawer-body{flex:1;overflow-y:auto;padding:16px 18px}`；`.ui-drawer-foot` 同 dialog-foot。`@media (prefers-reduced-motion: reduce){.ui-dialog-overlay,.ui-dialog,.ui-drawer-overlay,.ui-drawer,.ui-drawer--left,.ui-drawer--right{animation:none}}`（显式列出承载动画的侧向选择器，不依赖源码顺序）。`:focus-visible` 圆角重申：`.ui-dialog:focus-visible{border-radius:14px}`（Content 可获焦时）。
- `web/test/radix-platform.ts`：`if (!("ResizeObserver" in globalThis)) globalThis.ResizeObserver = class { observe() {} unobserve() {} disconnect() {} }`；`Element.prototype.hasPointerCapture ??= () => false`、`setPointerCapture ??= () => {}`、`releasePointerCapture ??= () => {}`、`scrollIntoView ??= () => {}`。只补这四项、只补缺失、不覆盖已有实现（仓内 jsdom 29.0.1 已带 `PointerEvent`，不补）；文件头注释说明用途与引入方式（测试文件顶部 `import "./radix-platform.js"`）。
- `Icon`：映射加 `"triangle-alert": TriangleAlert`、`info: Info`；`ui-icon-brand.test.tsx` 的 `ICON_NAMES` +2。
- `ATTRIBUTION.md` §3 Radix 用途行：已安装 `@radix-ui/react-switch`（#276）、`@radix-ui/react-dialog`（#277）。

Governing invariant: 所有模态/覆盖层行为（焦点进入、Tab 循环、Escape、外点、labelledby/describedby、`hideOthers`、portal 到 body）只由 Radix Dialog 提供，本仓不再手写焦点循环；唯一例外是关闭后的焦点归还（Radix 只归还给 `Dialog.Trigger`；本组件经 `onOpenAutoFocus` 记录打开者、经 `onCloseAutoFocus` 在「传了 `returnFocus`」或「无 `trigger`」时归还给 `returnFocus` 或打开者，有 `trigger` 且无 `returnFocus` 时交给 Radix）与显式 `aria-modal="true"`；`role="alertdialog"` 变体不因外点关闭、无 X 按钮；组件 tsx 无 `style={`（宽度/尺寸全部枚举为类名）；颜色只经 token。

Sibling surfaces: `web/src/features/auth/footer.tsx`、`web/src/features/files/dialogs.tsx`、`web/src/features/files/page.tsx:119`（手写 `role="dialog"` 切换器）、`web/src/lib/dialog.ts`、`web/test/dialog-platform.ts`（全部不动，迁移切片处理）；`web/src/ui/button.tsx`（`ConfirmDialog` 消费 `loading`）；`web/src/ui/icon.tsx`/`ui-icon-brand.test.tsx`；`web/src/ui/motion.css`（复用 `wb-fadein`/`wb-pop`/`wb-drawer-in` 关键帧名，不改文件）；`web/test/ui-guardrails.test.ts`（新 css/tsx 自动纳入字面量与 `style={` 扫描；ATTRIBUTION Radix 包断言）；`web/test/ui-tokens.test.ts`（未定义引用守卫覆盖 dialog.css）；`web/e2e/ui-walk.spec.ts`（退出 `alertdialog` 定位 :100 已是页面范围，#280 无需改；:387/:394/:428 的 `main` 范围 files 定位归 #302）；迁移预告（#280/#302/#283）：Radix 打开期间 `hideOthers` 把 portal 外全部元素标 `aria-hidden`，打开状态下用 `getByRole` 找页面元素需 `{ hidden: true }` 或先关闭再找；jsdom 里模拟遮罩关闭必须发完整 pointerdown→click 序列（`deferPointerDownOutside`），复用 `ui-dialog.test.tsx` 的 `pressOverlay`；`settings-footer.test.tsx` 焦点回 trigger 的断言改经 `returnFocus`；`styles.css:79-83` 全局 `:focus-visible`。

Seams under test（`web/test/ui-dialog.test.tsx`，顶部 `import "./radix-platform.js"`）: (1) 打开/关闭与 portal：`open` 时 `getByRole("dialog")` 存在且 `document.body` 直接包含 portal 容器、`aria-modal="true"`、`aria-labelledby` 指向标题、有 `description` 时 `aria-describedby` 指向说明；`open=false` 时不存在；(2) 焦点进入：默认（`dismissible` 为 true）聚焦 head 的 `关闭` 按钮（DOM 首个可聚焦控件）；`dismissible=false` 且 body 内含 input 时聚焦该 input；`initialFocus` 指向 body 内第二个控件时该控件获焦；(3) Tab 循环：焦点在最后一个可聚焦元素上 `fireEvent.keyDown(document.activeElement, {key:"Tab"})` → 焦点回到第一个；`shift+Tab` 在第一个 → 最后一个；(4) Escape：`fireEvent.keyDown(document, {key:"Escape"})` → `onOpenChange(false)` 被调用一次；`dismissible=false` → 不调用；(5) 遮罩：DismissableLayer 在挂载后的 `setTimeout(0)` 才注册 document `pointerdown` 监听，且 Radix Dialog 传 `deferPointerDownOutside: true`——左键 `pointerdown` 只登记一次性 `click` 监听、收到 `click` 才关闭。测试 helper `pressOverlay(el)` 按真实顺序派发 pointerdown → mousedown → pointerup → mouseup → click（render 后先 `await act(() => new Promise(r => setTimeout(r, 0)))`）→ `onOpenChange(false)`；点 Content 内部同序列 → 不关闭；`dismissible=false` → 不调用；(6) 焦点归还：先 `button.focus()` 再 rerender `open=true`、再 `open=false`，FocusScope 卸载归还也在 `setTimeout(0)`，`await waitFor(() => expect(document.activeElement).toBe(button))`；第二用例：打开前活动元素为 body、传 `returnFocus={ref}`（指向另一个按钮）→ 关闭后焦点在该按钮；第三用例：传 `trigger`（点击打开、不手动聚焦）且无 `returnFocus` → 关闭后焦点在 trigger（Radix 路径）；第四用例：同时传 `trigger` 与 `returnFocus`（指向另一元素）→ 关闭后焦点在 `returnFocus` 元素、不在 trigger；(7) ConfirmDialog：`getByRole("alertdialog")`、`getByRole("button",{name:"取消"})` 点击 → `onOpenChange(false)`、`getByRole("button",{name:"退出"})` 点击 → `onConfirm` 一次、`danger` → 确认按钮类名含 `ui-btn--danger` 且标题 svg 存在、`pending` → 确认按钮 `aria-busy`、遮罩 `pressOverlay`（同样先让出宏任务）不关闭、Escape 关闭、无 `关闭` 按钮、`getByRole("heading", { name: "退出登录？" })` 存在；(8) Drawer：默认 `role="dialog"` 类名 `ui-drawer ui-drawer--right ui-drawer--w420` `data-side="right"`；`side="left" width={288}` → `ui-drawer--left ui-drawer--w288`；`关闭` 按钮点击 → `onOpenChange(false)`；Drawer 焦点归还：`button.focus()` → `open` → `open=false` → `waitFor(activeElement === button)`；(9) 静态：`dialog.css` 含 `.ui-drawer--w288{width:288px}`、`.ui-drawer--w420{width:420px}`、`.ui-drawer{…max-width:92vw…}`、`.ui-dialog--sm{width:400px}`、reduced-motion 块覆盖四个动画选择器、`@keyframes ui-drawer-in-left`；`radix-platform.ts` 只在缺失时定义（文本含 `??=` 或 `in globalThis` 判断，且不含 `PointerEvent`）；(9b) 无 description 的 Dialog/Drawer 渲染期间 `console.warn`/`console.error` 未被调用（spy）；(10) 出口：三组件可从 `../src/ui/index.js` 导入；`Icon` 新名渲染。

Required evidence:
- `ui-dialog.test.tsx` 先红（导入失败）后绿；每个 seam 各≥1 用例；涉及 DismissableLayer 外点与 FocusScope 归还的用例按 (5)(6) 写明的方式让出一个宏任务，不得因假红改为手写处理器。反向注入：去掉 `dismissible=false` 分支的 `onEscapeKeyDown` → (4) 红；ConfirmDialog 去掉 `role="alertdialog"` → (7) 红；去掉 `onCloseAutoFocus` 归还处理器 → (6) 无 trigger 用例红；有 description 时误传 `aria-describedby={undefined}` → (1) 红；Drawer 类名拼错 → (8) 红；`radix-platform.ts` 改为无条件覆盖 → (9) 红。
- `make check` exit 0（含 knip：三导出与 shim 被测试消费；typecheck 覆盖 test）；`npm run build --workspace web` exit 0；`ci-compiled-server.sh ui-walk`（CI 环境变量）1 passed。
- 守卫（既有）：`web/src/ui/**` 无字面颜色、无 `style={`；ATTRIBUTION 含 `@radix-ui/react-dialog`。

Non-goals: 迁移与删除既有对话框实现；Menu/Popover/Tooltip/Toast；模态栈建模；任意数值宽度；Drawer 的 `initialFocus`/`dismissible`；`make ui-shots` 视觉证据（#297/#298）。

Review focus: (1) `role="alertdialog"` 覆盖是否真的落到 DOM（Radix props 展开顺序）；(2) Content 放在 Overlay 内是否影响 DismissableLayer 的外点判定（点击 Overlay 空白 = outside，点击 Content 内 = inside）——若 Radix 把 Overlay 视为 Content 的祖先导致外点失效，改为同级 + Overlay 自带 flex 居中的替代结构；(3) `radix-platform.ts` 是否只补缺不覆盖，且没有把 `PointerEvent` 之类 polyfill 写得比 jsdom 真实实现更宽；(4) 焦点归还分支是否正确：打开者经 `onOpenAutoFocus` 记录（不是 effect）；`onCloseAutoFocus` 只在「传了 `returnFocus`」或「无 `trigger`」时 `preventDefault` 并聚焦 `returnFocus ?? 打开者`，有 `trigger` 且无 `returnFocus` 时不拦截、由 Radix 归还 trigger；没有额外手写焦点逻辑；(5) `aria-describedby` 条件展开是否正确（有 description 时不传 key），无 description 时不触发 Radix 控制台告警（spy 断言）；`aria-modal="true"` 是否显式写在 Content 上；(6) `Icon` 名新增未破坏 `ui-icon-brand.test.tsx` 的"全集"断言；(7) dialog.css 的 z-index（1400/1350/1360）与现有页面 z-index 无冲突（grep `z-index` 全仓）；(8) 三个组件在 `web/src/ui/**/*.tsx` 无 `style={` 与颜色字面量。
