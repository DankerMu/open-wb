## Context
Sources: demo resource/workbuddy-live-demo.html:925 escape,1145–1185 mdRender,3910–3938 preview/code/CSV; parent files-web spec:27–36; API types via ApiClient in web/src/lib/api.ts.
Change surface: web/src/features/files/{md-render.ts,csv.ts,preview.tsx} and paired tests only; no page/tree/route/API edits. Demo source and its header remain untouched.
Governing invariant: untrusted file text may produce only renderer-owned markup; source text/destinations cannot create active elements, event attributes or navigable links, and each preview state faithfully represents its supplied data.
Sibling surfaces: Markdown escaped text, inline/fenced code, table cells, CSV/CodeView text nodes, filename/path/error labels; supplied image URL from canonical API is caller-owned.
Must preserve: demo headings h1..h4, unordered/ordered lists, bold/inline code, fenced code, tables, paragraphs, rules, inert links; CSV first row headers and body-row count; code line numbering; text safety.
## Decisions and scope
mdRender(src:string):string returns safe HTML. Escape untrusted text before emitting markup; destinations never enter output attributes. Its serialized links retain required href="#" without inline onclick.
One canonical parser produces a closed Markdown node representation; mdRender serializes it with escaping and PreviewPane renders the same nodes as React elements. No dangerouslySetInnerHTML, DOMParser roundtrip, second parser or global lint exception.
User decision after round1 review: preserve the authoritative href="#" anchor contract in both HTML and React DOM. React handlers prevent mouse/keyboard navigation. A narrowly documented useValidAnchor suppression only at this controlled anchor is explicitly authorized; no other lint suppression or unsafe HTML sink is authorized. The earlier button interpretation is rejected.
Document rows/cells use stable document-local source identities so duplicate text remains legal without duplicate React keys; no random UUIDs, text-only keys, or disguised array-index mapping. Keep identity internal rather than enlarging CSV public contract for rendering mechanics.
Do not blindly preserve accidental demo pipeline defects: demo applies inline replacement before fence recognition and checks blockquote > after escaping. Parse block/code context first, escape content at its output boundary; code examples remain literal and blockquotes render as intended. This is a documented correctness correction, not a CommonMark expansion.
User-authorized parity repair preserves demo strict bold grammar and code→strong→link recognition order through whole-segment spans and a closed-node projection, including formatted labels and crossing spans. Actual demo/jsdom comparisons govern the observed sibling cases; raw first-] and destination delimiters remain authoritative even inside literal code. Earlier split-at-code test assumptions were corrected with primary-source evidence, not excused as unspecified nesting.
User decision #119 comment5747856916 explicitly replaces the previous no-cap/no-flatten constraint for pathological reconstructed depth: canonical output has at most 64 strong ancestors. Elide only redundant strong wrappers beyond that bound, preserving every text byte/order, link scope/count and visible formatting; code stays literal and shallow demo parity unchanged. Both projections consume that same normalized structure; no dangerous HTML sink or plaintext fallback. This is an application policy, not the browser's observed depth510.
Deep acceptance includes same-instance shallow→near1MiBdeep→ordinary, source/render toggle, file change, actual unmount and StrictMode in dev/production, with exact text/link counts, strong-depth bound, responsive completion and zero errors. Earlier failing deep tests are retained and judged against this newly user-authorized contract; no unreported oracle weakening.
Browser diagnosis additionally found quadratic-looking layout cost for long unbreakable inline text across links. A single-variable overflow-wrap:anywhere probe preserved identical content and cut 2k-unit commit from 544ms to 72ms; the actual Markdown host now owns that wrap rule. CodeView/raw source and pre whitespace semantics are unchanged. Acceptance uses no injected test stylesheet and verifies visible text as well as DOM counts.
Keep lightweight demo subset; no raw HTML, images in Markdown, arbitrary attributes, external link navigation, plugins, syntax highlighter or new dependency. Inline code content remains literal, never passed through bold/link substitution.
csv.ts owns simple demo CSV parsing (trim whole input, newline rows, comma cells, first row headers); CsvTable lives in preview.tsx because JSX belongs .tsx. Quoted fields/embedded delimiters are out of demo scope, not an RFC4180 parser promise. Empty input has zero data rows; React text escaping covers cells.
CodeView displays one numbered row per split newline including final empty line, preserving spaces; JSON preview attempts JSON.stringify(JSON.parse(text),null,2) and falls back to original text on syntax error.
PreviewPane receives path/name metadata (size in bytes and mtime epoch-ms), a discriminated preview success or error/unsupported state. Derive success type from Awaited<ReturnType<ApiClient['fetchPreview']>> instead of duplicating network shape. No fetching or URL creation/revocation in components.
Markdown defaults rendered; buttons 查看源码 / 渲染视图 toggle; switching file identity resets rendered mode (keyed content or equivalent local state). Pure means no network/DB effects, not forbidding this local UI interaction.
Diagnosed browser repair: rendered Markdown document host subtree is keyed by exact text snapshot; MarkdownPreview/toggle state stays outside. Same-path content refresh preserves mode while replacing immutable document markup, avoiding React getHostSibling repeated placement scans. Acceptance retains the same mounted component across 1 MiB brackets → 1 MiB unmatched links → 200k strong nodes in development and production, with exact text/counts and responsive subsequent interaction.
Display path/size/mtime breadcrumb, supported text/Markdown/CSV/code/image; unsupported text 该类型不支持预览 and inline supplied sandbox_denied/preview_* message; truncated result shows banner and original total bytes, not text length. Image alt is filename; src is canonical caller-supplied image URL, not a Markdown destination.
Components use existing React/style conventions; minimal readable semantic layout without a new global styling system. CsvTable and CodeView are exercised as public component exports; no unused exported types/helpers.
Preserve provenance with source header comments naming local demo paths/line ranges, and keep original demo source header unchanged. No app-reference code is reused.
## Evidence / risks
Semantic jsdom snapshot covers supported Markdown DOM structure, not implementation source text. Separate DOM tests assert no script/img payload elements, event attributes or javascript href; legitimate escaped source strings remain visible literal text. Link click must leave location unchanged.
Literal code regression covers Markdown markers and injection strings inside closed/unclosed fences and inline code; do not merely assert output lacks a substring.
CSV headers/two body rows/count; CodeView numbered rows including empty; valid/invalid JSON; Markdown toggle/reset; image/unsupported/error/truncated states and metadata. Use real React DOM, no component mocks.
Repair evidence compares HTML/React supported structure including actual href="#" anchors, duplicate rows/lines without warnings, mouse/keyboard no-navigation, adjacent link labels, and bounded 1 MiB unmatched/dense marker inputs without quadratic scanning or argument-spread overflow.
Parent Chromium smoke mounts actual components using disposable harness outside production routing; exercise states/toggle/link click, capture screenshot and zero console/page errors. This does not claim /files page or server integration.
Risk: Blob URL lifecycle belongs #129 (replacement/unmount/stale result), components borrow URL only. Real endpoint interactions and lazy tree remain #129/#133; no premature combined parent requirement promotion.
Review focus: escaping/HTML insertion, code-context isolation, inert navigation, exact local state transitions, metadata and provenance, minimal scope.
Rollback: revert additive component commit; no persistence or production-route change.
