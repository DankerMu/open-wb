## 1. Fixture and semantic baseline
- [x] 1.1 Expanded fixture review PASS after revise1 pins the flat ServerConfig projection; strict validation exit0.133 existing source/test/config identities frozen. User-approved OMP_IDLE_MS range1..2147483647 recorded with Node overflow characterization. Parent existing compiled resolver shows semantic RED for new defaults/invalid idle/empty settings, and real HTTP app shows404 versus expected201 before implementation.
- [x] 1.2 Corrected A1 existing-callable baseline:22cases,13 semanticRED/9GREEN, no SETUP/timeout. Parent probes independently fail new defaults/invalid settings, actualHTTP404vs201 and actualstartup3vs5modules/accepted overflow. Before source authorization, parent rejected wrong401/502 expectation, pre-issued-token reuse assumption, nonfunctional duplicate fake child, upstream own-property overconstraint, removed cwd assertions and inert secret checks; originals/logs retained, existing RPC harness reused and exact behavior restored. Two corrected test hashes frozen.
## 2. Atomic configuration and assembly
- [x] 2.1 Extended canonical pure config with seven keys/defaults/overrides/named failures and shared agent-config constants/parser, no app→entry cycle or extra environment reads. Existing resolver/import/root identity preserved; entry passes resolved settings and records five modules.
- [x] 2.2 Real proxy then sessions always assemble with one registry and startup reconciliation. Typed app.sessions exposes actual owned handles. Three colliding fixtures migrated without production disable flags; REST stub uses existing standalone auth host with injected clock. Generic existing-logger fault observation retains supervisor shutdown errors and never closes caller DB.
## 3. Independent acceptance and delivery
- [x] 3.1 Eight parent compiled configuration/realHTTP/SQLite/process/shared-runtime-token scenarios GREEN; nine isolated semantic mutants rejected, restoration8/8 and dedicated stability8/8; post-cleanup8/8 rerun. Scoped Biome/build/types/anti-drift zero clones and full server60files/1123tests exit0 (92.36%statements/88.97%branches).133 protected baseline inputs audited; only authorized source/test paths changed, no dependency/threshold/CI change.
- [ ] 3.2 Expanded three-seat correctness/test-evidence+spec/invariant-security review, bounded two fix passes and exact-head CI; source PR merge.
- [ ] 3.3 Separate canonical full-startup requirement promotion/archive and parent3.7 completion with its own CI. #102 models.yml/full signal and #103 SSE remain unclaimed.
## Core risk mapping
API/entry→1.2/2.1/2.2/3.1 existing pure resolver and app consumers; config→2.1 all11keys; fileIO/path→3.1 entryroot/absolute/:memory:/no prevalidation sideeffects and no agent dirs at startup; schema/units→2.1 canonical milliseconds/user-approved native cap and exact startup record; auth/secrets→2.2/3.1 sharedregistry, cookie-vs-bearer precedence, no raw error/config logs; concurrency/order→2.2/3.1 auth→guard→proxy→sessions and reconcile beforetraffic, existing module preClose; resources→3.1 no eagerchildspawn and DB stays caller-owned; compatibility→1.2/2.2 existing auth/static/parser/startup failure/signal guarantees; errors/partialoutputs→2.1/3.1 namedkey pure errors and generic managed process failure records; release/packaging→3.1/3.2 compiled sameentry and unchanged asset packaging; docs→3.3 plus affected architecture notes.
## Project domain packs
Selected tenant/auth lifecycle, process/child-environment isolation, SQLite startup state, HTTP envelope, offline deployment and cross-service credential boundary map to2.2/3.1. No new sandbox enforcement/path traversal logic, DB migration, browser behavior, KB or dependency surface. Parent same-uid limitation remains; no production-release claim.
## Scope and governance
One writer/currentcheckout/no newworktree/no nesting. Parent owns specs/protected probes/baseline/acceptance; writer tests first, source only after baseline authorization. Authorized production scope app.ts/server.ts plus minimal canonical agent configuration helper if needed; module code changes only when strictly required to connect public types/default constants, with explicit parent approval. Test scope server-config, one assembly integration test if existing file cannot host it, existing three collision fixtures and affected startup expectation tests; no unrelated tests/config changes. Existing lower-seam fixtures may reuse the standalone real auth/guard pattern; never fake the app under test. RegisterSessions-owned store/supervisor construction remains canonical.
Writer skips formatting/lint/build/types/fullsuite/git and may run only authorized focused tests. Parent executes final gates. Reviewers read-only/no validation/nesting. No skipped assertions/relaxed discovery/thresholds; genuine uncertain edge regressions retained. New behavior on already-existing callable surfaces must fail semantically before source edits; new helper tests cannot substitute module-not-found for this evidence.
## Reconnaissance and decisions
Source facts: server.ts pure resolve before sideeffects; existing createApp callers and three manual module collisions; Fastify proxy plugin is encapsulated/deferred while root session registration/reconcile is synchronous; preClose executes in registration order. Prior startup race fix1919b75 remains untouched. Node24.13.1 overflow experiment:2147483648ms→1ms; user chose startup rejection over runtime timer splitting, so fixture adds max2147483647. Upstream whitespace/model-ID registry validation not expanded beyond parent requirements. Evidence directory /tmp/open-wb-issue101-evidence; scout reports are facts, not implementation authority.

## Implementation authorization
B starts only after the corrected callable RED baseline. Production scope app.ts/server.ts plus one canonical agent-config helper; actual typed app.sessions handles, shared registry and assembly injection are permitted to migrate existing consumers. The existing resolveServerConfig export remains the public seam. Tests frozen except expressly approved type/comment cleanup and the three collision/startup migrations; no behavior assertion weakening. Parent owns protected real compiled process/HTTP probes. Fixture/parent baseline snapshots and initial wrong judge versions remain retained; initial claimed SETUP for ignored extra options was corrected to semantic wrong-route behavior, not used as an API/type failure.

## Evidence integrity and deviations
Initial A1 judges were corrected before production authorization with originals retained: unauthorized proxy401 not502, runtime token rotation not pre-issued identity, working shared RPC harness instead of an inert EventEmitter fake, absence/undefined upstream equivalence, restored legacy cwd assertions, no unobserved secret claims. B migrated a proxy fixture to real TokenRegistry after typecheck exposed a structural fake; default error observation was corrected from bare stderr.write to the existing generic logger. No caller/child credentials are logged.

The old server-startup-order.test.ts pinned source-text call order/sticky assignments and was removed rather than re-pinned. An attempted executable replacement introduced an undeclared transitive jiti loader, so it too was removed without adding dependencies; actual compiled startup before-IO/order/secret behavior is exercised by parent startup-probe.mjs (valid/invalid), qualified by startup-module and secret-leak mutants, while existing config/import and lifecycle tests remain. Same-purpose type/shape-only config assertions were removed; user-observable defaults/overrides/invalid values and compatibility remain tested. Test count changes are explicit, not narrowed discovery.

Two anti-drift repair rounds removed unused exports and reused existing standalone auth/createSession/prompt helpers, preserving assertions and cleanup ownership. Final source differs from the qualified snapshot only by the corrected eleven-key doc comment; actual final compiled portfolio was rerun8/8. Local evidence and all rejected candidates/logs live under /tmp/open-wb-issue101-evidence; local permission separation is not an OS-enforced enclave, and independent exact-head CI/review remain mandatory.

## Review evidence closure
Round1 correctness/invariant seats were clean; evidence seat found a P2 coverage regression for the deleted sticky-failure source pins. Treated as blocking under the workflow and closed in fixpass1 with parent-only executable evidence, not restored source scans: sticky-failure-probe.mjs runs the real compiled entry with an actual filesystem failure and a call-through fs dependency hook that queues SIGTERM during cleanup. It asserts the installed handler ran, exit remains nonzero, exactly one generic failure record and no success. Isolated unconditional-exit0 and suppressed-failure-record mutants both fail semantic assertions; candidate/restoration/dedicated stability pass.

The evidence seat's thin full-upstream entry seam is also closed: entry-upstream-probe.mjs launches the actual configured production entry and executable fake child, captures the real runtime bearer in test-owned scratch, and proves env→entry→app→proxy forwarding to a real local upstream with exact credential/body and secret-free stdout/stderr. Dropping the entry's full upstream pair fails502vs202; restoration/stability pass. These two extra oracles and three mutants leave production/test bytes unchanged, complementing the original8cases/9mutants. Fresh re-review and same-head CI remain required.
