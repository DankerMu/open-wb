## Context
ParentS0b D4/spec defines three tables; current migration order is0010,002,010,030,031. migration-ledger.ts369-383 enforces a contiguous discovered prefix; index.ts28-41 executes only missing assets using the unchanged runner. A disposable actualopenDb experiment proved inserting020 blocks oldDB reopening while appending032 upgrades with oldreceipts unchanged. User approved032 and issue82 was updated.
References: migrations/010_auth_schema_seed.sql and031_workspaces.sql (explicitNOTNULL/type/domain checks, no IFNOTEXISTS); migration-runner.ts (runner-owned transaction); core-db-helpers.ts TRACKED_MIGRATION_FILENAMES/COMPLETE_CATALOG, referenced by auth/audit/workspaces/core catalog suites. LSP found20/10 references respectively; update shared source of truth rather than ad-hoc counts.
## Goals / Non-Goals
OnlySQL and schema tests: realconstraints, cascades, indexing, fresh/upgrade/reopen/rollback. No store/reconciliation, runtime/token changes, REST/SSE, rootassembly, oldmigration edits, validator relaxation or automaticdatabase deletion. No migrationSQL BEGIN/COMMIT; runner owns transaction and receipt.
## Schema decisions
chat_sessions: id TEXT NOTNULL PK,32 lowercasehex (rejectembeddedNUL; SQLiteTEXT PK alone permitsNULL); owner_id TEXT NOTNULL FKaccounts(id) cascade; title/omp_session_file nullableTEXT; status TEXT NOTNULL enumidle/running/done/failed; stream_epoch INTEGER NOTNULL DEFAULT0 with integer/nonnegative check; created_at/updated_at INTEGER NOTNULL with integer/nonnegative checks. Index owner_id,updated_atDESC; do notpinindexname inbehavior tests.
chat_messages: id INTEGER PRIMARYKEY AUTOINCREMENT; session_id TEXT NOTNULL cascadeFKchat_sessions; role TEXT NOTNULL user/assistant; content TEXT NOTNULL DEFAULT''; status TEXT NOTNULL done/running/failed; created_at INTEGER NOTNULL. ExplicitNOTNULL makes membership sets actually excludeNULL; no extra message roles or statuses.032 SHALL include an ascending(session_id,created_at,id) index for the already-planned ordered history query; indexname is not an API and no query API is added here.
chat_steps: id INTEGER PRIMARYKEY AUTOINCREMENT; message_id INTEGER NOTNULL cascadeFKchat_messages; ordinal INTEGER NOTNULL with integer/nonnegative check; name TEXT NOTNULL; detail TEXT NOTNULL DEFAULT''; status TEXT NOTNULL running/done/failed; started_at INTEGER NOTNULL; ended_at INTEGER nullable; UNIQUE(message_id,ordinal). No extra ended_at>=started_at/businessstate rule. Only sessiontimestamps/epoch andordinal have explicit nonnegative constraints in parent; do not silently broaden othernumericdomains. SQLite affinity remains normal, no STRICT table conversion.
## Governing invariant / siblings
Newmetadata obeys declared domains/referential ownership and cascades only through its own parent chain. The firstfive receipts and auth/audit/workspaces data stayunchanged;032effects and receipt commit together or rollback together. Existingledger/catalog trigger/sequence expectations staycanonical. Existingmigration bytes immutable.
Sibling audit_events actorFK uses ONDELETE RESTRICT (030 lines12-13) and append-onlytriggers. A parentaccount with audit history cannot be deleted; do not weaken this to forcechatcascade. Accountcascade tests use an owner without auditreferences; additionally retain audit-restricted deletion behavior with chat descendants unchanged afterrejection.
## Test-first and independent evidence
Because openDb already exists, tests can obtain semanticRED directly with SQLfile absent: actualdb hasfive receipts and no chat tables. No missing-import-only scaffold is needed. StageA tests plus sharedexpectedreceipt changes run while032absent; parentchecks genuine wrongschema/receipt failures before authorizing SQL StageB. Freeze and preserve the original testfiles as standalonebytes before latermechanical refactors.
Tests use actual node:sqlite/openDb and existinghelpers. Observe schema viaPRAGMA and realwrites: enum/null/id/NUL/epoch/ordinal violations, FKmisses at each level, uniqueness permessage, valid independentmessageordinals, defaults, generatedAUTOINCREMENTno-reuse, message/session/accountcascades with unrelatedrow preservation. Freshmemory and fileDB, snapshot pre032receipt/businessdata upgrade, stable reopen. Preexistingconflict at laterchat table causes migrationfailure; assert earlierDDL+032receipt absent and priorcommittedDB untouched; remove onlytestownedconflict andprove recovery.
Parent owns baselineoldDB createdbefore032, compiledruntime independent SQL probes and disposablewrongSQLvariants for constraint/foreignkey/cascade/receiptatomicity classes, reference/restoration/nochangecontrols. Run core-db+auth/audit/workspace affectedtests/fullservercoverage/types/lint/drift/build/strictOpenSpec and sameSHA CI. No testsource/compiler-only substitute for actualdb observations.
## Risks / Rollback
SQLiteNULL/CHECK behavior andNUL-length quirks→explicitguards+negativewrites. Ledgerorder→append032, oldDBupgrade regression and untouchedoldhashes. PartialDDL→unchangedrunnertransaction plus lateconflicttest. Cascades→crossowner/siblingpreservation. Preapplication revert is safe; afterapplication restore a preupgradeDBbackup with correspondingcode or designappend-onlyreversal, never simplyremove032/rewritereceipts. ParentEpicfinalarchive must reconcile this promoted requirement without reapplyingduplicates.
