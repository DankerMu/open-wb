## 1. Contract and regression baseline
- [x] 1.1 Expanded fixture review PASS initially and after the evidence-driven native-reopen correction; strict validation passed. Existing source/tests/configuration and original counterexamples are frozen before A. User approved standalone198→resume99; exact paused REST artifact identity and stash are recorded.
- [x] 1.2 New real-API regressions preceded source changes:19 collected,15 intended semantic failures and4 already-green compatibility controls. Original test hash b0a6fd37 retained before source-only B;120 existing source/test/config identities unchanged at A. No artificial inert baseline.
## 2. Atomic lossless read repair
- [x] 2.1 One encoding-aware core/db factory plus all SessionStore free-text/title reuse/compensation and audit-title projections implemented.19 focused tests GREEN; public string/null shapes and persisted TEXT retained. Audit detail JSON, schema, pins and write statements unchanged.
## 3. Independent acceptance and delivery
- [x] 3.1 Parent field/physical-title/encoding probes and unchanged frozen99 realHTTP oracle PASS. Fresh holdouts cover BOM/NUL/astral values across3encodings, ignored-encoding failure with unchanged title, interleaving and retained read accessor after native reopen.12 disposable compiled mutants rejected by assertions; five-probe known-good/restoration/dedicated-stability portfolio passed via qualify-text.py. Full server54files/1081tests,91.93% statements/89.41% branches; final build/types/scopedBiome/anti-drift pass. Full suite preceded test-only setup dedup; final focused19 GREEN and static gates follow it.
- [ ] 3.2 Expanded correctness/test-evidence+spec/invariant-state review, bounded fix gate, same-head CI and source PR merge.
- [ ] 3.3 Docs-only canonical delta promotion/archive with its own CI; then restore paused99 branch/stash, deliberately rebase dependency identity and rerun its unweakened acceptance before continuing the original queue.
## Core risk mapping
Public API selected→1.2/2.1: unchanged consumer DTOs plus one shared core helper; config/project setup not selected: pins/dependencies unchanged; filesystem selected only as evidence→3.1 file reopen, no production path change; schema/units selected→1.2/3.1 BLOB projection vs TEXT storage, database encoding and Unicode/BOM; auth selected→1.2/3.1 owner/audit filters unchanged; state/order selected→1.2/3.1 title compensation, interleaved connections and same-object native reopen; resources selected→2.1 bounded stateless decoders by encoding, one metadata read per operation rather than per row, no retained text/DB handles; compatibility selected→1.2/3.1 UTF8/UTF16/null/empty/existing suite; errors/rollback selected→1.2/3.1 propagate DB failures and preserve caller-owned transactions; release selected→3.1/3.2 compiled consumers and same-SHA CI; documentation selected→3.3 and core module API comment.
## Project domain mapping
Selected SQLite metadata fidelity/read-modify-write safety, tenant/audit visibility and offline deployability→1.2/3.1. No migration/catalog changes (receipts and existing data preserved). No process spawn/child environment, sandbox file access, new HTTP envelopes or browser routing; frozen HTTP composition is acceptance evidence only.
## Evidence integrity and deviations
One writer/current checkout/no new worktree. Parent owns fixture, independent scripts, fingerprints and acceptance. Writer may not read parent evidence/probes/manifests or paused99 artifacts; only its own reports/logs and explicitly authorized failure extracts. Existing tests remain unchanged; new regressions freeze before source-only B. No mock of the system under test, no skipped/deleted/weakened assertions or threshold changes. Reviewers read-only, no test/probe runs/nesting. Implementer skips formatter/linter/types/build/full suites/git; parent runs final gates. No OS-enclave or production-release claim. Local Darwin and Linux CI evidence are distinct. Original title damage is not retrospectively recoverable.

Deviations/evidence: fixture replaced its initially proposed per-DatabaseSync encoding cache after a real native-close/open experiment demonstrated stale identity; re-review PASS before implementation. A harness persisted UTF16 encoding by creating/dropping a temporary bootstrap table; source remained unchanged. After GREEN, parent formatting and a test-only shared setup extraction removed one jscpd clone while preserving19 cases and all behavioral assertions; no threshold changes. The atomic three-source-file repair plus regression corpus exceeds400-line review-only guidance; store.ts is800 lines, within the hard limit. Disposable replay/mutation trees removed; original RED, scripts, snapshots and logs retained under /tmp/open-wb-issue198-evidence and durable minimal repro in #198. Shared REST replay is a prospective-consumer proof only: no #99 source ships here. No writer read-boundary deviation reported and no OS-enclave/release claim.
