## 1. Contract and baseline
- [x] 1.1 Expanded fixture first-round PASS;136 existing source/test/judge identities frozen, LSP option/class consumers checked, user-approved synchronous contract recorded. Parent fourteen-case compiled baseline has two valid controls GREEN and twelve unsupported-return/getter semantic counterexamples RED; all owned native exits observed with the repaired containment runner.
- [ ] 1.2 Add the first failing public regression through a faithful actual app sink seam before source edits. Preserve current synchronous containment tests; do not use a wrapper that discards the returned thenable.
## 2. Synchronous containment
- [ ] 2.1 Add one private shared return guard to event/error sink call sites, document both options and app assembly semantics; cover native fulfilled/rejected/pending and non-native object/function thenables, getter failure and ordinary synchronous returns with vertical RED→GREEN evidence.
- [ ] 2.2 Preserve one-notification fault ownership, error-sink non-recursion, runtime/token retirement, no new async shutdown barrier, caller DB ownership and all existing synchronous faults/recording consumers.
## 3. Acceptance and delivery
- [ ] 3.1 Parent replays protected compiled/native cases, qualifies missing/partial/awaiting guard faults with restoration and dedicated stability, verifies exact scope, then runs focused/fullserver coverage, static/types/build/anti-drift/strict without changing discovery/thresholds.
- [ ] 3.2 Expanded three-seat review, bounded two fix passes, same-head source CI/merge, then separate additive canonical promotion/archive PR with independent CI before #214/#103.

## Core risk mapping
Selected entry/API→1.2/2.1 faithful sink return seam; schema/types→2.1 structural thenable versus ordinary void values; concurrency/state→2.1 pending/rejected work and one fault transition; resources→2.2 native/token cleanup; compatibility→2.2 existing sync throws and recorder values; errors→2.1/2.2 rejection consumption/non-recursion; tests/evidence→1.1/3.1 semantic RED versus setup/cleanup failure; release/docs→3.2 explicit options/canonical contract and same-headCI. Not selected config (no keys), file IO (no new production IO), auth/secrets (no auth or credential policy changes; native cleanup still audited).

## Domain risk mapping
Selected auth/session lifecycle (faulted pump/native ownership,2.2); process/child-environment isolation (real native retirement and no detached process-level rejection,3.1); offline deployability (zero new dependencies,3.1). Not selected tenant/sandbox enforcement, SQLite migration/catalog changes, server/web HTTP-envelope changes, browser runtime/navigation/persistence or cross-service network protocol. Existing DB remains caller-owned; no schema or permission changes.

## Ownership and execution
One writer/current checkout/no worktree/no nesting. Allowed source: server/src/sessions/supervisor.ts; JSDoc-only sink changes in sessions/index.ts and app.ts. Tests: augment server/test/session-supervisor-faults.test.ts; any helper expansion requires a concrete blocker before edits. All other source/tests/config/fixture and /tmp/open-wb-issue204-evidence are read-only to the writer. Writer may run only focused `npm exec --workspace server -- vitest run --no-coverage test/session-supervisor-faults.test.ts` during vertical TDD; skip formatter/linter/types/build/fullsuite/git. Parent owns verification and fixture updates. Reviewers read-only/no validation/nesting.

## Oracle integrity
Initial parent event-rejection probe observed the genuine Node detached rejection but then its runner attempted a group signal after leader exit and got EPERM; that attempt is not counted as a clean containment baseline. Originals/logs are retained. Revised runner signals a group only while its own leader is verified live; an uncaughtExceptionMonitor does not swallow Node fatal handling and stops only the probe-owned native ChildProcess handles. IPC identifies native children; non-destructive liveness verifies exit. The repaired core baseline was expanded before candidate authorization to fourteen cases: two GREEN/twelve semantic RED, all native exits confirmed, no cleanup error or timeout. Every expanded case reran against the current oracle fingerprint; earlier core logs remain historical rather than substitute evidence. Same-user local separation is not an OS enclave; independent same-head CI remains mandatory.
