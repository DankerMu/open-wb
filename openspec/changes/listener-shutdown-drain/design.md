Change surface: `createApp` Fastify assembly (`server/src/app.ts:83-...`, hook registration after all modules and routes), `closeOwnedApp`/`releaseOwned` callers in `server/src/server.ts:290-318` (only to pass the notifier), tests.
Must preserve:
- Shutdown order `runtime-exit → listener-close → db-close` (`server/test/server-startup-order.test.ts` "records native exit before listener close before database close", ~:135-172).
- Existing module `preClose` hooks and their order: model-proxy (`model-proxy/index.ts:56`), sessions (`sessions/index.ts:55`), SSE closing gate (`sessions/stream/sse.ts:61-97`) — late SSE attach still rejected; SSE streams still closed by their own owner.
- `forceCloseConnections` stays at the Fastify default `'idle'`; no request/route behaviour change; `requestTimeout` untouched.
- Exit-code semantics of `requestShutdown` (`server.ts:272-283`): shutdown without prior failure → 0; sticky failures keep 1.
- Apps created via `createApp` and used only with `inject()` (most tests) behave identically: hooks must be no-ops when the server never listened.
- Root-hook ordering assumption: Fastify runs a root instance's preClose hooks in registration order and only then recurses into encapsulated children (`node_modules/fastify/lib/hooks.js:142-146`). "Runs after every module preClose" therefore holds because every module preClose is a root `app.addHook` (model-proxy `index.ts:56`, sessions `index.ts:55`, SSE `sse.ts:63`) and none lives inside an encapsulated plugin; the final hook MUST be a root `app.addHook` registered last in `createApp`, and module preClose hooks must stay on the root app.
Must add/change: final `preClose` hook (registered last in `createApp`) sets a closing flag, calls `app.server.closeIdleConnections()` and arms the budget timer; an `onResponse` (or `reply.raw` `finish`) hook, when closing, schedules `setImmediate(() => app.server.closeIdleConnections())`; an `onClose` hook clears the timer. Budget expiry with the server still not closed (tracked by our own flag cleared in onClose / the `server.close` callback — NOT `app.server.listening`, which turns false as soon as `close()` is called) → `app.server.closeAllConnections()` once + `onListenerForceClose?.()` (guarded so a throwing callback cannot break close). Constant `LISTENER_CLOSE_BUDGET_MS = 2_000` exported/named; option `listenerCloseBudgetMs` validated as a positive finite integer (invalid → throw at assembly, like `sessionTtlMs`).
Governing invariant: after the last module `preClose` completes, the listener closes as soon as every accepted request has completed, and never later than the named budget; no request that is still in flight before the budget expires is cut.
Sibling surfaces: SSE long-lived responses (closed by the sessions preClose/owner before our hook — verify they do not count as "in flight" forever; if an SSE response could survive, the budget must still bound it); model-proxy streaming responses (upstream stream closed by its preClose); static file responses; `server.ts` `requestShutdown` / `releaseOwned` / `closeOwnedApp` / `closeOwnedDb`; test helpers that call `app.close()` after `listen` (`raw-http-helpers.ts`, `server-startup-helpers.ts`).
Seams under test: real `createApp` + real `app.listen` on 127.0.0.1:0 + real `http.request` keep-alive client (no `inject`), and the compiled entry for the stderr record/exit code where needed.
Required evidence:
- Re-drain (issue AC1/AC2): ordinary REST request held in a handler/preHandler by a deterministic barrier (test-only route registered on the app before listen) → start `app.close()` → release the barrier from a test `preClose` probe hook added to the returned app after `createApp` (root hooks run in registration order, so it runs after the final hook; alternatively release once `app.server.listening === false`, i.e. after `server.close()`'s single idle sweep) → response 200, full body, `Connection: keep-alive` → `app.close()` settles within 2 s with `listenerCloseBudgetMs` set well above the deadline (e.g. 10_000) AND `onListenerForceClose` never called, so a broken re-drain cannot be masked by escalation; record measured settle ms. On pre-fix source the same test must fail at the 2 s deadline (implementer shows RED; orchestrator probe: pre-fix misses 2 s, fixed ≈55 ms).
- No lost request (AC3): the 200 body is fully received before close settles; not achieved via `forceCloseConnections: true` or unconditional `closeAllConnections()`.
- Budget escalation: a request that never completes (held past a short `listenerCloseBudgetMs`, e.g. 200 ms) → close settles shortly after the budget, the client socket is destroyed, `onListenerForceClose` called exactly once; a normal (re-drained) close does not call it.
- Entry-level: forced close writes exactly one `{"event":"listener_force_close"}` stderr line and exit code stays 0 (compiled entry test, or a focused unit around the notifier if the entry test would be too slow — state which).
- Order contract test unchanged and green; SSE closing-gate tests green; full server suite green.
Non-goals: env/config override of the budget (YAGNI: no deployment currently needs a different value; adding a config key widens the validated config surface and its docs/tests — can be added when a real need appears); `requestTimeout`; native TERM/KILL timing (#205 done); route behaviour; claiming a fix for "permanent hang" (the defect is a bounded ~73 s window).
Review focus: hook placement really runs after all module preClose hooks (root registration, last); closed-state tracking not based on `server.listening`; re-drain cannot close a socket with an unfinished request; timer cannot keep the process alive or fire after close; escalation only on expiry; inject-only apps unaffected; exit code unchanged.
