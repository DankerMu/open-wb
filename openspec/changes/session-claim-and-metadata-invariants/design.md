Change surface: `SessionSupervisor` pump-exit bookkeeping (`server/src/sessions/supervisor.ts:317-327` pump registration/finally; `#claim` :336-342; `#releaseClaim` :344-351; `#retireSlot` :608-624) with the pump-exit bookkeeping extracted into a small unit-testable function; `SessionStore.bumpStreamEpoch` / `setSessionFile` (`server/src/sessions/store.ts:337-364`).
Must preserve:
- Duplicate admission → `session_busy` (early `#claims` checks in `#prompt`, `#claim`); `server/test/session-supervisor-admission.test.ts`.
- Infra fault / retire releases the current claim and removes the slot; shutdown ownership (`server/test/session-supervisor-faults.test.ts`); sink contract (`session-supervisor-sinks.test.ts`) unchanged.
- `slot.pump` cleared only by the pump that currently owns it; `#pumps`/`#releasePump` generation accounting unchanged.
- Store: `bumpStreamEpoch` increments exactly once and returns the new epoch for an existing session; `setSessionFile` persists metadata; caller-owned transaction and rollback behaviour, fault-injection receipt checks (`RAISE(IGNORE)` → generic receipt error) unchanged (`server/test/session-store*.test.ts`).
- Supervisor acquisition error provenance: a bumpStreamEpoch fault stays a generic (non-HttpError) acquisition fault → generic 5xx, as the existing `Supervisor dispatch and generation binding` requirement states.
Must add/change: pump exit releases `(slot, assistantMessageId)` unconditionally and clears `slot.pump` only if it is still this pump, implemented as an extracted function (module-level, exported only for tests or placed in a small sibling module) that the finally calls; no new public `SessionSupervisor` method; `bumpStreamEpoch` loses the unreachable branch; comments state trusted-caller existence ownership.
Governing invariant: `#claims` contains exactly the assistant ids of turns whose pump or dispatch is still in flight; every claim is released by its own turn's completion or by slot retirement, regardless of which pump currently occupies the slot.
Sibling surfaces: `#retireSlot` releases only `slot.claimedAssistantId` (the latest turn) — with the fix the old turn's claim is already gone, so no stale entry survives retire; pre-dispatch failure paths in `#prompt` (release on catch) and `#dispatchNew`; `#abortPreProgress`; subscriber/sink publish path that can re-enter `prompt` synchronously; store `acceptPrompt`/`startStep` (canonical not_found, untouched); REST routes never call the two metadata methods.
Seams under test: the extracted pump-exit function (direct unit); public `SessionSupervisor` API (`prompt`, sink `onEvent`, `shutdown`) with the existing supervisor test helpers (`server/test/session-supervisor-helpers.ts`) and a real in-memory SQLite store; public `SessionStore` methods on real SQLite.
Required evidence:
- Unit (RED/GREEN, issue AC2 option (b) + AC3): call the extracted pump-exit function with a claims map holding A and B and a slot whose `pump` is B's pump and `claimedAssistantId` is B, as A's pump exits → A removed from claims, B still present, `slot.claimedAssistantId === B`, `slot.pump` still B's pump. Also: when `slot.pump` is A's own pump → A removed and `slot.pump` cleared; releasing A never removes B. RED on the pre-fix logic (A kept when `slot.pump !== pump`), implementer must show by running the unit against the original finally semantics.
- Integration (behavioural regression, not claimed to produce RED): sink on turn A's `turn.end` synchronously calls `store.acceptPrompt(...)` then `void supervisor.prompt(...)` for turn B on the same session and returns `undefined` (returning a promise trips `synchronousSinkViolation`, `supervisor.ts:471-475`, and retires the slot); re-enter only once (A's turn.end only, otherwise B's turn.end would chain C). Assert B is admitted and completes, then a third prompt is admitted and completes; no infra fault retained.
- Store missing session: `bumpStreamEpoch("<absent id>")` and `setSessionFile("<absent id>", "x")` throw a non-`HttpError` `Error` whose message is the receipt message, leave all rows unchanged, and do not break a caller-owned transaction's rollback semantics.
- Existing supervisor admission/faults/sinks/stream/subscribe and session-store suites pass unchanged.
Non-goals: async sink lifecycle; changing admission gates, runtime turn state machine, REST mapping; canonical `not_found` for metadata writes; memory-profiling tests.
Review focus: claim release cannot free a *newer* turn's claim; no double-release side effects on `slot.claimedAssistantId`; the extraction is behaviour-preserving apart from the unconditional release and adds no public supervisor API; the integration test really re-enters on the same slot; store change removes only dead code and keeps receipt semantics.
