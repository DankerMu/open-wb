# Tasks: s1c-session-metadata-presentation

> 执行顺序（组级阅读顺序；真实依赖以各组尾部的边为准）：1（schema/配置/错误归属）→ 6（fake-omp / fake-upstream）→ 2（cwd 绑定）→ 5（跨端 DTO 同刀）→ 3（归约与发布）→ 4（元数据 REST 与删除）→ 7（web）→ 8（harness）→ 9（收尾）。组内与跨组的有向边写在每组尾部两行里（Stage 5 宽度门禁输入）；未写出的边即不存在。跨 change 的边一律写成 `change A #<issue>`（A = `s1c-turn-control-governance`，Epic #448），B 的每个 task 只在其列出的 A issue 全部合入后才可开工。
>
> 通用纪律：
> - size-guard（`scripts/size-guard.sh`，单文件 ≤800 行）：`store.ts` 798、`omp/runtime.ts` 797、`supervisor.ts` 770、`omp/process.ts` 732、`web/src/lib/api.ts` 749、`page.tsx` 734、`stream.ts` 717、`web/e2e/ui-walk.spec.ts` 799 均无余量，且 A 已对 `runtime.ts`/`store.ts`/`supervisor.ts`/`api.ts`/`page.tsx` 各做过一刀纯搬迁；B **不再拆既有文件**，全部新代码落 design「模块拆分」列出的新模块，既有大文件只加接线（每个 task 的验证都含 `bash scripts/size-guard.sh` 退出 0 + `knip` 零新增）。A 已占用的新文件名 B 不复用。
> - 新测试一律写进新建测试文件（`server/test/*.test.ts`、`web/test/*.test.ts(x)`），既有测试文件不增长（仅允许为既有断言改期望值，如 `session-events.test.ts:98-128` 噪声帧列表移出 `thinking_delta`、`core-db-helpers.ts` 回执常量、`fake-omp.test.ts` probe 期望串、`chat-composer.test.tsx` 中 `session-nav` 的引用改为 `session-sidebar`）。唯一的增长例外：`server/test/linux/uid-isolation.test.ts`——CI `uid-isolation` job（`.github/scripts/ci-uid-isolation.sh:225`）只运行该单一文件，4.3a 的「sudo 模式删除会话后文件不存在」用例只能落在此文件。
> - 服务端集成测试经 `server/test/session-supervisor-helpers.ts:98` 既有 `spawnImpl` 注入点选择 fake-omp `--scenario` 与子进程工作目录（`edit-write` 场景的路径由 fake 的 `process.cwd()` 拼出，把 cwd 放进/放出绑定空间根即可覆盖归属两侧）。
> - 跨端 DTO 键（会话八键、消息 `thinking`、步骤 `changes`）在 `hasExactlyKeys` 下是破坏性变更，只在 5.1 一刀同 PR 落地（design D11）；新事件类型 `thinking.delta`/`files.changed` 对旧 web 是未知类型、server 可先发。
> - 规格基线：B 的 MODIFIED 重述以 A 的 delta 为底；A 归档后、B 实施前补 `specs/turn-control/spec.md`（9.2）。

## 1. chat-sessions / http-service-skeleton / model-proxy — schema、配置与错误归属

- [ ] 1.1 `server/src/core/db/migrations/035_chat_session_metadata.sql`：按 design D1 五条 `ALTER TABLE … ADD COLUMN`（`chat_sessions.workspace_id TEXT REFERENCES workspaces(id) ON DELETE SET NULL`、`scene TEXT CHECK(...)`、`pinned_at INTEGER CHECK(...)`、`chat_messages.thinking TEXT`、`chat_steps.changes TEXT`），无 DEFAULT/NOT NULL/索引；A 的 034 之后第九条回执。`server/test/core-db-helpers.ts:13-23` 增 `MIGRATION_035` 进 `TRACKED_MIGRATION_FILENAMES`、:70-77 回执表增 `[9, MIGRATION_035]`、`sequenceRows` 为 9；`core-db-chat-schema.test.ts` 顺序断言追加第九条（改期望值）。验证：新建 `server/test/core-db-session-metadata.test.ts`：新库与「带数据的 034 库」升级后五列存在且可空、旧行全 NULL、`PRAGMA foreign_key_check` 为空；删一行 `workspaces` 后引用它的会话 `workspace_id` 变 NULL 而会话仍在；`scene='other'`、`pinned_at=-1`、`pinned_at=1.5` 被拒；`thinking`/`changes` 任意文本可写
- [ ] 1.2 `server/src/agent-config.ts` + `server/src/model-proxy/models-yml.ts` + `server/src/server.ts`：`MODEL_REASONING` 第十四个启动配置键（缺省 `on`；只接受精确 `on`/`off`，空串与其它值在任何副作用前启动失败，同 `OMP_IDLE_MS`）；`writeManagedModelsYml(agentDir, {proxyBaseUrl, modelId, reasoning})` 在 `reasoning === true` 时为唯一模型条目写 `reasoning: true` 与 `compat.reasoningContentField: reasoning_content`，`false` 时两者皆省略，输出确定；`server.ts:272` 重写 models.yml 处传入解析值。验证：新建 `server/test/model-proxy-reasoning.test.ts` 断言 on/off 两种 YAML 精确文本与非法值启动失败（不落任何文件）；`server/test/server-config.test.ts` 既有配置键计数/清单断言改期望值（含 `MODEL_REASONING`）
- [ ] 1.3 `server/src/http/errors.ts`：content-parser 归属集由 URL 集合改为 `method + matched route` 精确身份集合并去掉 :69 的 POST-only 门：A 的十条 + `POST /api/sessions`（POST 归属）+ `PATCH /api/sessions/:id`（PATCH 归属）= 十二条；`DELETE /api/sessions/:id` 不在集合内（带 malformed body 的 DELETE 走既有「非归属已注册路由」语义）。验证：新建 `server/test/http-parser-owners.test.ts`：十二条身份逐条 malformed/empty/非 JSON/超限 → 400 `bad_request`；同路由 `DELETE /api/sessions/:id` 带 malformed body 不被 PATCH 映射覆盖；`PATCH` 其它路由与 `GET` 归属集路由不映射；错误码表仍十三码

Suggested fixture level: expanded - 迁移、启动配置与 content-parser 归属集均为 public API / schema 硬触发项
Minimal mergeable slice: 1.1（atomic：五列同一迁移文件、同一回执号，拆成多条迁移只是多占回执序号且 A 的显式列 fork 插入要分次跟随）依赖 change A #449（034 必须先落账本，035 为第九条）；1.2（atomic：`MODEL_REASONING` 解析与 models.yml 写出是同一布尔值的定义端与消费端，单独合入解析端会留下无消费者的配置键）依赖 change A #451（`agent-config.ts` 同文件，A 先加 `OMP_MAX_PROCESSES`）；1.3（atomic：归属集判定方式的改法与十二条身份是同一个函数、同一测试文件；在 4.1/4.2 路由存在前，两条新身份对未注册路由不产生行为，单独合入保绿）依赖 change A #475、#467、#468、#469（A 的四条新路由把归属集扩到十条后 B 再扩到十二条）

## 2. omp-runtime / chat-sessions — 空间绑定 cwd

- [ ] 2.1 `server/src/sessions/omp/process.ts` + `omp/runtime.ts`：`SpawnOmpOpts`/`SessionRuntimeOpts` 增必填 `cwd`，`process.ts:52` 改用 `opts.cwd` 且子进程 `cwd` 选项与 `--cwd` 参数取同一值；`mkdir -p` 只保留给 session-dir/home/agent 与「等于所有者根的 cwd」，其它 cwd 不创建；本刀所有调用方仍传 `join(sandboxRoot, ownerId)`（行为不变）。验证：新建 `server/test/omp-spawn-cwd.test.ts` 断言 argv `--cwd` 与 spawn 选项相等、所有者根缺失时被建出、非所有者根 cwd 缺失时不 mkdir；既有 spawn 契约测试不改动全绿
- [ ] 2.2 `server/src/app.ts` + `server/src/sessions/index.ts` + `store.ts` + `supervisor.ts`（及 A 的 `turn-control.ts` 惰性获取处）：`createWorkspaceStore` 移到 `registerSessions` 之前；`RegisterSessionsOptions.workspaceRootOf(ownerId, workspaceId)` 由 `app.ts` 以 `rootOf({id: ownerId}, workspaceId)` 包装注入；`store.runtimeState` 增 `workspaceId`；`#dispatchNew`、regenerate 惰性获取、fork 临时进程统一计算 `cwd`：未绑定 → 所有者根；绑定 → `workspaceRootOf`，返回 null 或非已存在目录 → 不调用 `spawnOmp`、不创建、不回退，抛 `agent_unavailable`（prompt 502 + 既有补偿）。验证：新建 `server/test/session-workspace-cwd.test.ts`（supervisor + 真实 fake-omp）：绑定会话 probe `cwd=` 为空间根、未绑定为所有者根；空间根被删后 prompt 502、未 spawn、目录未被重建、受理对已移除；`app.ts` 装配次序单测（既有 app 装配测试改期望值）

Suggested fixture level: expanded - 改 spawn 契约与 app 装配次序，子进程 cwd 只有真实 fake-omp 能证明
Minimal mergeable slice: 2.1（atomic：`cwd` 参数的定义端与 process.ts 消费端必须同刀，否则 argv 与 spawn 选项来源分裂；本刀调用方传值不变，单独合入保绿）依赖 change A #452、#460（`runtime.ts` 拆分与 `process.ts` 审批识别先落，避免同文件冲突）；2.2（atomic：`workspaceRootOf` 注入、`runtimeState.workspaceId` 与三处 cwd 计算是同一条「绑定 → cwd」数据流，只注入不消费会被 knip 标记）依赖 2.1、1.1（`workspace_id` 列）、6.2（probe `cwd=`）、change A #454、#487（store/supervisor 拆分）、#465、#466（regenerate/fork 的惰性获取与临时进程处）

## 3. chat-stream / thinking-fold / turn-artifacts — 归约与发布（服务端）

- [ ] 3.1 `server/src/sessions/events.ts`：回合内 assistant `message_update.assistantMessageEvent.type==="thinking_delta"` 且 `delta` 非空 → `thinking.delta{messageId, delta}`；`thinking_start`/`thinking_end` 与 `message_end` 内 thinking 块不产生事件；`ChatEvent` 联合增 `thinking.delta` 与 `files.changed{messageId, stepId, files:[{path, added, removed, kind}]}` 两型（`files.changed` 本刀只定义类型不产出）；`supervisor.ts` `persistEvent`（:641-692 的 switch 无 default）为两型各加显式分支且本刀均返回 `undefined`（不落库、不发布；3.3/3.4 分别替换），使联合扩展后 switch 仍穷举且中间切片不会把 `details` 原始绝对路径发上 SSE；`server/test/session-events.test.ts:98-128` 噪声帧列表移出 `thinking_delta`（改期望值）。验证：新建 `server/test/session-events-thinking.test.ts` 断言回合外/空 delta/start-end 帧不产出、回合内逐帧一条；新建 `server/test/session-persist-new-events.test.ts` 断言 `persistEvent` 对两型返回 `undefined`、ring 无事件、无落库
- [ ] 3.2 新建 `server/src/sessions/file-changes.ts`（纯函数）+ `events.ts` 接线：对已知 running 调用、帧与 `result` 均非 `isError`、`details` 为普通对象的 `tool_execution_end` 提取候选——`edit`：`perFileResults[{path, diff}]` 逐项，否则 `details.path`+`details.diff` 一项，`kind:"edit"`，`added/removed` = 按 `\n` 切分后匹配 `^\+\d+\|`/`^-\d+\|` 的行数；`write`：`details.resolvedPath` → `{kind:"write", added:null, removed:null}`；其它工具（含 `ast_edit`、bash）永不；有候选时在该调用 `step.end` 之前、同一返回结果中紧邻输出 `files.changed{stepId:<toolCallId>, path:<原始路径>}`；`normalizeOutput` 仍丢弃 details。验证：新建 `server/test/file-changes.test.ts`（diff 计数边角：上下文行 ` N|`、无编号 `+`、CRLF、空 diff；perFileResults 优先；isError 不产出；原型键不读）与 `session-events-files.test.ts`（次序紧邻 `step.end`、bash 无事件）
- [ ] 3.3 新建 `server/src/sessions/store-thinking.ts`（`appendThinking(messageId, chunk)`：单条 `UPDATE chat_messages SET thinking = COALESCE(thinking,'') || ?`，共享上限 32768 码点、不拆代理对，超限那次只落「填满上限的前缀 + `…（已截断）`」并返回实际落库片段，此后返回空且不写）+ `thinking-buffer.ts`（每助手消息一缓冲，注入 `SessionClock`；累计 2048 UTF-8 字节 / 首段起 2000 ms / 同回合其它事件发布前 / 回合终态前任一时刻整段刷出）+ `supervisor.ts` 接线：刷出投递到 pump 串行提交链，先 `appendThinking` 再在同一同步段发布该片段（中间无 await），落库失败不发布走 owned error-sink；`applyFailure`/`applyStop`/崩溃/优雅关停前刷出。验证：新建 `server/test/session-thinking.test.ts`（supervisor + fake-omp `thinking` 场景）：三段小 delta 在首个 `text.delta` 前刷为一条 `thinking.delta`；快照 `thinking` 恰等于已发布片段之和；注入时钟推进 2 s 触发；32K 截断只发一次标记且此后不再发布；stop 途中缓冲被刷出
- [ ] 3.4 `file-changes.ts` 归属判定（IO）+ 新建 `server/src/sessions/store-metadata.ts` 的 `setStepChanges(stepId, json)` + `supervisor.ts` `persistEvent` 分支：会话未绑定 → 丢弃事件不落库不发布；绑定时逐候选：绝对原样/相对拼会话 cwd → realpath（不存在则父目录 realpath + 文件名，两者都失败丢弃）→ 严格位于空间根 realpath + 分隔符之内（根自身/根外/符号链接逃出丢弃）→ 存为相对空间根 `/` 分隔路径，UTF-8 >1024 字节丢弃 → 同步骤同路径合并（计数求和、位置取首次）→ 超 50 项只留前 50；无幸存项返回 `undefined`；有则 `setStepChanges` 提交成功后以数字步骤 id 发布 `files.changed`，随后 `step.end` 照既有路径经 `finishStep`（不读写 `changes`）。验证：新建 `server/test/session-file-changes.test.ts`（supervisor + fake-omp `edit-write` 场景）：cwd 在空间根内 → 两条 `files.changed` 先于各自 `step.end`、`chat_steps.changes` 已提交且键恰为 `path/added/removed/kind`；cwd 在根外 → 无事件、列 NULL；未绑定会话 → 无事件；符号链接逃出被丢弃；`setStepChanges` 注入失败 → 不发布、ring 序号不推进

Suggested fixture level: expanded - 改纯协议归约与 supervisor 发布次序，事件为 SSE 公共契约，合并时序只有真实子进程能证明
Minimal mergeable slice: 3.1（atomic：thinking 映射与两个事件类型定义同落 `events.ts`，类型先定义才能让 3.2/3.3 的两侧各自编译；server 先发 `thinking.delta` 对旧 web 安全，单独合入保绿）依赖 change A #455、#453（A 的归约改动与联合扩展先落）；3.2（atomic：候选提取与「紧邻 `step.end`」接线是同一纯函数的定义与唯一调用点，knip 要求有调用方；本刀产出的 `files.changed` 在 3.4 落地前被 3.1 加的 `persistEvent` 显式分支丢弃，不发布原始路径）依赖 3.1；3.3（atomic：缓冲刷出、落库与同步段发布是「快照恰等于已发布之和」这一不变量的三个不可分部分）依赖 3.1、1.1（`thinking` 列）、6.1（fake `thinking` 场景）、change A #487、#490（supervisor 拆分与 stop 终态路径）；3.4（atomic：归属判定、`setStepChanges` 与「提交后发布、`step.end` 在后」是同一持久化次序契约）依赖 3.2、2.2（会话 cwd 与空间根）、1.1（`changes` 列）、6.1（fake `edit-write` 场景）

## 4. session-metadata — 元数据 REST、删除与 fork 继承（服务端）

- [ ] 4.1 新建 `server/src/sessions/rest-metadata.ts` + `store-metadata.ts` 的 `createSession(owner, {workspaceId?, scene?})`：`POST /api/sessions` 入 parser 归属集（16 KiB）；无 body 与 `{}` → 201 三新键 null；有 body 须为 `{workspaceId?, scene?}` 子集（多余键、`workspaceId` 非字符串或 `null`、`scene` 非三值、数组/`null` 根 → 400）；`workspaceId` 经所有者作用域 `rootOf` 为 null（不存在/他人/形态不合法）→ 三者逐字相同的 404、不写行；插入与 `session.bind` 审计（`title:"绑定工作空间"`，`detail:{sessionId, scene}`）同一事务，未绑定不审计；201 八键 DTO。验证：新建 `server/test/session-metadata-rest.test.ts`（inject + 真实 SQLite）覆盖 201 三态、400 各形状、404 三者响应字节相等、审计行与会话行同事务（注入审计失败 → 无会话行）；`smoke/chat.hurl:16` 与 web 既有无 body 创建不受影响（既有 `session-rest.test.ts` 不改动全绿）
- [ ] 4.2 `rest-metadata.ts` `PATCH /api/sessions/:id` + `store-metadata.ts` `patchSession` + `store.ts` `rollbackPrompt` 标题规则：preParsing `requireOwnedSession`（401/404 先于 body）；body 为 `{title?, scene?, pinned?}` 非空子集（`{}`、多余键含 `workspaceId`、类型不符、`scene:null` → 400，任一不合法整体 400 不写列）；`title` trim 后 1..80 码点；`pinned:true` → `COALESCE(pinned_at, now)`，`false` → NULL；单条所有者作用域 UPDATE 只写所给列、不改 `updated_at`/status/`workspace_id`，影响 0 行 → 404；running 与占用期间均允许；不审计；200 八键。Turn 记录 `titleSetByPrompt`，PATCH 写 title 时把活跃 Turn 的 `titleTouched` 置 true，`rollbackPrompt` 仅当 `titleSetByPrompt && !titleTouched` 恢复 `previousTitle`。验证：`session-metadata-rest.test.ts` 追加 PATCH 用例（本 change 新建文件可增长）：三字段单独/组合、400 全形状、他人会话 404 且 body 未解析（malformed body 仍 404）、`updatedAt` 不变、已置顶再 `pinned:true` 时刻不变；新建 `server/test/session-title-rollback.test.ts`：未命名会话受理 → PATCH 标题 → 注入派发失败 → 502、status/`updatedAt` 复原而标题仍为 PATCH 值；不 PATCH 时标题回到 null
- [ ] 4.3a 删除（非 running 路径）：新建 `server/src/sessions/session-delete.ts` + `store-metadata.ts` `deleteSession` + `supervisor.ts` 公开 `retire(sessionId)` 与 `subscribe` 第四参 `onEnd` + `stream/sse.ts` 传入 `endOwned` 闭包 + `rest-metadata.ts` `DELETE /api/sessions/:id`：401/404 先于任何 supervisor 调用；登记 A 的控制占用（已被持有 → 409 `session_busy` 无副作用）并加入删除墓碑集；非 running → `retire`（有 slot 走既有 `#retireSlot`、丢弃 slot 与事件环；对每个订阅者调用关闭回调——SSE 连接 `raw.end()` 不写事件——并清空订阅集；墓碑期新 `subscribe` 立即得到已关闭订阅）→ store 单事务（确认无活跃回合/缓冲/步骤内存态，否则视为不变量破坏、通用失败不删行；读出 `omp_session_file` 与消息数 → `DELETE FROM chat_sessions WHERE id=? AND owner_id=?` 级联；fork 子会话 `parent_session_id` 置 NULL → `session.delete` 审计 `{sessionId, ompSessionFile, messageCount}`，审计失败整体回滚）→ 提交后 `unlink` 当前 `omp_session_file`（ENOENT 视为成功；其它错误经 `onError` 上报仍 204）→ `finally` 释放占用与墓碑 → 204 无 body；之后该 id 的 messages/events/PATCH/DELETE 与未知 id 相同地 404。验证：新建 `server/test/session-delete.test.ts`（supervisor + fake-omp + inject）：idle 会话（空闲进程存活）删除后进程退出、两个 SSE 订阅者连接结束且无任何事件、行与文件消失、审计行存在、再 DELETE 404、fork 子会话保留且 parent 为 NULL、占用被他路持有 → 409 无副作用、unlink 非 ENOENT 失败 → 204 + 错误上报；`server/test/linux/uid-isolation.test.ts` 新增「sudo 模式删除会话后会话文件不存在」用例（Linux CI job 证据；design Open Questions 3）
- [ ] 4.3b 删除（running 路径）：`session-delete.ts` 在已持有占用下执行与 A stop 相同的停止序列（挂起审批 `deny` 结算 → `abort`，或派发前登记停止意图）→ 等待该回合 `turn.end` 落库发布 → 转入 4.3a 的 retire → 删行 → 删文件；上界由 A 的 `OMP_ABORT_GRACE_MS`/retire 升级保证，不另设计时器；第 2–4 步失败 → 通用 5xx、行保留。验证：`session-delete.test.ts` 追加：`abort-ok` 场景 running 时 DELETE → 助手与会话先 `stopped` 落库再消失、订阅者见到 `turn.end stopped` 后连接结束；`approval` 场景 pending 审批时 DELETE → 审批 `deny` 结算后删除；`abort-ignored` 场景 → 有界退回 retire 后仍 204，总时长受 A 上界约束；删除期间并发 prompt/regenerate/fork/DELETE 均 409，stop 按自身规则 202/204
- [ ] 4.4 fork 继承：A 的 `server/src/sessions/store-branch.ts` fork 新会话显式列插入增 `workspace_id`、`scene`（取源会话值），`pinned_at` NULL；分叉点前消息/步骤拷贝语句分别增 `chat_messages.thinking`、`chat_steps.changes`；fork 继承不写 `session.bind` 审计；fork 201 的 `session` 为八键（投影由 5.1 提供）。验证：新建 `server/test/session-fork-metadata.test.ts`：绑定+场景+置顶的源会话 fork → 新会话 `workspaceId/scene` 相同、`pinnedAt` null、无 bind 审计；含 thinking 与 changes 的历史 fork 后逐字相等；fork 会话 prompt 的 probe `cwd=` 为源空间根

Suggested fixture level: expanded - 三条公共 REST 路由、审计事务与进程/订阅回收，均为 public API 与 supervisor 状态机硬触发项
Minimal mergeable slice: 4.1（atomic：POST body 解析、`rootOf` 校验与绑定+审计同事务是同一条创建路径，拆开会留下可绑定但不审计或可解析但不落库的中间态）依赖 1.1、1.3、5.1（201 八键投影）、change A #449；4.2（atomic：PATCH 三字段共用同一校验器与同一 UPDATE，标题补偿规则与 PATCH 写 title 是同一 `titleTouched` 标志的两端）依赖 1.1、1.3、5.1；4.3a（atomic：`retire`/`onEnd` 若无 DELETE 调用方会被 knip 标记，删行事务与 unlink 次序是同一不变量「行先于文件消失」；非 running 删除本身就是可交付的用户功能，是删除的首刀）依赖 1.1（审计 `workspaceId`）、change A #487（supervisor 拆分）、#463（池 `onExit` 释放名额）、#473（控制占用登记 API）；4.3b（atomic：占用下停止 → 等终态 → 转入 4.3a 是一条连续状态机，任何前缀都不是可签收的 running 删除证据）依赖 4.3a、change A #490、#464、#474（停止意图、审批结算与非作答结算）、#456、#458（fake `abort-ok`/`approval` 场景）；4.4（atomic：三条显式列语句描述同一次 fork 的同一份历史，少一条即静默丢数据）依赖 1.1、5.1、2.2（fork 临时进程 cwd）、change A #466

## 5. chat-sessions / chat-web — 跨端 DTO 同刀

- [ ] 5.1 跨端同刀：server `store.ts` `SESSION_COLUMNS`/`toSessionView` 八键 `{id,title,status,createdAt,updatedAt,scene,workspaceId,pinnedAt}` 作用于列表、创建、消息快照 `session` 与 A 的 fork 201；消息投影增 `thinking: string|null`（用户消息恒 null）；步骤投影增 `changes: Change[]|null`（列 NULL → `null`，非空 JSON → 解析数组）；web `web/src/lib/session-contract.ts` 会话八键严格解析（`scene` ∈ 三值|null、`workspaceId` 字符串|null、`pinnedAt` 非负安全整数|null）、消息 `thinking`、步骤 `changes`（元素键恰 `path/added/removed/kind`，`kind` ∈ `edit|write`，计数为非负安全整数或 null）——缺键/多键/值不合法整体视为无效成功响应；`web/src/lib/api-sessions.ts` 增 `createSession(body?)`（有 body 才发 `application/json`）、`patchSession(id, patch)`（空对象不发请求、`TypeError` 拒绝）、`deleteSession(id)`（恰 204 成功、200 视为无效）。验证：server 新建 `server/test/session-snapshot-metadata.test.ts` 断言四处投影形状（含 fork 201 与 NULL/非 NULL `changes`）；web 新建 `web/test/session-contract-metadata.test.ts` 与 `api-sessions-metadata.test.ts` 断言解析三键、拒绝旧五键与多键、三方法请求形状与 204/200 分支

Suggested fixture level: expanded - 公共 REST 快照形状与跨端严格解析同刀，public API 硬触发项
Minimal mergeable slice: 5.1（atomic：`hasExactlyKeys` 严格键集使 server 投影与 web 解析任一侧先合入都让会话页整体失效——server 先加键 → 旧 web 判整个响应非法；web 先要求键 → 旧 server 响应非法——两侧无法各自保绿，见组尾 Width exception）依赖 1.1（三列存在）、change A #476（A 的 5.3 同刀先落，消息键集含 `approvals`）、#472（`stopped` 联合与 `api-sessions.ts` 四方法）、#469（fork 201 路由存在才有其投影可改）、#471（`api-sessions.ts` 由 A 拆出）
Width exception: multi-path - 5.1 `hasExactlyKeys` 严格键集使 server/web 任一侧先合入都会让会话页整体失效，两侧无法各自保绿，故 server 投影与 web 解析同一 PR、两条验证路径（server inject 与 web 单测）

## 6. omp-test-harness — fake-omp / fake-upstream

- [ ] 6.1 `server/test/support/fake-omp.mjs` 新 scenario `thinking`（`agent_start` 后 `thinking_start{contentIndex:0}`、恰三段 `thinking_delta`（`先读需求，`/`再列要点，`/`最后作答。`）、`thinking_end`、≥3 段正文 delta、content 含 thinking 块与文本块的 `message_end{stop}`、`agent_end`；任何 `--approval-mode` 下无工具帧与 select）与 `edit-write`（一个双工具 `message_end{toolUse}`；`edit` args 恰 `{input}`、end 帧 `details:{path:"<cwd>/notes.md", diff:"+1|a\n+2|b\n-3|c\n 4|d"}`；`write` args `{path:"out/report.html", content}`、end 帧 `details:{resolvedPath:"<cwd>/out/report.html"}` 无 diff；`<cwd>` 为 fake 的 `process.cwd()`，每个 end 帧前真实写出所报告文件（建 `out/`）；无审批）。验证：新建 `server/test/fake-omp-metadata-scenarios.test.ts`（真实子进程）断言两场景帧序列、thinking 拼接常量、文件真实存在、`yolo`/`write` 下均无 select
- [ ] 6.2 probe 尾部 `frames=` 之后追加 ` cwd=<process.cwd()>`（最后一个字段）；`server/test/fake-omp.test.ts` `expectedProbeReport` 精确串与 `server/test/linux/uid-isolation.test.ts` `REPORT_LABELS` 以 `"frames", "cwd"` 结尾同 PR 更新（改期望值）。验证：本地 fake-omp 测试绿 + CI uid-isolation job 绿的证据
- [ ] 6.3 `server/test/support/fake-upstream.mjs` 两个 prompt 文本标记（与 `WORKBUDDY_UI_WALK:<uuid>` 同一「最后 user 文本子串」查找，可组合）：`WORKBUDDY_THINK` → 作答轮在不变的正文分片前先发三个 `reasoning_content` 分片（拼接恰为 `先读需求，再列要点，最后作答。`）；`WORKBUDDY_WRITE` → 工具轮（`toolFrames` :366 旁新增分支）改发 `write {"path":"workbuddy-report.html", …}` 而非 bash；无标记请求逐字节不变。验证：新建 `server/test/fake-upstream-markers.test.ts` 断言三种组合的 SSE 分片序列与无标记请求字节不变；既有 `fake-upstream.test.ts`/`fake-upstream-gates.test.ts` 不改动全绿

Suggested fixture level: compact - 测试支撑脚本 + 自带契约测试，无生产代码、无共享入口
Minimal mergeable slice: 6.1（atomic：两个 scenario 各自独立分支，但共享同一份新契约测试文件与「场景计数八→十」的同一处 spec 计数，拆成两刀只是重复起进程）依赖 change A #470（A 的八个场景全部合入后计数才为十；scenario 分支互不共享状态，功能上无依赖）；6.2（atomic：probe 字符串精确相等使 fake 输出与两处解析期望值必须同 PR）依赖 change A #459（`frames=` 先落，`cwd=` 在其后）；6.3（atomic：两个标记共用同一子串查找与同一契约测试文件，且 ui-walk 以二者组合驱动）无前置依赖（无标记路径逐字节不变）

## 7. chat-web / session-sidebar / spa-shell / thinking-fold / turn-artifacts / conversation-search — web

- [ ] 7.0 `web/src/ui/icon.tsx` 注册 `star`、`pencil`、`trash`、`more-horizontal`、`package`、`download`、`globe`、`palette`、`chevron-up`、`filter`、`search`、`folder`、`code`（master 已有 `img`/`file-code`/`file-text`/`chevron-right`，A 加 `square`/`refresh-cw`/`git-branch`）；`web/src/lib/topbar.tsx` context 增 `actions: readonly TopbarAction[]`（`{key, label, icon, expanded?, onSelect(trigger)}`），`useTopbar({breadcrumb, actions})` layout effect 上报、Provider 仅在 `key/label/icon/expanded` 浅比较变化时 setState、`onSelect` 经 ref 读最新闭包、卸载/未提供时清空；`routes/shell/topbar.tsx` 在 h1 之后渲染 `.topbar-actions`（ghost icon Button，accessible name 与 Tooltip 为 `label`，`expanded` 定义时带 `aria-expanded`；h1 名不含按钮）；无 actions 时三态 DOM 不变。验证：新建 `web/test/topbar-actions.test.tsx`：注入/清空/顺序/`aria-expanded`、每次渲染新回调不触发重复上报、无 actions 时与既有快照一致；既有 shell 测试不改动全绿
- [ ] 7.1 新建 `web/src/features/chat/session-groups.ts`（纯函数：筛选 状态 全部/进行中=`running`/已完成=`done|failed|stopped` × 时间 全部时间/今天=本地日历日/更早，取交集；分区 置顶 > 空间（按 `workspaceId` 分子组、顺序为工作空间列表顺序、不在列表 → 末位 `未知空间`）> 任务，互斥、保持服务端顺序）+ `session-sidebar.tsx`（取代并删除 `session-nav.tsx`；分区 `置顶任务`（无计数）/`任务 (n)`/`空间 (n)` 及子组为带名 `role="group"`，空分区不渲染，无任何会话 → `没有匹配的任务`；保留选择按钮、`新会话` 标题回退与状态元素；`新建会话` 名称不变）+ `session-filter.tsx`（`筛选任务` Popover，两个 `role="radiogroup"`，选择即生效、弹层保持打开，状态只在页面内存）；`ChatPage.refreshList` 并行拉取会话与工作空间列表；`chat-composer.test.tsx` 中 `session-nav` 引用改指 `session-sidebar`（改引用不增长）。验证：新建 `web/test/session-groups.test.ts`（互斥、计数、`未知空间`、今天/更早跨午夜边界、`idle` 只在全部）与 `chat-page-sidebar.test.tsx`（三分区 DOM、筛选交集、空态、工作空间读取失败 → `未知空间`）
- [ ] 7.2 新建 `session-menu.tsx`（行尾 `更多操作：<显示标题>` 按钮打开 Menu：`重命名`/`置顶任务`|`取消置顶`/`删除`（danger），任何状态可用）+ `rename-dialog.tsx`（标题 `重命名任务`、输入 `任务名称`、trim 空时 `保存` 禁用、Enter/保存 → PATCH → 更新列表与面包屑、toast `已重命名`；失败保持打开并 `role="alert"` 显示信封 message）+ `session-actions.ts`（置顶/取消 → PATCH → toast `已更新置顶状态`；删除 ConfirmDialog 标题 `删除任务`、说明 `确定要删除「<显示标题>」吗？删除后不可恢复。`、确认 `删除` 请求中忙碌；204 → toast `任务已删除`、移出列表，若为当前会话则关闭事件流、以 replace 移除 `?session=`（保留其它 search/hash）回欢迎态；失败 → 关闭确认框、toast 信封 message、重新读取列表）+ 会话页顶栏第二态注入 `重命名` action（pencil，同一 Dialog）。验证：新建 `web/test/chat-page-session-actions.test.tsx`：菜单三项、重命名成功/失败、置顶往返与分区迁移、删除当前/非当前会话（URL 变化、事件流关闭、toast）、409 文案
- [ ] 7.3 新建 `scene-pills.tsx`（hero 与快捷任务行之间 `场景` 组：`日常办公`/`代码开发`/`创意设计`，`aria-pressed`，默认日常办公；切换换快捷任务清单并 toast `已切换到「<场景名>」场景`，重复点击无变化）+ `welcome-content.ts` 增三组场景清单（demo:1221-1239）+ `composer-footer.tsx`（仅欢迎态 composer 卡片内工具栏之后：`任务启动于 <空间名>|未选择`（folder）→ Popover：`搜索工作空间` 大小写不敏感过滤、恒有 `未选择`、各空间「名称 + `<account>/<dir>`」按返回顺序、无匹配 `没有匹配的工作空间`、读取中 `正在读取工作空间`；不渲染权限元素/新建/挂载）；欢迎态首次发送与侧栏 `新建会话` 均以 `POST /api/sessions {scene, workspaceId?}` 创建（场景与空间为页面内存状态）。验证：新建 `web/test/chat-page-welcome-scene.test.tsx`：胶囊切换与清单、toast、footer 过滤/选择/未选择、首次发送与 `新建会话` 的请求 body、会话页不渲染 footer 与胶囊
- [ ] 7.4 新建 `stream-thinking.ts`（`thinking.delta` 严格解码与归约：按 `messageId` 追加到消息 `thinking`，未知消息忽略）+ `thinking-block.tsx`（`<details class="thinking-block">`，summary `深度思考过程` 前置 `chevron-right`，主体 `pre-wrap` 纯文本不经 Markdown；消息 running 展开、进入 `done|failed|stopped` 收起、快照终态为收起、两次迁移间手动开合保留；null/空串不渲染；不参与 `复制`）+ `conversation-view.tsx` 助手块次序 思考折叠块 → 审批条 → 正文 → 步骤卡 → 错误 → 文件变更卡 → 产物卡 → `已停止` 徽章 → 操作行（本刀只插入折叠块位）+ `stream.ts` `DATA_EVENTS` 增 `thinking.delta` 接线。验证：新建 `web/test/chat-thinking.test.tsx`：解码拒绝形状不符、归约追加、开合三态迁移、null 不渲染、32K 标记原样显示、`复制` 不含 thinking
- [ ] 7.5 新建 `stream-artifacts.ts`（`files.changed` 严格解码与归约：按 `stepId` 设置步骤 `changes`，后到覆盖）+ `file-changes-card.tsx`（一条助手消息只汇总**已结束**步骤的 `changes`，按路径去重取 ordinal 最大者、位置取首次；卡头 `文件变更（N 个）`；行 `+a`/`-d`（>0 才显）或 `写入` + 逻辑路径 `<account>/<dir>/<path>` + `查看详情 <逻辑路径>` 按钮 → `/files?ws=<workspaceId>`；空间不可解析时只显相对路径、无 `查看详情`、不渲染产物卡）+ `artifact-card.tsx`（同一汇总与顺序，按扩展名派生：`html` → globe/`HTML`/`打开网页预览`/卡脚 `可交互预览` → 点击时 `fetchPreview` → Dialog（标题文件名）内 `<iframe sandbox="allow-scripts" srcdoc title>`，截断时 iframe 上方 `文件超过 1 MiB，仅预览前 1 MiB`；`png`/`jpg|jpeg` → `img`/`PNG`|`JPG`/`下载` → Blob URL 临时链接下载后撤销；`md/txt/log/csv/json/js/ts/tsx` → file-code/大写扩展名/`复制代码` → 剪贴板 → toast `已复制到剪贴板`|`复制失败`，截断 → 不写剪贴板 toast `文件过大，无法复制`；其它扩展名不派生；拉取中按钮禁用不并发、卸载/切会话 abort 并撤销 Blob URL；预览失败 toast 信封 message 不开 Dialog）+ `conversation-view.tsx` 插入两卡位 + `stream.ts` `DATA_EVENTS` 增 `files.changed`。验证：新建 `web/test/chat-page-artifacts.test.tsx`：running 步骤不出卡、`step.end` 后出卡、去重取最新、`+a/-d`/`写入`、`查看详情` 导航 URL、html Dialog iframe sandbox 恰为 `allow-scripts` 且 srcdoc 为取回文本、截断提示、img 下载链接属性与撤销、code 三分支 toast、未知扩展无卡、预览 404 toast
- [ ] 7.6 新建 `artifacts-panel.tsx`（顶栏 `产物面板` action（package）→ 按当前视图全部已结束步骤 `changes` 按路径聚合（消息靠后优先、同消息 ordinal 大者优先、位置取首次）；为空 → 只 toast `当前任务暂无产物`；否则右侧 `ui/drawer.tsx` Drawer（宽 420，accessible name `产物面板`），每行同文件变更卡行，可派生扩展名附同名操作按钮（行为同 7.5）；关闭后焦点还给按钮）。验证：新建 `web/test/chat-page-artifacts-panel.test.tsx`：空态 toast 不开抽屉、聚合最新优先、行操作复用、焦点归还
- [ ] 7.7 新建 `search-match.ts`（`matchMessages(messages, query)`：空查询无匹配；否则转录顺序中每条消息 `content` 与查询各 `toLowerCase()` 子串判断；步骤/thinking/审批/卡片/错误不参与）+ `conversation-search.tsx`（`role="search"` 名 `对话内搜索`，打开即聚焦；输入 `搜索对话内容`、计数 `i/n`（`aria-live="polite"`，无匹配 `0/0`，无当前 `0/n`）、`上一个`/`下一个`（`n=0` 禁用）/`关闭`；Enter/`下一个` 前进、Shift+Enter/`上一个` 后退、首尾循环；Escape/`关闭`/再点顶栏按钮关闭并清空、焦点还给顶栏按钮；查询变化 → 当前为首条匹配并滚动高亮；消息集合变化 → 重算 `n`，原当前仍匹配则保持并更新 `i` 否则清空当前、不滚动；切换会话/欢迎态/卸载即关闭）+ `scroll-follow.tsx` `FollowTranscript` 增 `handleRef` 暴露 `scrollToMessage(id)`（`[data-message-id]` + `scrollIntoView({block:"center"})`，贴底跟随规则照旧）+ 当前匹配 `<article>` 带 `aria-current="true"` 与类 `chat-msg--search-current`（至多一条）+ 顶栏第二态注入 `对话内搜索` action（search，`aria-expanded` 反映搜索框）。验证：新建 `web/test/search-match.test.ts` 与 `chat-page-search.test.tsx`：大小写/子串/空查询、`1/1`→`0/0`、循环、流式追加时保持当前、跳到非底部后新内容不拽回、关闭清空与焦点归还、切换会话关闭

Suggested fixture level: expanded - 呈现改动按前端通用契约须真实浏览器 + 视口矩阵收口（由组 8 ui-walk 承担），7.0 改 shell 顶栏公共通道
Minimal mergeable slice: 7.0（atomic：图标注册、context 字段与顶栏渲染是同一 `actions` 通道的三段，只加通道不注入时三态 DOM 不变，单独合入保绿）依赖 change A #477（A 的 7.2 先在 `icon.tsx` 注册三个图标，避免同文件冲突）；7.1（atomic：纯函数分区/筛选与侧栏渲染是同一「三分区互斥」不变量的计算与呈现，`session-nav.tsx` 的取代与删除必须与新侧栏同刀否则 knip 报未用或页面双侧栏）依赖 5.1（`pinnedAt`/`workspaceId` 解析）、7.0（`筛选任务` 按钮图标 `filter` 由 7.0 注册）、change A #489（`page.tsx` 拆分先落）；7.2（atomic：菜单三项、两个对话框与 handler 共用同一 `session-actions.ts` 与同一页面级 jsdom fixture）依赖 7.1、7.0、4.2、4.3a、change A #489；7.3（atomic：胶囊、清单与 footer 都是欢迎态首次发送 body 的输入源，任一缺失 body 就不完整）依赖 5.1、4.1、7.0、change A #489；7.4（atomic：解码、归约与折叠块是同一条 `thinking` 数据流的三段，先合解码不渲染会被 knip 标记）依赖 5.1（消息 `thinking` 键）、7.0、change A #489、#480（审批条位次已在 `conversation-view.tsx`）；7.5（atomic：文件变更卡与产物卡共用同一「已结束步骤按路径去重」汇总、同一 `fetchPreview` 拉取纪律与同一卡位插入）依赖 5.1（步骤 `changes` 键）、7.0、7.4（`conversation-view.tsx` 助手块次序按 7.4 → 7.5 串行插入）、change A #480；7.6（atomic：单一 Drawer 组件与其 action）依赖 7.5、7.0；7.7（atomic：匹配、跳转、高亮与顶栏按钮是同一交互闭环，`scrollToMessage` 无调用方会被 knip 标记）依赖 7.0、change A #489

## 8. chat-harness — smoke 与 ui-walk

- [ ] 8.1 新建 `smoke/session-meta.hurl`（独立登录 `zhangsan`；按 `smoke/files.hurl:17-32` 幂等模式创建或采用工作空间 `smoke-sessions`（201|409）；`POST /api/sessions {workspaceId, scene:"code"}` 201 八键、无 body 201 `scene` null、他人/未知 `workspaceId` 404、多余键 400；`PATCH` title/pinned/scene 200 与 `{}`/`workspaceId` 400、他人会话 404；`GET /api/sessions` 反映；`WORKBUDDY_THINK` prompt → 按 A 的 `skip_turn_control=false` 作答 pending 审批 → done → 快照助手 `thinking` 恰为 `先读需求，再列要点，最后作答。` 且 PATCH 设的标题未被 prompt 覆盖 → `DELETE` 204 → `GET` 404 → 再 `DELETE` 404 → 列表不含；可重跑）+ `Makefile:64` smoke 配方在 `smoke/files.hurl` 之后追加 `smoke/session-meta.hurl`（`smoke-live` 不变）+ `scripts/test-ci-harness.sh` :122/:123/:126 精确串改为以 `smoke/files.hurl smoke/session-meta.hurl` 结尾并补「遗漏/前置/加入 smoke-live/第二行」四类拒绝断言 + `AGENTS.md:89` HTTP smoke 证据行四文件改五文件与 oracle :145 同步。验证：`make test-guardrails` 绿、`make smoke` 连跑两遍绿
- [ ] 8.2a 新建 `web/e2e/ui-walk-sessions.spec.ts`（走查一：创建与呈现）+ `web/playwright.config.ts:10` `testMatch` → `"ui-walk*.spec.ts"`、:15 `globalTimeout` 150_000 → 300_000：欢迎页点 `代码开发` → footer `任务启动于` 选工作空间 → 发送 `WORKBUDDY_THINK WORKBUDDY_WRITE 会话走查 <uuid>`（无审批条）→ 侧栏 `空间 › <空间名>` 出现该会话 → 折叠块 `深度思考过程` 文本为 `先读需求，再列要点，最后作答。` 且回合结束后收起 → 文件变更卡一行 `写入` + 逻辑路径 `workbuddy-report.html` → `打开网页预览` Dialog 内 iframe `sandbox` 恰 `allow-scripts` → REST 校验该会话 `scene==="code"`、`workspaceId` 匹配、步骤 `changes[0].kind==="write"`。验证：`make ui-walk` 两个 project（`desktop-light` 1440×900、`mobile-dark` 390×844）全绿，`ui-walk.spec.ts` 照常通过；CI 记录实测总时长
- [ ] 8.2b `ui-walk-sessions.spec.ts`（走查二：组织与删除，接 8.2a 的会话）：`更多操作` → `置顶任务` → 条目移到 `置顶任务`、`空间` 分区不再含它 → 顶栏 `重命名` → `任务名称` 输入 → `已重命名`、面包屑更新、刷新后保持 → 顶栏 `对话内搜索` 输入 `会话走查` → `1/1` 且该消息 `aria-current="true"`；输入不存在文本 → `0/0` → Esc 关闭 → `更多操作` → `删除` → 确认 → toast `任务已删除`、URL 无 `?session=`、欢迎态 hero 可见、REST `GET` 该会话 404。验证：`make ui-walk` 两个 project 全绿

Suggested fixture level: expanded - 真实 HTTP 与真实浏览器证据，CI 既有 smoke/ui-walk job；8.1 触碰受保护 Make 配方、oracle 与 AGENTS.md 证据行需同步
Minimal mergeable slice: 8.1（atomic：hurl 文件、Make 配方、oracle 变异断言与 AGENTS.md 证据行是同一 Hurl 文件的定义端与三处消费端，oracle 精确钉配方与证据行使其必须同 PR）依赖 4.1、4.2、4.3a（done 会话删除为非 running 路径）、3.3（快照 `thinking`）、5.1、6.3（`WORKBUDDY_THINK`）、1.2（models.yml reasoning 声明随服务启动写出）、change A #481（`skip_turn_control` 变量与审批作答前置）；8.2a（atomic：选场景 → 选空间 → 发送 → 侧栏归位 → 折叠块 → 写入卡 → 预览是首次走查的一段连续交互，任何前缀都不构成绑定会话的呈现证据；`testMatch`/`globalTimeout` 改动是让该文件被选中并跑完的前提）依赖 7.1、7.3、7.4、7.5、3.3、3.4、2.2、6.3、change A #492（A 的 ui-walk 步骤全部合入，共享 `ui-walk-layout.ts` 辅助与 gate）；8.2b（atomic：置顶 → 重命名 → 搜索 → 删除作用于 8.2a 创建的同一会话，是一条连续走查）依赖 8.2a、7.2、7.7、4.2、4.3a

## 9. 收尾

- [ ] 9.1 文档：`docs/architecture/system.md` §3.1 `sessions` 行补「空间绑定 cwd / 元数据 PATCH·DELETE / thinking 与 files.changed 事件」一句；`IMPLEMENTATION_PLAN.md:207-216` S1c 节标注 change B 归属与偏差留痕引用（proposal「功能覆盖声明」）；`CONTEXT.md` 增「任务」= 会话在 UI 中的用户可见称谓一条；proposal 中 `查看详情` 落点等 Stage 2 事实核对项已回写，核对无遗漏。验证：`make check` 全绿
- [ ] 9.2 规格同步（design Migration Plan「规格次序」、Open Questions 4）：确认 change A 已归档（`openspec/specs/turn-control/spec.md` 含「从此处分叉 REST」）后，新建 `openspec/changes/s1c-session-metadata-presentation/specs/turn-control/spec.md`，以 MODIFIED 整段重述该 Requirement，把场景中的五键会话视图改为八键并写入继承规则（`workspaceId`/`scene` 继承、`pinnedAt` null）。验证：`openspec validate s1c-session-metadata-presentation --strict --no-interactive` 通过；session-metadata 的 fork 场景与之逐字一致
- [ ] 9.3 Open Questions 关闭（design Open Questions 1、2）：(1) 本机以真实端点（`MODEL_REASONING=on`）跑一次 `make smoke-live` 并读回一条消息快照的 `thinking`：接受且返回 → 维持默认 on；拒绝请求 → 部署设 `off` 并记录；接受但不返回 → 记录「该模型无 reasoning，折叠块不渲染」；结论只改部署配置与 design 本节，凭据只在 shell env。(2) 随 change A #495 的真二进制手工验证结果，把 hashline 模式 edit `details.path` 绝对/相对的结论写回 design Open Questions 2 与 `file-changes.ts` 注释。验证：design Open Questions 1、2 标「已关闭」并注明日期与二进制 SHA256；`make check` 全绿

Suggested fixture level: none - 文档、规格同步与手工验证记录，无运行时行为（9.3 若触发默认值变更，走 1.2 的 fixture）
Minimal mergeable slice: 9.1（atomic：三处文档各一句话，描述的是组 1–8 全部合入后的同一事实，拆开无独立交付价值）依赖组 1–8 全部 task（随末尾 PR 或独立 docs PR）；9.2（atomic：一份 delta 文件与一次 validate）依赖 change A 归档（无 issue 编号；执行者以 `openspec/specs/turn-control/spec.md` 已含该 Requirement 为开工条件）、4.4；9.3（atomic：两项结论各一行写回同一节，拆开无独立交付价值）依赖 1.2、3.3、3.2、change A #495
