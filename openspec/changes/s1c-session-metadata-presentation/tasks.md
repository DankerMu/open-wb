# Tasks: s1c-session-metadata-presentation

> 执行顺序（组级阅读顺序；真实依赖以各组尾部的边为准）：1（schema/配置/错误归属）→ 6（fake-omp / fake-upstream）→ 2（cwd 绑定）→ 5（跨端 DTO 同刀）→ 3（归约与发布）→ 4（元数据 REST 与删除）→ 7（web）→ 8（harness）→ 9（收尾）。组内与跨组的有向边写在每组尾部两行里（Stage 5 宽度门禁输入）；未写出的边即不存在。跨 change 的边一律写成 `change A #<issue>`（A = `s1c-turn-control-governance`，Epic #448），B 的每个 task 只在其列出的 A issue 全部合入后才可开工。
>
> 通用纪律：
> - size-guard（`scripts/size-guard.sh`，单文件 ≤800 行）只扫描 `server/src`、`server/test`、`web/src`、`web/test`、`kbservice/src`、`kbservice/tests` 下的 ts/tsx/py；`web/e2e/**` 与 `scripts/**` 不在扫描范围。受扫描且贴近上限的文件：产品侧 `store.ts` 798、`omp/runtime.ts` 797、`supervisor.ts` 770、`omp/process.ts` 732、`web/src/lib/api.ts` 749、`page.tsx` 734、`stream.ts` 717；测试侧 `omp-process.test.ts` 791、`omp-runtime.test.ts` 790、`app.test.ts` 790、`session-rest.test.ts` 769。A 已对 `runtime.ts`/`store.ts`/`supervisor.ts`/`api.ts`/`page.tsx` 各做过一刀纯搬迁；B **不再拆既有文件**，全部新代码落 design「模块拆分」列出的新模块，既有大文件只加接线（每个 task 的验证都含 `bash scripts/size-guard.sh` 退出 0 + `knip` 零新增）。逐刀预算：7.x 每刀给 `page.tsx` 至多 +10 行（A #489 拆分后的基线行数在首个触及 `page.tsx` 的 B 刀（7.1）开工时记录，每刀 PR 记录合入后行数；7.0 不触及 `page.tsx`）；`supervisor.ts` 的五刀接线（2.2、3.1、3.3、3.4、4.3a）每刀 PR 以 `bash scripts/size-guard.sh` 验证仍 ≤800；`server/test/app.test.ts`（790）只允许改期望值、不增长。`web/e2e/ui-walk.spec.ts`（799 行）不受 size-guard 约束，B 的走查仍落新 spec 文件 `web/e2e/ui-walk-sessions.spec.ts`，理由是 A 的走查已占满该文件的单一旅程、两条旅程分文件便于 `testMatch` 精确选择与独立失败定位。A 已占用的新文件名 B 不复用。
> - 签名演进一律「首刀可选、后刀必填」：2.1 `cwd` 可选（缺省所有者根）、1.2 `reasoning` 可选（缺省 `false`）、4.3a `subscribe` 的 `onEnd` 可选、5.1 `createSession(body?, options?)` 保留 `options.signal`；2.1/1.2/4.3a 三刀对既有调用方与测试无强制改动即保绿（1.2 主动改 `server.ts:272` 传入解析值、4.3a 主动改 `stream/sse.ts` 传入 `onEnd`，均为新增实参）；5.1 因 `createSession` 首参位由 `options` 变为 `body`，须同刀改 `page.tsx:555` 与 `api-sessions.test.ts:79/:135` 的调用形状（见 5.1）。唯一的必填化在 2.2（`cwd`），同刀改下条列出的夹具。
> - 新测试一律写进新建测试文件（`server/test/*.test.ts`、`web/test/*.test.ts(x)`），既有测试文件不增长（仅允许为既有断言改期望值或改调用形状，如 `session-events.test.ts:98-128` 噪声帧列表移出 `thinking_delta`、`core-db-helpers.ts` 回执常量、`fake-omp.test.ts` probe 期望串、`chat-composer.test.tsx:200`/`:226` 中 `session-nav.tsx` 路径与 `.chat-session-nav` 选择器改指新侧栏、`session-store.test.ts:122-127`/`:150`/`:328` 的 `runtimeState` 期望增 `workspaceId`（2.2）、`web/test/api-sessions.test.ts:79`/`:135` 的 `createSession` 调用形状（5.1））。增长例外（逐条封闭，别处不适用）：
>   - `server/test/linux/uid-isolation.test.ts`——CI `uid-isolation` job（`.github/scripts/ci-uid-isolation.sh:225`）只运行该单一文件，4.3b 的「sudo 模式删除会话后文件不存在」用例只能落在此文件；另 2.2 在其 :70-79、:99-108 两处 `SessionRuntime` 选项各补一行 `cwd`。
>   - 2.2 `cwd` 必填化的一行夹具补键：`server/test/omp-process.test.ts:506-516`（`optsOf`，预算 ≤800，现 791）、`server/test/sudo-launcher.test.ts:43-51`、`server/test/support/omp-rpc.ts:68-76`（`tempOpts`，扇出到 `omp-rpc*`/`omp-dispatch*`/`omp-runtime-io`/`session-supervisor-helpers.ts` 等调用方，无需逐个改）、`server/test/omp-runtime.test.ts:620`（经 `...temp` 展开继承 `tempOpts` 的 `cwd`；若类型需要显式赋值则一行，预算 ≤800，现 790）、上条的 `uid-isolation.test.ts` 两处。
>   - 5.1 跨端 DTO 扩键的夹具补键：只允许以共享常量单行展开（server `NULL_SESSION_META` = `{scene: null, workspaceId: null, pinnedAt: null}`、`thinking: null`、`changes: null`；web 同名常量），常量落 5.1 新建的支撑模块，逐文件清单见 5.1；`session-rest.test.ts` 的五键 `toEqual`（:58-64）迁出到 5.1 新建的 `session-snapshot-metadata.test.ts`；`session-rest.test.ts` 因此 −7 行、补键约 +15 行（净约 +8，≤800 由 size-guard 守住），PR 记录前后行数。
>   - 7.1 既有会话页夹具为 `/api/workspaces` 补映射：`web/test/chat-page-support.tsx:17-22` `authenticatedChatRoutes` 增一行默认映射；其余自建 `createFetchMock` 且挂载会话页的文件各一行（清单见 7.1）。
> - 服务端集成测试经 `server/test/session-supervisor-helpers.ts:98` 既有 `spawnImpl` 注入点选择 fake-omp `--scenario` 与子进程工作目录（`edit-write` 场景的路径由 fake 的 `process.cwd()` 拼出，把 cwd 放进/放出绑定空间根即可覆盖归属两侧）。
> - 跨端 DTO 键（会话八键、消息 `thinking`、步骤 `changes`）在 `hasExactlyKeys` 下是破坏性变更，只在 5.1 一刀同 PR 落地（design D11）；新事件类型 `thinking.delta`/`files.changed` 对旧 web 是未知类型、server 可先发。
> - 规格基线：B 的 MODIFIED 重述以 A 的 delta 为底；A 归档后、B 实施前补 `specs/turn-control/spec.md`（9.2）。

## 1. chat-sessions / http-service-skeleton / model-proxy — schema、配置与错误归属

- [ ] 1.1 `server/src/core/db/migrations/035_chat_session_metadata.sql`：按 design D1 五条 `ALTER TABLE … ADD COLUMN`（`chat_sessions.workspace_id TEXT REFERENCES workspaces(id) ON DELETE SET NULL`、`scene TEXT CHECK(...)`、`pinned_at INTEGER CHECK(...)`、`chat_messages.thinking TEXT`、`chat_steps.changes TEXT`），无 DEFAULT/NOT NULL/索引；A 的 034 之后第九条回执。`server/test/core-db-helpers.ts:13-23` 增 `MIGRATION_035` 进 `TRACKED_MIGRATION_FILENAMES`、:70-77 回执表增 `[9, MIGRATION_035]`、`sequenceRows` 为 9；`core-db-chat-schema.test.ts` 顺序断言追加第九条（改期望值）。验证：新建 `server/test/core-db-session-metadata.test.ts`：新库与「带数据的 034 库」（含 fork 子会话、带审批的消息、带 detail/output 的步骤，两个所有者）升级后五列存在且可空、旧行全 NULL、既有列值/行数/外键/级联/唯一约束/索引/`sqlite_sequence` 不变、`PRAGMA foreign_key_check` 为空、重开目录稳定；回执止于 `033` 的库同次运行先 034 后 035；`035` 中途失败（一次性副本中预置冲突对象，如既有 `chat_sessions.scene` 列）→ 无 035 列与回执残留、034 及更早回执与行保留，移除冲突对象后重试恰应用一次；`workspace_id` 写入不存在的工作空间 id 被外键拒绝；删一行 `workspaces` 后引用它的会话 `workspace_id` 变 NULL 而会话与消息仍在；删除账号仍级联其工作空间、会话、消息、步骤与审批；`scene` 取 `chat`/`Office`/`''`、`pinned_at` 取 `-1`/`1.5`/`'x'` 被拒；`thinking`/`changes` 任意文本可写
- [ ] 1.2 `server/src/agent-config.ts` + `server/src/model-proxy/models-yml.ts` + `server/src/server.ts`：`MODEL_REASONING` 第十四个启动配置键（缺省 `on`；只接受精确 `on`/`off`，空串与其它值在任何副作用前启动失败，同 `OMP_IDLE_MS`）；`writeManagedModelsYml(agentDir, {proxyBaseUrl, modelId, reasoning?})` 的 `reasoning` 可选、缺省 `false`（既有 `server/test/model-proxy-models-yml.test.ts` 的 `OPTIONS` 与精确 YAML 断言不改动全绿）；`reasoning === true` 时为唯一模型条目写 `reasoning: true` 与 `compat.reasoningContentField: reasoning_content`，`false`/缺省时两者皆省略，输出确定；`server.ts:272` 重写 models.yml 处传入 `MODEL_REASONING` 的解析值。验证：新建 `server/test/model-proxy-reasoning.test.ts` 断言 on/off 两种 YAML 精确文本、缺省参数与 `false` 输出逐字相同、非法值启动失败（不落任何文件）；`server/test/server-config.test.ts` 既有配置键计数/清单断言改期望值（含 `MODEL_REASONING`）
- [ ] 1.3 `server/src/http/errors.ts`：content-parser 归属集由 URL 集合改为 `method + matched route` 精确身份集合并去掉 :69 的 POST-only 门：A 的十条 + `POST /api/sessions`（POST 归属）+ `PATCH /api/sessions/:id`（PATCH 归属）= 十二条；`DELETE /api/sessions/:id` 不在集合内（带 malformed body 的 DELETE 走既有「非归属已注册路由」语义）。验证：新建 `server/test/http-parser-owners.test.ts`，走 `handleHttpError` + `requestShaped` 接缝（先例 `server/test/auth-request-errors.test.ts:405-436`，不挂载产品路由）：十二条身份逐条以真实 parser 错误（malformed/empty/非 JSON/超限）→ 400 `bad_request`；同路径 `DELETE /api/sessions/:id` 的 parser 错误不被 PATCH 映射覆盖（通用 5xx）；其它方法打到归属路径、`GET` 归属集路由与伪造错误码不映射；错误码表仍十三码。经 production `createApp` 的四类 parser 失败断言归 4.1（POST）与 4.2（PATCH）

Suggested fixture level: expanded - 迁移、启动配置与 content-parser 归属集均为 public API / schema 硬触发项
Minimal mergeable slice: 1.1（atomic：五列同一迁移文件、同一回执号，拆成多条迁移只是多占回执序号且 A 的显式列 fork 插入要分次跟随）依赖 change A #449（034 必须先落账本，035 为第九条）；1.2（atomic：`MODEL_REASONING` 解析与 models.yml 写出是同一布尔值的定义端与消费端，单独合入解析端会留下无消费者的配置键）依赖 change A #451（`agent-config.ts` 同文件，A 先加 `OMP_MAX_PROCESSES`）；1.3（atomic：归属集判定方式的改法与十二条身份是同一个函数、同一测试文件。合入即时行为：`POST /api/sessions` 在 master 已注册（`server/src/sessions/rest.ts:109`，无 `bodyLimit`），本刀起其 malformed body 由通用 5xx 变为 400（预期行为）；16 KiB 超限 → 400 要到 4.1 为该路由设 `bodyLimit` 后才在真实路由上成立；`PATCH /api/sessions/:id` 身份在 4.2 注册路由前不产生行为；故单独合入保绿）依赖 change A #475、#467、#468、#469（A 的四条新路由把归属集扩到十条后 B 再扩到十二条）

## 2. omp-runtime / chat-sessions — 空间绑定 cwd

- [ ] 2.1 `server/src/sessions/omp/process.ts` + `omp/runtime.ts`：`SpawnOmpOpts`/`SessionRuntimeOpts` 增**可选** `cwd`（缺省 `join(sandboxRoot, ownerId)`，行为不变），`process.ts:52` 改用解析后的 cwd 且子进程 `cwd` 选项与 `--cwd` 参数取同一值；`mkdir -p` 只保留给 session-dir/home/agent 与「等于所有者根的 cwd」，其它 cwd 不创建；本刀不改任何调用方与测试夹具。验证：新建 `server/test/omp-spawn-cwd.test.ts` 断言缺省与显式 `cwd` 两种下 argv `--cwd` 与 spawn 选项相等、所有者根缺失时被建出、非所有者根 cwd 缺失时不 mkdir；既有 spawn 契约测试（`omp-process.test.ts`、`sudo-launcher.test.ts`、`omp-runtime*.test.ts`、`omp-rpc*.test.ts`）不改动全绿
- [ ] 2.2 `server/src/app.ts` + `server/src/sessions/index.ts` + `store.ts` + `supervisor.ts`（及 A 的 `turn-control.ts` 惰性获取处）：`cwd` 由可选改为必填，同刀按头部「增长例外」清单给既有夹具各补一行；`createWorkspaceStore` 移到 `registerSessions` 之前；`RegisterSessionsOptions.workspaceRootOf(ownerId, workspaceId)` 由 `app.ts` 以 `rootOf({id: ownerId}, workspaceId)` 包装注入；`store.runtimeState` 返回值增 `workspaceId`，由 supervisor 解析 cwd；`#dispatchNew`、regenerate 惰性获取、fork 临时进程统一计算 `cwd`：未绑定 → 所有者根；绑定 → `workspaceRootOf`：返回 null（不变量破坏：工作空间行已不存在）→ 通用失败（5xx 通用信封，非 502 信封）；返回路径但存在性检查失败（缺失或非目录）→ `agent_unavailable` 502；两支都不调用 `spawnOmp`、不创建目录、不回退所有者根，prompt 走既有受理对补偿。验证：新建 `server/test/session-workspace-cwd.test.ts`（supervisor + 真实 fake-omp）：绑定会话 probe `cwd=` 为空间根、未绑定为所有者根；闲置回收后再 prompt，首次与 resume 两次 spawn 的 argv `--cwd` 逐字相等且 probe `cwd=` 相同；空间根目录被删 → prompt 502 `agent_unavailable` 信封字节、未 spawn、目录未被重建、受理对已移除；`workspaceRootOf` 返回 null（删工作空间行后以注入桩保持绑定）→ 通用 5xx 信封字节、未 spawn、受理对已移除；同文件 createApp 级用例：经 production `createApp` 以 `POST /api/workspaces` 建 W → SQL 直写 `chat_sessions.workspace_id` 绑定（4.1 前无绑定路由）→ prompt → probe `cwd=` 为 W 根。既有断言改期望值：`session-store.test.ts:122-127`/`:150`/`:328` `runtimeState` 增 `workspaceId: null`；装配次序既有断言在 `server/test/server-assembly.test.ts:455-478`（order spy，仅在次序期望变化时改期望值），`server/test/app.test.ts`（790）只允许改期望值不增长

Suggested fixture level: expanded - 改 spawn 契约与 app 装配次序，子进程 cwd 只有真实 fake-omp 能证明
Minimal mergeable slice: 2.1（atomic：`cwd` 参数的定义端与 process.ts 消费端必须同刀，否则 argv 与 spawn 选项来源分裂；参数可选且缺省值等于现行所有者根，调用方与夹具不改，单独合入保绿）依赖 change A #452、#460、#462、#488、#481（`runtime.ts` 拆分、`process.ts` 审批识别、`runtime.ts`/`commands.ts` 的 `command(frame)` 与 `process.ts` argv 改动先落，均为同文件次序边）；2.2（atomic：`workspaceRootOf` 注入、`runtimeState.workspaceId` 与三处 cwd 计算是同一条「绑定 → cwd」数据流，拆开会出现同一会话不同 generation cwd 不一致的中间态；`cwd` 必填化与夹具补键必须同刀否则 typecheck 红）依赖 2.1、1.1（`workspace_id` 列）、6.2（probe `cwd=`）、change A #454、#487（store/supervisor 拆分）、#465、#466（regenerate/fork 的惰性获取与临时进程处）

## 3. chat-stream / thinking-fold / turn-artifacts — 归约与发布（服务端）

- [ ] 3.1 `server/src/sessions/events.ts`：回合内 assistant `message_update.assistantMessageEvent.type==="thinking_delta"` 且 `delta` 非空 → `thinking.delta{messageId, delta}`；`thinking_start`/`thinking_end` 与 `message_end` 内 thinking 块不产生事件；`ChatEvent` 联合增 `thinking.delta` 与 `files.changed{messageId, stepId, files:[{path, added, removed, kind}]}` 两型（`files.changed` 本刀只定义类型不产出）；`supervisor.ts` `persistEvent`（A #487 搬迁后的穷举 switch，无 default）为两型各加显式分支且本刀均返回 `undefined`（不落库、不发布；3.3/3.4 分别替换），使联合扩展后 switch 仍穷举且中间切片不会把 `details` 原始绝对路径发上 SSE；`server/test/session-events.test.ts:98-128` 噪声帧列表移出 `thinking_delta`（改期望值）。验证：新建 `server/test/session-events-thinking.test.ts` 断言回合外/空 delta/start-end 帧不产出、回合内逐帧一条；新建 `server/test/session-persist-new-events.test.ts` 断言 `persistEvent` 对两型返回 `undefined`、ring 无事件、无落库
- [ ] 3.2 新建 `server/src/sessions/file-changes.ts`（纯函数）+ `events.ts` 接线：对已知 running 调用、帧与 `result` 均非 `isError`、`details` 为普通对象的 `tool_execution_end` 提取候选——`edit`：`perFileResults[{path, diff}]` 逐项，否则 `details.path`+`details.diff` 一项，`kind:"edit"`，`added/removed` = 按 `\n` 切分后匹配 `^\+\d+\|`/`^-\d+\|` 的行数；`write`：`details.resolvedPath` → `{kind:"write", added:null, removed:null}`；其它工具（含 `ast_edit`、bash）永不；有候选时在该调用 `step.end` 之前、同一返回结果中紧邻输出 `files.changed{stepId:<toolCallId>, path:<原始路径>}`；`normalizeOutput` 仍丢弃 details。验证：新建 `server/test/file-changes.test.ts`（diff 计数边角：上下文行 ` N|`、无编号 `+`、CRLF、空 diff；perFileResults 优先；isError 不产出；原型键不读；带 `details.path`+`diff` 的 `ast_edit` 结束帧无候选）与 `session-events-files.test.ts`（次序紧邻 `step.end`、bash 与 `ast_edit` 无事件）
- [ ] 3.3 新建 `server/src/sessions/store-thinking.ts`（`appendThinking(messageId, chunk)`：单条 `UPDATE chat_messages SET thinking = COALESCE(thinking,'') || ?`，共享上限 32768 码点、不拆代理对，超限那次只落「填满上限的前缀 + `…（已截断）`」并返回实际落库片段，此后返回空且不写）+ `thinking-buffer.ts`（每助手消息一缓冲，注入 `SessionClock`；累计 2048 UTF-8 字节 / 首段起 2000 ms / 同回合其它事件发布前 / 回合终态前任一时刻整段刷出）+ `supervisor.ts` 接线：刷出投递到 pump 串行提交链，先 `appendThinking` 再在同一同步段发布该片段（中间无 await），落库失败不发布走 owned error-sink；`applyFailure`/`applyStop`/崩溃/优雅关停前刷出。验证：新建模块级 `server/test/thinking-buffer.test.ts`（注入时钟、合成增量，真实 SQLite）：跨过 2048 字节的那段合入后立即刷出一条、首段后时钟推进 2000 ms 刷出、其它事件（`step.start`/`text.delta`）发布前先刷出、刷出后的增量进入新缓冲；合计 40000 码点且截断点落在代理对处 → 列为前 32768 码点（无孤立代理项）+ `…（已截断）`、跨限片段为到限前缀加标记、之后返回空不写；恰 32768 码点全文保存无标记。新建 `server/test/session-thinking.test.ts`（supervisor + fake-omp `thinking` 场景，直接读 `chat_messages.thinking` 列，不经快照投影）：三段小 delta 在首个 `text.delta` 前刷为一条 `thinking.delta`；不含 thinking 帧的场景（A 的 `abort-ok`）→ 列为 null（thinking-fold「真 omp 帧到达」的 null 分支；快照为 null 由 5.1 证明）；列值恰等于已发布片段之和；`--thinking-repeat <n>` 使合计超 32768 码点 → 标记只发一次、此后无该消息的 `thinking.delta`、列值等于已发布之和；`--hold-after-thinking` 挂起时 stop → 缓冲内容以 `thinking.delta` 先于 `turn.end stopped` 发布且已落列；`appendThinking` 注入失败 → 无 `thinking.delta` 发布、ring 序号不推进、错误进 error-sink；带 `thinking.delta` 之前游标的 SSE 重连按序恰重放一次，回合 running 时无游标连接从活跃 `turn.start` 起的回放包含它
- [ ] 3.4 `file-changes.ts` 归属判定（IO）+ 新建 `server/src/sessions/store-changes.ts` 的 `setStepChanges(stepId, json)` + `supervisor.ts` `persistEvent` 分支：会话未绑定 → 丢弃事件不落库不发布；绑定时逐候选：绝对原样/相对拼会话 cwd → realpath（不存在则父目录 realpath + 文件名，两者都失败丢弃）→ 严格位于空间根 realpath + 分隔符之内（根自身/根外/符号链接逃出丢弃）→ 存为相对空间根 `/` 分隔路径，UTF-8 >1024 字节丢弃 → 同步骤同路径合并（计数求和、位置取首次）→ 超 50 项只留前 50；无幸存项返回 `undefined`；有则 `setStepChanges` 提交成功后以数字步骤 id 发布 `files.changed`，随后 `step.end` 照既有路径经 `finishStep`（不读写 `changes`）。验证：新建 `server/test/file-changes-ownership.test.ts`（真实临时目录、合成候选）：同路径两候选合并且 `added/removed` 求和、位置取首次；60 个候选只留前 50；相对路径 UTF-8 超 1024 字节被丢弃；`/etc/passwd`、`../other`、空间根自身、符号链接逃出均被丢弃；不存在文件按父目录 realpath 判定。新建 `server/test/session-file-changes.test.ts`（supervisor + fake-omp `edit-write` 场景）：cwd 在空间根内 → 一条合并的 `thinking.delta` 先于首个 `text.delta`、两条 `files.changed` 先于各自 `step.end`、`chat_steps.changes` 已提交且键恰为 `path/added/removed/kind`；同一回合以早于 `thinking.delta` 的游标断线重连 → thinking/两条 files.changed/step.end 各恰重放一次且顺序不变，重连后 `GET …/messages` 快照的 `thinking` 与两步 `changes` 与事件流一致（chat-stream「Ordered publication after persistence」）；cwd 在根外 → 无事件、列 NULL；未绑定会话 → 无事件；`setStepChanges` 注入失败 → 不发布、ring 序号不推进

Suggested fixture level: expanded - 改纯协议归约与 supervisor 发布次序，事件为 SSE 公共契约，合并时序只有真实子进程能证明
Minimal mergeable slice: 3.1（atomic：thinking 映射与两个事件类型定义同落 `events.ts`，类型先定义才能让 3.2/3.3 的两侧各自编译；server 先发 `thinking.delta` 对旧 web 安全，单独合入保绿）依赖 change A #455、#453（A 的归约改动与联合扩展先落）、#487（`supervisor.ts` 纯搬迁先落，`persistEvent` 以搬迁后位置为准）；3.2（atomic：候选提取与「紧邻 `step.end`」接线是同一纯函数的定义与唯一调用点，次序契约只有接线后才可测；本刀产出的 `files.changed` 在 3.4 落地前被 3.1 加的 `persistEvent` 显式分支丢弃，不发布原始路径）依赖 3.1；3.3（atomic：缓冲刷出、落库与同步段发布是「列值恰等于已发布之和」这一不变量的三个不可分部分）依赖 3.1、1.1（`thinking` 列）、6.1（fake `thinking` 场景及 `--thinking-repeat`/`--hold-after-thinking`）、change A #487、#490（supervisor 拆分与 stop 终态路径）；3.4（atomic：归属判定、`setStepChanges` 与「提交后发布、`step.end` 在后」是同一持久化次序契约）依赖 3.2、2.2（会话 cwd 与空间根）、1.1（`changes` 列）、6.1（fake `edit-write` 场景）、3.3（合并后的 `thinking.delta` 须已发布才能断言其次序与重放）、5.1（重连后快照 `thinking`/`changes` 断言需投影）

## 4. session-metadata — 元数据 REST、删除与 fork 继承（服务端）

- [ ] 4.1 新建 `server/src/sessions/rest-metadata.ts` + 新建 `server/src/sessions/store-metadata.ts` 的 `createSession(owner, {workspaceId?, scene?})`：`POST /api/sessions` 入 parser 归属集（`bodyLimit` 16 KiB）；无 body 与 `{}` → 201 三新键 null；有 body 须为 `{workspaceId?, scene?}` 子集（多余键、`workspaceId` 非字符串或 `null`、`scene` 非三值、数组/`null` 根 → 400）；`workspaceId` 经所有者作用域 `rootOf` 为 null（不存在/他人/形态不合法）→ 三者逐字相同的 404、不写行；插入与 `session.bind` 审计（`title:"绑定工作空间"`，`detail:{sessionId, scene}`）同一事务，未绑定不审计；201 八键 DTO。验证：新建 `server/test/session-metadata-rest.test.ts`（production `createApp` + inject + 真实 SQLite）覆盖 201 三态、400 各形状、经 `createApp` 的四类 parser 失败（malformed JSON/空 body 带 `application/json`/非 JSON media/超 16 KiB）→ 400 `bad_request`、404 三者响应字节相等、审计行与会话行同事务（注入审计失败 → 无会话行）、`GET /api/audit` 对管理员返回该 `session.bind` 事件而非管理员不可见；`smoke/chat.hurl:16` 与 web 既有无 body 创建不受影响（5.1 之后的 `session-rest.test.ts` 不再改动且全绿）
- [ ] 4.2 扩展 `rest-metadata.ts` 加 `PATCH /api/sessions/:id` + 扩展 `store-metadata.ts` 加 `patchSession` + `store.ts` `rollbackPrompt` 标题规则：preParsing `requireOwnedSession`（401/404 先于 body）；body 为 `{title?, scene?, pinned?}` 非空子集（`{}`、多余键含 `workspaceId`、类型不符、`scene:null` → 400，任一不合法整体 400 不写列）；`title` trim 后 1..80 码点；`pinned:true` → `COALESCE(pinned_at, now)`，`false` → NULL；单条所有者作用域 UPDATE 只写所给列、不改 `updated_at`/status/`workspace_id`，影响 0 行 → 404；running 与占用期间均允许；不审计；200 八键。Turn 记录 `titleSetByPrompt`，PATCH 写 title 时把活跃 Turn 的 `titleTouched` 置 true，`rollbackPrompt` 仅当 `titleSetByPrompt && !titleTouched` 恢复 `previousTitle`。验证：`session-metadata-rest.test.ts` 追加 PATCH 用例（本 change 新建文件可增长）：三字段单独/组合、400 全形状、经 `createApp` 的四类 parser 失败 → 400、他人会话 404 且 body 未解析（malformed body 仍 404）、`updatedAt` 不变、已置顶再 `pinned:true` 时刻不变；新建 `server/test/session-title-rollback.test.ts`：未命名会话受理 → PATCH 标题 → 注入派发失败 → 502、status/`updatedAt` 复原而标题仍为 PATCH 值；不 PATCH 时标题回到 null
- [ ] 4.3a 删除的回收原语：`supervisor.ts` 公开 `retire(sessionId)`（有 slot 走既有 `#retireSlot`、丢弃 slot 与事件环；对每个订阅者调用其关闭回调并清空订阅集；无存活 generation 时直接返回、不 spawn 不写不升 epoch）+ `subscribe` 增**可选**第四参 `onEnd`（既有三参调用方不改）+ `stream/sse.ts` 以其文件私有 `endOwned` 闭包作为 `onEnd` 传入（SSE 连接 `raw.end()`，不写任何事件）。本刀不加任何路由。验证：新建 `server/test/session-retire.test.ts`（supervisor + 真实 fake-omp + 真实 SSE 订阅）按 chat-sessions「Public retire for deletion」：idle 会话（空闲子进程存活、两个 SSE 订阅者）`retire` → 子进程退出、其 token 在模型代理处不再认证、进程名额释放、两个订阅者响应结束且无新事件、SQLite 行无变化，之后在该会话 prompt 获取新 generation 且 `stream_epoch` +1；第二次 `retire` 与对无存活 generation 会话的 `retire` 均立即返回、不 spawn、不写、不升 epoch；未传 `onEnd` 的订阅者仅被移出订阅集；既有 `session-supervisor-subscribe.test.ts`、`session-sse.test.ts` 不改动全绿
- [ ] 4.3b 删除（非 running 路径）：新建 `server/src/sessions/session-delete.ts` + 扩展 `store-metadata.ts` 加 `deleteSession` + 扩展 `rest-metadata.ts` 加 `DELETE /api/sessions/:id` + `stream/sse.ts` 在 `subscribe` 前查询 `session-delete.ts` 导出的 `isDeleting(sessionId)`（命中即 `endOwned`、不写事件；不改 `supervisor.ts`）：preParsing 401/404 先于任何 supervisor 调用；格式良好的 body 被忽略；登记 A 的控制占用（已被持有 → 409 `session_busy` 无副作用）并加入删除墓碑集；会话 running → 过渡期 `409 session_busy`（释放占用与墓碑、无任何副作用，4.3c 替换为停止路径）；非 running → 4.3a `retire`（墓碑期内新的 `GET …/events` 在 sse.ts 调用 `subscribe` 之前即被 `isDeleting` 拦下、连接立即结束、不写事件）→ store 单事务（确认无活跃回合/缓冲/步骤内存态，否则视为不变量破坏、通用失败不删行；读出 `omp_session_file` 与消息数 → `DELETE FROM chat_sessions WHERE id=? AND owner_id=?` 级联；fork 子会话 `parent_session_id` 置 NULL → `session.delete` 审计 `{sessionId, ompSessionFile, messageCount}`，审计失败整体回滚）→ 提交后 `unlink` 当前 `omp_session_file`（为 NULL 跳过；ENOENT 视为成功；其它错误经 `onError` 上报仍 204）→ `finally` 释放占用与墓碑 → 204 无 body；之后该 id 的 messages/events/PATCH/DELETE 与未知 id 相同地 404。验证：新建 `server/test/session-delete.test.ts`（supervisor + fake-omp + production `createApp` inject）：未登录 401、他人与未知 id 404 且 supervisor 未被调用（spy）；idle 会话（空闲进程存活）删除后进程退出、两个 SSE 订阅者连接结束且无任何事件、行与文件消失、`GET /api/audit` 对管理员返回 `session.delete` 且 `detail` 恰为 `{sessionId, ompSessionFile, messageCount}`、非管理员不可见、再 DELETE 404、PATCH 404、fork 子会话保留且 parent 为 NULL；`omp_session_file` 为 NULL（从未派发）→ 204；带格式良好 JSON body 的 DELETE → 忽略、204；在 retire 与删行之间注入一次 `GET …/events` → 连接立即结束、无事件；删除事务注入失败 → 通用 5xx、行保留（进程已回收）、占用已释放；审计写入注入失败 → 该窗口内注入的一次 `GET …/events` 仍立即结束且无事件、DELETE 通用 5xx、此后新 `GET …/events` 订阅正常建立（墓碑已解除）；「审计形状」整轮：`{workspaceId, scene:"office"}` 创建 → fake `thinking` 场景 prompt 至 done → DELETE → `GET /api/audit?limit=2` 依次为 `session.delete`（`detail.messageCount` 2）与 `session.bind`，非管理员不可见；production `createApp` 下带 malformed JSON body 的 DELETE → 通用 500、行/审计/进程不变，随后无 body 的 DELETE → 204；占用被他路持有 → 409 无副作用；running 会话 → 过渡期 409、进程/行/订阅不变；unlink 非 ENOENT 失败 → 204 + 错误上报；`server/test/linux/uid-isolation.test.ts` 新增「sudo 模式删除会话后会话文件不存在」用例（Linux CI job 证据；design Open Questions 3）
- [ ] 4.3c 删除（running 路径）：`session-delete.ts` 在已持有占用下置 A 的「停止已在途」标志（并发用户 stop 按 A 自身规则应答，不重复 `abort`、不重复结算），执行与 A stop 相同的停止序列（挂起审批 `deny` 结算 → `abort`，或 prompt 帧未写出时登记停止意图）→ 等待「该回合进入终态（`turn.end` 已落库发布）」或「该次受理被补偿（受理对已移除、停止意图已丢弃）」二者之一 → 按此刻的非 running 状态转入 4.3b 的 retire → 删行 → 删文件；上界由 A 的 `OMP_ABORT_GRACE_MS`/retire 升级与获取失败补偿保证，不另设计时器；第 2–4 步失败 → 通用 5xx、行保留、占用释放；删除 4.3b 的过渡期 running → 409 分支。验证：`session-delete.test.ts` 把过渡期 409 用例替换为：`abort-ok` 场景 running 时 DELETE → 助手与会话先 `stopped` 落库再消失、订阅者见到 `turn.end stopped` 后连接结束；`approval` 场景 pending 审批时 DELETE → 审批 `deny` 结算后删除；`abort-ignored` 场景 → 有界退回 retire 后仍 204，总时长受 A 上界约束；`slow-ready` 场景 prompt 受理后 DELETE 并注入握手失败（`slow-ready` 延迟期间子进程退出或握手超时，A 的获取失败路径 → prompt 502 `agent_unavailable`）→ prompt 502 且受理对已补偿、DELETE 204、之后该 id 404、无残留占用（随后对同 owner 新会话的 prompt 与 DELETE 不 409）；删除期间并发 prompt/regenerate/fork/DELETE 均 409（inject 层），并发 stop 按 A 规则 202/204 且 fake 只收到一次 `abort`
- [ ] 4.4 fork 继承：A 的 `server/src/sessions/store-branch.ts` fork 新会话显式列插入增 `workspace_id`、`scene`（取源会话值），`pinned_at` NULL；分叉点前消息/步骤拷贝语句分别增 `chat_messages.thinking`、`chat_steps.changes`；fork 继承不写 `session.bind` 审计；fork 201 的 `session` 为八键（投影由 5.1 提供）。验证：新建 `server/test/session-fork-metadata.test.ts`：绑定+场景+置顶的源会话 fork → 新会话 `workspaceId/scene` 相同、`pinnedAt` null、无 bind 审计；含 thinking 与 changes 的历史 fork 后逐字相等；fork 会话 prompt 的 probe `cwd=` 为源空间根

Suggested fixture level: expanded - 三条公共 REST 路由、审计事务与进程/订阅回收，均为 public API 与 supervisor 状态机硬触发项
Minimal mergeable slice: 4.1（atomic：POST body 解析、`rootOf` 校验与绑定+审计同事务是同一条创建路径，拆开会留下可绑定但不审计或可解析但不落库的中间态）依赖 1.1、1.3、5.1（201 八键投影）、2.2（`workspaceRootOf` 注入与 `app.ts` 装配次序，供 owner 作用域 `rootOf` 校验）、change A #449；4.2（atomic：PATCH 三字段共用同一校验器与同一 UPDATE，标题补偿规则与 PATCH 写 title 是同一 `titleTouched` 标志的两端）依赖 4.1（`rest-metadata.ts`、`store-metadata.ts` 与 `session-metadata-rest.test.ts` 由 4.1 创建）、1.1、1.3、5.1；4.3a（atomic：`retire` 结束订阅者而不写事件需要 supervisor 侧回调调用与 sse 侧 `endOwned` 闭包同刀，缺一端则订阅者要么不结束、要么被写入事件；由 chat-sessions「Public retire for deletion」独立验证，不依赖任何路由）依赖 change A #487（supervisor 拆分）、#463（池 `onExit` 释放名额）；4.3b（atomic：占用登记、墓碑、删行事务、审计与 unlink 次序是同一不变量「行先于文件消失、占用在每条结束路径释放」；running 的过渡期 409 是无副作用的真实拒绝而非占位，非 running 删除本身即可交付的用户功能）依赖 4.3a、4.1、4.2（文件由 4.1/4.2 创建，PATCH 404 断言需 PATCH 路由）、1.1（审计 `workspaceId`）、6.1（「审计形状」整轮用 fake `thinking` 场景）、change A #467（prompt 受理前占用检查与 regenerate 路由）、#473（控制占用登记 API）；4.3c（atomic：置停止已在途 → 停止序列 → 等终态或补偿 → 转入 4.3b 尾段是一条连续状态机，任何前缀都不是可签收的 running 删除证据）依赖 4.3b、change A #467、#469（prompt/regenerate/fork 路由的并发 409）、#490、#464、#474（停止意图、审批结算与非作答结算）、#456、#458、#461（fake `abort-ok`/`abort-ignored`、`approval`、`slow-ready` 场景）；4.4（atomic：三条显式列语句描述同一次 fork 的同一份历史，少一条即静默丢数据）依赖 1.1、5.1、2.2（fork 临时进程 cwd）、change A #466
Width exception: multi-path - 4.3b 的「sudo 模式删除会话后 `.jsonl` 不存在」用例只能在 CI `uid-isolation` job（Linux、`OMP_USER`）运行，本地 vitest 无法证明跨 uid 删除权限；两条验证路径（本地 `session-delete.test.ts` + CI uid-isolation job）缺一即无法签收 Open Questions 3 的关闭

## 5. chat-sessions / chat-web — 跨端 DTO 同刀

- [ ] 5.1 跨端同刀：server `store.ts` `SESSION_COLUMNS`/`toSessionView` 八键 `{id,title,status,createdAt,updatedAt,scene,workspaceId,pinnedAt}` 作用于列表、创建、消息快照 `session` 与 A 的 fork 201；消息投影增 `thinking: string|null`（用户消息恒 null）；步骤投影增 `changes: Change[]|null`（列 NULL → `null`，非空 JSON → 解析数组）；web `web/src/lib/session-contract.ts` 会话八键严格解析（`scene` ∈ 三值|null、`workspaceId` 为 32 位小写十六进制字符串|null、`pinnedAt` 非负安全整数|null）、消息 `thinking`、步骤 `changes`（元素键恰 `path/added/removed/kind`，`kind` ∈ `edit|write`，计数为非负安全整数或 null）——缺键/多键/值不合法整体视为无效成功响应；`web/src/lib/api-sessions.ts` 签名改为 `createSession(body?, options?)`（`options` 仍为 `ApiRequestOptions`、保留 `signal`；有 body 才发 `application/json`），`web/src/features/chat/page.tsx:555` 调用改为 `createSession(undefined, { signal })`；增 `patchSession(id, patch)`（空对象不发请求、`TypeError` 拒绝）、`deleteSession(id)`（恰 204 成功、200 视为无效）。既有夹具迁移（仅以共享常量单行展开补键，常量定义在新建 `server/test/session-meta-fixtures.ts` 与 `web/test/session-meta-fixtures.ts`）：server `session-rest.test.ts`（:58-64 五键 `toEqual` 迁出到新文件；:100-120、:160-164、:169-208、:468-474、:722-760 会话/消息/步骤对象补键）、`session-snapshot.test.ts`（:118-150）、`session-store.test.ts`（:33-80 `list`/`getMessages` 期望）；web `api-sessions.test.ts`（另 :79、:135 调用改为 `createSession(undefined, options)`）、`chat-page-ownership-support.ts`、`chat-stream-support.ts`、`chat-stream.test.ts`、`chat-stream-connection.test.ts`、`chat-page-lifecycle.test.tsx`、`chat-page-ownership.test.tsx`、`chat-page-ownership-gaps.test.tsx`、`chat-page.test.tsx`、`chat-steps.test.tsx`、`chat-messages.test.tsx`、`chat-copy.test.tsx`、`chat-composer.test.tsx`（:159）、`sidebar.test.tsx`（:108）；清单以 5.1 PR 的 `make typecheck` + `make test` 为准，清单外若有文件需补键同样只允许共享常量单行展开并在 PR 中列出。验证：server 新建 `server/test/session-snapshot-metadata.test.ts`：四处投影形状（列表、创建、快照 `session`、fork 201，含迁入的五键→八键 `toEqual`）、NULL/非 NULL `changes`、快照消息 `thinking` 恰等于 `chat_messages.thinking` 列值；web `step.end` 事件 payload 若携带 `changes` 键 → 视为形状不符触发重同步（chat-web「步骤 args 与输出分栏」，落 `session-contract-metadata.test.ts`）；web 新建 `web/test/session-contract-metadata.test.ts` 与 `api-sessions-metadata.test.ts` 断言解析三键、拒绝旧五键/多键/非 32 位小写十六进制 `workspaceId`、三方法请求形状、`createSession(undefined, {signal})` 可取消、204/200 分支；`bash scripts/size-guard.sh` 退出 0 且 PR 记录 `session-rest.test.ts` 5.1 前后行数（预期净约 +8，≤800）

Suggested fixture level: expanded - 公共 REST 快照形状与跨端严格解析同刀，public API 硬触发项
Minimal mergeable slice: 5.1（atomic：`hasExactlyKeys` 严格键集使 server 投影与 web 解析任一侧先合入都让会话页整体失效——server 先加键 → 旧 web 判整个响应非法；web 先要求键 → 旧 server 响应非法——两侧无法各自保绿，见组尾 Width exception）依赖 1.1（三列存在）、change A #476（A 的 5.3 同刀先落，消息键集含 `approvals`）、#472（`stopped` 联合与 `api-sessions.ts` 四方法）、#469（fork 201 路由存在才有其投影可改）、#471（`api-sessions.ts` 由 A 拆出）
Width exception: multi-path - 5.1 `hasExactlyKeys` 严格键集使 server/web 任一侧先合入都会让会话页整体失效，两侧无法各自保绿，故 server 投影与 web 解析同一 PR、两条验证路径（server inject 与 web 单测）

## 6. omp-test-harness — fake-omp / fake-upstream

- [ ] 6.1 `server/test/support/fake-omp.mjs` 新 scenario `thinking`（`agent_start` 后 `thinking_start{contentIndex:0}`、恰三段 `thinking_delta`（`先读需求，`/`再列要点，`/`最后作答。`）、`thinking_end`、≥3 段正文 delta、content 含 thinking 块与文本块的 `message_end{stop}`、`agent_end`；任何 `--approval-mode` 下无工具帧与 select；可选 argv `--thinking-repeat <n>`（正整数，缺省 1）把三段 delta 按序重复 n 次，足够次数使合计超过 32768 码点；可选 `--hold-after-thinking` 在 `thinking_end` 之后像 `abort-ok` 一样挂起直到收到 `abort`，随后以 aborted 尾部结束）与 `edit-write`（`agent_start` 后先发与 `thinking` 相同的 `thinking_start`/三段 `thinking_delta`/`thinking_end`，再一个双工具 `message_end{toolUse}`；`edit` args 恰 `{input}`、end 帧 `details:{path:"<cwd>/notes.md", diff:"+1|a\n+2|b\n-3|c\n 4|d"}`；`write` args `{path:"out/report.html", content}`、end 帧 `details:{resolvedPath:"<cwd>/out/report.html"}` 无 diff；工具结束后再发至少两段正文 delta、`message_end{stop}`、`agent_end`（工具前无正文）；`<cwd>` 为 fake 的 `process.cwd()`，每个 end 帧前真实写出所报告文件（建 `out/`）；无审批）。验证：新建 `server/test/fake-omp-metadata-scenarios.test.ts`（真实子进程）断言两场景帧序列、thinking 拼接常量、`--thinking-repeat 3` 恰九段且顺序循环、非法 repeat 值启动失败、`--hold-after-thinking` 在 `abort` 前无后续帧且收到后以 aborted 尾部结束、文件真实存在、`yolo`/`write` 下均无 select
- [ ] 6.2 probe 尾部 `frames=` 之后追加 ` cwd=<process.cwd()>`（最后一个字段）；`server/test/fake-omp.test.ts` `expectedProbeReport` 精确串与 `server/test/linux/uid-isolation.test.ts` `REPORT_LABELS` 以 `"frames", "cwd"` 结尾同 PR 更新（改期望值）；`server/test/fake-omp-helpers.ts` `startFake` 增可选 `cwd`（支撑文件，非测试文件）。验证：新建 `server/test/fake-omp-probe-cwd.test.ts`——在含空格的临时目录下起 fake，probe 末字段 `cwd=` 为该目录且 `parseLabeledReport` 切分正确（omp-test-harness「probe 报告带 cwd 字段」）；本地 fake-omp 测试绿 + CI uid-isolation job 绿的证据
- [ ] 6.3 `server/test/support/fake-upstream.mjs` 两个 prompt 文本标记（与 `WORKBUDDY_UI_WALK:<uuid>` 同一「最后 user 文本子串」查找，可组合）：`WORKBUDDY_THINK` → 作答轮在不变的正文分片前先发三个 `reasoning_content` 分片（拼接恰为 `先读需求，再列要点，最后作答。`）；`WORKBUDDY_WRITE` → 工具轮（`toolFrames` :366 旁新增分支）改发 `write {"path":"workbuddy-report.html", …}` 而非 bash；无标记请求逐字节不变。验证：新建 `server/test/fake-upstream-markers.test.ts` 断言三种组合的 SSE 分片序列与无标记请求字节不变；既有 `fake-upstream.test.ts`/`fake-upstream-gates.test.ts` 不改动全绿

Suggested fixture level: compact - 测试支撑脚本 + 自带契约测试，无生产代码、无共享入口
Minimal mergeable slice: 6.1（atomic：两个 scenario 与 `thinking` 的两个 argv 旋钮共享同一份新契约测试文件与「场景计数八→十」的同一处 spec 计数，拆成多刀只是重复起进程）依赖 change A #470、#456、#457、#458、#461（仅为计数前提：A 的八个场景全部合入后计数才为十；scenario 分支互不共享状态，与这些 A 场景无功能依赖）；6.2（atomic：probe 字符串精确相等使 fake 输出与两处解析期望值必须同 PR）依赖 change A #459（`frames=` 先落，`cwd=` 在其后）；6.3（atomic：两个标记共用同一子串查找与同一契约测试文件，且 ui-walk 以二者组合驱动）无前置依赖（无标记路径逐字节不变）
Width exception: multi-path - 6.2 的 probe 期望串同时被本地 `fake-omp.test.ts` 与只在 CI Linux job 运行的 `uid-isolation.test.ts` 消费，两处解析同 PR 才能保证 `cwd=` 切分一致；两条验证路径（本地 fake-omp 测试 + CI uid-isolation job）

## 7. chat-web / session-sidebar / spa-shell / thinking-fold / turn-artifacts / conversation-search — web

- [ ] 7.0 `web/src/ui/icon.tsx` 只注册 master 尚缺的十个：`star`、`pencil`、`trash`、`more-horizontal`、`package`、`download`、`globe`、`palette`、`chevron-up`、`filter`（`folder`/`search`/`code`/`image`/`file-code`/`file-text`/`message-square`/`file-spreadsheet`/`layout-grid`/`chevron-right` 已在 master，A 加 `square`/`refresh-cw`/`git-branch`；重复登记即 TS1117）；`web/src/lib/topbar.tsx` context 增 `actions: readonly TopbarAction[]`（描述符 `{key, label, icon, expanded?, onSelect(trigger)}`），`useTopbar({breadcrumb, actions})` layout effect 上报、Provider 仅在 `key/label/icon/expanded` 浅比较变化时 setState、`onSelect` 经 ref 读最新闭包、卸载/未提供时清空；`routes/shell/topbar.tsx` 在 h1 之后渲染 `.topbar-actions`（ghost icon Button，accessible name 与 Tooltip 为 `label`，`expanded` 定义时带 `aria-expanded`；h1 名不含按钮）；无 actions 时三态 DOM 不变。验证：新建 `web/test/topbar-actions.test.tsx`：注入/清空/顺序/`aria-expanded`、每次渲染新回调不触发重复上报、无 actions 时与既有快照一致；既有 shell 测试不改动全绿
- [ ] 7.1 新建 `web/src/features/chat/session-groups.ts`（纯函数：筛选 状态 全部/进行中=`running`/已完成=`done|failed|stopped` × 时间 全部时间/今天=本地日历日/更早，取交集；分区 置顶 > 空间（按 `workspaceId` 分子组、顺序为工作空间列表顺序、不在列表 → 末位 `未知空间`）> 任务，互斥、保持服务端顺序）+ `session-sidebar.tsx`（取代并删除 `session-nav.tsx`；分区 `置顶任务`（无计数）/`任务 (n)`/`空间 (n)` 及子组为带名 `role="group"`，空分区不渲染，无任何会话 → `没有匹配的任务`；保留选择按钮、`新会话` 标题回退与状态元素；`新建会话` 名称不变）+ `session-filter.tsx`（`筛选任务` Popover，两个 `role="radiogroup"`，选择即生效、弹层保持打开，Escape 关闭且焦点回到 `筛选任务`，状态只在页面内存）；`ChatPage.refreshList` 并行拉取会话与工作空间列表，工作空间读取失败只影响归组（全部落 `未知空间`）、会话列表照常渲染。须保留的既有 DOM 钩子：`nav[aria-label="会话列表"]` 内含 `新建会话` 且位于主导航与 footer 之间（`web/test/sidebar.test.tsx:149-166`、`web/test/app-shell-responsive.test.tsx:341-373`、`web/e2e/ui-walk-layout.ts:272-296`）、`button.chat-session-button` 与 `aria-current`（`ui-walk-layout.ts:301-313`）、`.chat-new-session`（`sidebar.test.tsx:161`）、加载文案 `正在读取会话`（`app-shell-responsive.test.tsx:382`）。既有测试改引用不增长：`chat-composer.test.tsx:200` 读取的源文件路径改为 `session-sidebar.tsx`、`:226` 的 `.chat-session-nav` 选择器随 nav 类名改名同步。既有夹具补 `/api/workspaces` 映射（`createFetchMock` 对未映射路径抛 `unexpected request`，`web/test/support.ts:79-92`）：`web/test/chat-page-support.tsx:17-22` `authenticatedChatRoutes` 增一行默认 `{workspaces: []}`（覆盖所有经 `renderChatPage` 的用例）；自建 `createFetchMock` 且挂载会话页的 `chat-page-lifecycle-support.tsx`、`sidebar.test.tsx`、`app-shell-responsive.test.tsx`、`topbar.test.tsx`、`settings-support.tsx` 各一行映射或改用 `support.ts:102` 既有 `allowWorkspaceListFetch()`；断言精确 fetch 调用序列的既有用例改期望值。验证：新建 `web/test/session-groups.test.ts`（互斥、计数、`未知空间`、今天/更早跨午夜边界、`idle` 只在全部）与 `chat-page-sidebar.test.tsx`（三分区 DOM、筛选交集、空态、Escape 关闭与焦点归还、工作空间读取失败 → 列表照常且归 `未知空间`、上述 DOM 钩子仍在）；`make ui-walk` 两个 project（`desktop-light`、`mobile-dark`）全绿
- [ ] 7.2a 新建 `web/src/features/chat/session-menu.tsx`（行尾 `更多操作：<显示标题>` 按钮打开 Menu，本刀两项：`重命名`/`置顶任务`|`取消置顶`，任何状态可用）+ `rename-dialog.tsx`（标题 `重命名任务`、输入 `任务名称`、trim 空时 `保存` 禁用、Enter/保存 → PATCH → 更新列表与面包屑、toast `已重命名`；失败保持打开并 `role="alert"` 显示信封 message）+ `session-actions.ts` 的 rename/pin handler（置顶/取消 → PATCH → toast `已更新置顶状态`）+ 新建 `web/src/features/chat/topbar-actions.ts`（有序常量 `CHAT_TOPBAR_ACTIONS`，固定槽位次序 `重命名` → `对话内搜索` → `产物面板`；页面只从该常量按序构造 `useTopbar` 的 actions，未实现的槽位不产出按钮）+ 会话页顶栏第二态填入 `重命名` 槽（pencil，打开同一 Dialog）。验证：新建 `web/test/chat-page-session-rename-pin.test.tsx`：菜单两项、行菜单与顶栏两入口的重命名成功/失败、置顶往返与分区迁移、欢迎态无顶栏按钮
- [ ] 7.2b `session-menu.tsx` 增 `删除`（danger）项 + `session-actions.ts` 删除 handler：ConfirmDialog 标题 `删除任务`、说明 `确定要删除「<显示标题>」吗？删除后不可恢复。`、确认 `删除` 请求中忙碌；204 → toast `任务已删除`、移出列表，若为当前会话则关闭事件流、以 replace 移除 `?session=`（保留其它 search/hash）回欢迎态；失败 → 关闭确认框、toast 信封 message、重新读取列表；409 `session_busy` 显示其信封文案。验证：新建 `web/test/chat-page-session-delete.test.tsx`：菜单三项、删除当前/非当前会话（URL 变化、事件流关闭、toast）、running 会话删除、409 文案与列表重读
- [ ] 7.3 新建 `scene-pills.tsx`（hero 与快捷任务行之间 `场景` 组：`日常办公`/`代码开发`/`创意设计`，`aria-pressed`，默认日常办公；切换换快捷任务清单并 toast `已切换到「<场景名>」场景`，重复点击无变化）+ `welcome-content.ts` 增三组场景清单（demo:1221-1239；图标只用已注册键：日常办公沿用 master `WELCOME_QUICK_PROMPTS`（`welcome-content.ts:8-17`）既有图标 `file-text`/`zap`/`file-spreadsheet`/`folder`/`file-chart-line` 不变，代码开发 `code`/`file-code`/`layout-grid`，创意设计 `palette`/`image`/`file-text`）+ `composer-footer.tsx`（仅欢迎态 composer 卡片内工具栏之后：`任务启动于 <空间名>|未选择`（folder）→ Popover：`搜索工作空间` 大小写不敏感过滤、恒有 `未选择`、各空间「名称 + `<account>/<dir>`」按返回顺序（消费 7.1 引入的工作空间列表状态，不另拉取）、无匹配 `没有匹配的工作空间`、读取中 `正在读取工作空间`；不渲染权限元素/新建/挂载）；欢迎态首次发送与侧栏 `新建会话` 均以 `createSession({scene, workspaceId?}, {signal})` 即 `POST /api/sessions {scene, workspaceId?}` 创建（场景与空间为页面内存状态）。验证：新建 `web/test/chat-page-welcome-scene.test.tsx`：胶囊切换与清单、toast、footer 过滤/选择/未选择、首次发送与 `新建会话` 的请求 body、会话页不渲染 footer 与胶囊
- [ ] 7.4 新建 `stream-thinking.ts`（`thinking.delta` 严格解码与归约：按 `messageId` 追加到消息 `thinking`，未知消息忽略）+ `thinking-block.tsx`（`<details class="thinking-block">`，summary `深度思考过程` 前置 `chevron-right`，主体 `pre-wrap` 纯文本不经 Markdown；消息 running 展开、进入 `done|failed|stopped` 收起、快照终态为收起、两次迁移间手动开合保留；null/空串不渲染；不参与 `复制`）+ `conversation-view.tsx` 助手块插入折叠块位（位于审批条之前）+ `stream.ts` `DATA_EVENTS` 增 `thinking.delta` 接线。验证：新建 `web/test/chat-thinking.test.tsx`：解码拒绝形状不符、归约追加、`turn.start` 重置新助手消息 `thinking` 为 null、开合三态迁移、null 不渲染、32K 标记原样显示、`复制` 不含 thinking
- [ ] 7.5a 新建 `stream-artifacts.ts`（`files.changed` 严格解码与归约：按 `stepId` 设置步骤 `changes`，后到覆盖）+ `file-changes-card.tsx`（一条助手消息只汇总**已结束**步骤的 `changes`，按路径去重取 ordinal 最大者、位置取首次；卡头 `文件变更（N 个）`；行 `+a`/`-d`（>0 才显）或 `写入` + 逻辑路径 `<account>/<dir>/<path>`（`dir` 取该会话 `workspaceId` 在 7.1 工作空间列表中的 `dir`）+ `查看详情 <逻辑路径>` 按钮 → `/files?ws=<workspaceId>`；空间不可解析时只显相对路径、无 `查看详情`）+ `conversation-view.tsx` 助手块插入文件变更卡位（错误之后、`已停止` 徽章之前）+ `stream.ts` `DATA_EVENTS` 增 `files.changed`。验证：新建 `web/test/chat-page-file-changes.test.tsx`：`files.changed` 严格解码（`files: []`、未知 `kind`、缺键 → 触发既有重同步路径）、未知 `stepId` 归约返回同一引用、running 步骤不出卡、`step.end` 后出卡、去重取最新、`+a/-d`/`写入`、`查看详情` 导航 URL、空间不可解析时只显相对路径；助手块全序断言：思考块 → 审批条 → 正文 → 步骤卡 → 错误 → 文件变更卡 → `已停止` 徽章 → 操作行，`复制` 只含正文
- [ ] 7.5b 新建 `artifact-card.tsx`（同一「已结束步骤按路径去重」汇总与顺序，按扩展名派生：`html` → `globe`/`HTML`/`打开网页预览`/卡脚 `可交互预览` → 点击时 `fetchPreview` → Dialog（标题文件名）内 `<iframe sandbox="allow-scripts" srcdoc title>`，截断时 iframe 上方 `文件超过 1 MiB，仅预览前 1 MiB`；`png`/`jpg|jpeg` → `image`/`PNG`|`JPG`/`下载` → Blob URL 临时链接下载后撤销；`md/txt/log/csv/json/js/ts/tsx` → `file-code`/大写扩展名/`复制代码` → 剪贴板 → toast `已复制到剪贴板`|`复制失败`，截断 → 不写剪贴板 toast `文件过大，无法复制`；其它扩展名不派生；空间不可解析时不渲染；拉取中按钮禁用不并发、卸载/切会话 abort 并撤销 Blob URL；预览失败 toast 信封 message 不开 Dialog）+ `conversation-view.tsx` 在文件变更卡之后、`已停止` 徽章之前插入产物卡位。验证：新建 `web/test/chat-page-artifact-card.test.tsx`：html Dialog iframe sandbox 恰为 `allow-scripts` 且 srcdoc 为取回文本、截断提示、图片下载链接属性与撤销、code 三分支 toast、未知扩展无卡、空间不可解析无卡、拉取中禁用与卸载 abort、预览 404 toast；全序断言在 7.5a 基础上补产物卡位于文件变更卡与 `已停止` 徽章之间
- [ ] 7.6 新建 `artifacts-panel.tsx`（填入 `CHAT_TOPBAR_ACTIONS` 的 `产物面板` 槽（package）→ 按当前视图全部已结束步骤 `changes` 按路径聚合（消息靠后优先、同消息 ordinal 大者优先、位置取首次）；为空 → 只 toast `当前任务暂无产物`；否则右侧 `ui/drawer.tsx` Drawer（宽 420，accessible name `产物面板`），每行同文件变更卡行，可派生扩展名附同名操作按钮（行为同 7.5b）；关闭后焦点还给按钮）。验证：新建 `web/test/chat-page-artifacts-panel.test.tsx`：空态 toast 不开抽屉、聚合最新优先、抽屉打开期间新的 `step.end` 使列表随之更新、Escape 与遮罩点击关闭、行操作复用、焦点归还
- [ ] 7.7 新建 `search-match.ts`（`matchMessages(messages, query)`：空查询无匹配；否则转录顺序中每条消息 `content` 与查询各 `toLowerCase()` 子串判断；步骤/thinking/审批/卡片/错误不参与）+ `conversation-search.tsx`（`role="search"` 名 `对话内搜索`，打开即聚焦；输入 `搜索对话内容`、计数 `i/n`（`aria-live="polite"`，无匹配 `0/0`，无当前 `0/n`）、`上一个`（`chevron-up`）/`下一个`（`chevron-down`）（`n=0` 禁用）/`关闭`；Enter/`下一个` 前进、Shift+Enter/`上一个` 后退、首尾循环；Escape/`关闭`/再点顶栏按钮关闭并清空、焦点还给顶栏按钮；查询变化 → 当前为首条匹配并滚动高亮；消息集合变化 → 重算 `n`，原当前仍匹配则保持并更新 `i` 否则清空当前、不滚动；切换会话/欢迎态/卸载即关闭）+ `scroll-follow.tsx` `FollowTranscript` 增 `handleRef` 暴露 `scrollToMessage(id)`（`[data-message-id]` + `scrollIntoView({block:"center"})`，贴底跟随规则照旧）+ 当前匹配 `<article>` 带 `aria-current="true"` 与类 `chat-msg--search-current`（至多一条）+ 填入 `CHAT_TOPBAR_ACTIONS` 的 `对话内搜索` 槽（search，`aria-expanded` 反映搜索框）。验证：新建 `web/test/search-match.test.ts` 与 `chat-page-search.test.tsx`：大小写/子串/空查询、`1/1`→`0/0`、循环、流式追加时保持当前、跳到非底部后新内容不拽回、关闭清空与焦点归还、切换会话关闭；本刀为最后一个顶栏槽位，同文件断言 banner 内 heading 之后恰三个按钮且 DOM 次序为 `重命名` → `对话内搜索` → `产物面板`、欢迎态无任何顶栏按钮

Suggested fixture level: expanded - 呈现改动按前端通用契约须真实浏览器 + 视口矩阵收口（由组 8 ui-walk 承担），7.0 改 shell 顶栏公共通道
Minimal mergeable slice: 7.0（atomic：图标注册、context 字段与顶栏渲染是同一 `actions` 通道的三段，只加通道不注入时三态 DOM 不变，单独合入保绿）依赖 change A #477（A 的 7.2 先在 `icon.tsx` 注册三个图标，避免同文件冲突）；7.1（atomic：纯函数分区/筛选与侧栏渲染是同一「三分区互斥」不变量的计算与呈现，`session-nav.tsx` 的取代与删除必须与新侧栏同刀否则页面双侧栏）依赖 5.1（`pinnedAt`/`workspaceId` 解析）、7.0（`筛选任务` 按钮图标 `filter` 由 7.0 注册）、change A #489（`page.tsx` 拆分先落）；7.2a（atomic：菜单两项、重命名对话框、handler 与顶栏 `重命名` 槽共用同一 `session-actions.ts` 与同一 PATCH 客户端，`CHAT_TOPBAR_ACTIONS` 须随首个顶栏按钮同刀创建）依赖 7.1、7.0、4.2、change A #489；7.2b（atomic：菜单 `删除` 项、确认框与删除当前会话后的事件流关闭/URL replace 是同一删除交互闭环）依赖 7.2a、4.3c（任何状态可删须 running 路径已落）；7.3（atomic：胶囊、清单与 footer 都是欢迎态首次发送 body 的输入源，任一缺失 body 就不完整）依赖 5.1、4.1、7.0、7.1（工作空间列表状态）、change A #489；7.4（atomic：解码、归约与折叠块是同一条 `thinking` 数据流的三段，折叠块的开合迁移读归约后的消息状态，单独合入解码端没有用户可见行为可签收）依赖 5.1（消息 `thinking` 键）、7.0、change A #489、#480（审批条位次已在 `conversation-view.tsx`）；7.5a（atomic：`files.changed` 解码归约、文件变更卡与其卡位是同一条 `changes` 数据流的解码与呈现，全序断言须在卡位插入的同刀）依赖 5.1（步骤 `changes` 键）、7.0、7.1（工作空间列表供 `dir`/`未知空间` 判定）、7.4（`conversation-view.tsx` 助手块次序按 7.4 → 7.5a 串行插入）、change A #480；7.5b（atomic：产物卡的三类派生共用同一 `fetchPreview` 拉取纪律、abort 与 Blob 撤销）依赖 7.5a；7.6（atomic：单一 Drawer 组件与其顶栏槽）依赖 7.5b、7.2a（`CHAT_TOPBAR_ACTIONS` 由 7.2a 创建）、7.0；7.7（atomic：匹配、跳转、高亮与顶栏按钮是同一交互闭环；作为最后一个顶栏槽位承担三按钮全序断言）依赖 7.0、7.2a、7.6、change A #489

## 8. chat-harness — smoke 与 ui-walk

- [ ] 8.1 新建 `smoke/session-meta.hurl`（按 chat-harness「会话元数据 HTTP 冒烟」步骤：独立登录 `zhangsan`；按 `smoke/files.hurl:17-32` 幂等模式创建或采用工作空间 `smoke-sessions`（201|409）；`POST /api/sessions {workspaceId, scene:"code"}` 201 八键与 `session.bind` 审计、无 body 201 `scene` null、他人/未知 `workspaceId` 404、多余键 400；`PATCH` title/pinned/scene 200 与多余键 400、`GET /api/sessions` 反映、取消置顶；`WORKBUDDY_THINK` prompt → 与 `make smoke` 下 `chat.hurl` 相同地轮询并作答唯一的 bash 审批 `allow`（不使用 `skip_turn_control`）→ done → 快照助手 `thinking` 恰为 `先读需求，再列要点，最后作答。` 且 PATCH 设的标题未被 prompt 覆盖 → `DELETE` 204 + `session.delete` 审计 → messages `GET` 404 → 列表不含 → 再 `DELETE` 404；`lisi` 对他人会话 PATCH/DELETE 404；可重跑）+ `Makefile:64` smoke 配方在 `smoke/files.hurl` 之后追加 `smoke/session-meta.hurl`（`smoke-live` 不变）+ `AGENTS.md:89` HTTP smoke 证据行四文件改五文件 + `scripts/test-ci-harness.sh` 受保护 oracle 同步（按函数名与变异标签锚定，不按行号——A #481 已改写同一配方与 oracle，行号必然漂移）：`contract()` 基线中的 `recipes("smoke", [...])` 配方串与 HTTP smoke 的 `one(matrix_rows, "| HTTP smoke | …")` 证据行；`contract Make smoke … mutation` 族中查找/替换串含配方文件列表者（当前为 `min-bash-steps`、`four-file order`、`drop files.hurl`、`chat file`、`whitespace duplicate`、`protected-first multi-target`、`export after target`）及 `contract Make Hurl connection mutation`、`contract Make recipe comment mutation`、`contract Make conditional mutation`；AGENTS 族 `contract AGENTS stale HTTP evidence mutation`、`contract AGENTS matrix wrap comment mutation`、`contract AGENTS matrix wrap fence mutation`、`contract AGENTS matrix comment mutation`、`contract AGENTS matrix fence mutation`、`contract AGENTS matrix HTML block mutation`、`contract AGENTS smoke command mutation`、`contract AGENTS smoke evidence mutation`、`contract AGENTS smoke matrix peer-section-move mutation`；兜底规则：实施时脚本内每个查找/替换串含 `smoke/files.hurl` 或 `四文件` 的 `cm` 变异都须同步（变异辅助 `ed` 在查找串计数 ≠1 时 `exit 2` 记 FAIL）；新增四条拒绝变异：配方遗漏 `smoke/session-meta.hurl`、置于 `smoke/files.hurl` 之前、加入 `smoke-live` 配方、作为配方第二行单独调用。验证：`make test-guardrails` 绿（四条新变异各自先 FAIL 于缺失断言、后 PASS 的记录）、`make smoke` 连跑两遍绿
- [ ] 8.2a 新建 `web/e2e/ui-walk-sessions.spec.ts`（走查一：按 chat-harness「UI 走查会话元数据」第 1–6 步）+ `web/playwright.config.ts:10` `testMatch` → `"ui-walk*.spec.ts"`、:15 `globalTimeout` 150_000 → 300_000：UI 登录 `zhangsan` 并经页面请求上下文确保工作空间 `ui-walk-sessions`（201|409）→ 欢迎页点 `代码开发`（`aria-pressed` 与快捷任务切换）→ footer `任务启动于` 选该工作空间 → 发送 `WORKBUDDY_THINK WORKBUDDY_WRITE 会话走查 <uuid>`（每 project 新 UUID），以 Playwright 请求拦截观测（不 fulfill）`POST /api/sessions` 请求体深等于 `{"workspaceId":<该空间 id>,"scene":"code"}`、无审批条、`write` 步骤 `已完成` → 折叠块 `深度思考过程` 收起、展开后文本恰为 `先读需求，再列要点，最后作答。` → 文件变更卡 `文件变更（1 个）` 一行 `写入` + 逻辑路径 `zhangsan/ui-walk-sessions/workbuddy-report.html`，`查看详情` 导航到 `/files?ws=<空间 id>` → `HTML` 产物卡 `打开网页预览` Dialog 内 iframe `sandbox` 恰 `allow-scripts` 且文档含标题 `WorkBuddy` → 顶栏 `产物面板` 打开抽屉列出该文件后关闭 → 侧栏该会话恰一次出现在 `空间` 分区的 `ui-walk-sessions` 子组（`mobile-dark` 经侧栏浮层）；整段旅程包在 `try/finally` 中，`finally` 经页面请求上下文 `DELETE /api/sessions/<id>`（204|404 均接受）清理。验证：`make ui-walk` 两个 project（`desktop-light` 1440×900、`mobile-dark` 390×844）全绿，`ui-walk.spec.ts` 照常通过；CI 记录实测总时长
- [ ] 8.2b `ui-walk-sessions.spec.ts`（走查二：按第 7–10 步，接 8.2a 的同一会话、同一 `try` 块内）：行菜单 `更多操作` → `置顶任务` → 条目移到 `置顶任务`、`空间` 分区不再含它、菜单改为 `取消置顶` → `更多操作` → `重命名` → `重命名任务` 对话框 `任务名称` 输入 → `保存` → 侧栏条目与顶栏标题更新、刷新后标题与置顶分区保持 → 顶栏 `对话内搜索` → `搜索对话内容` 输入该 UUID → `1/1`、该用户消息滚入视图且 `aria-current="true"`；输入不存在文本 → `0/0` → Esc 关闭并清除高亮 → `更多操作` → `删除` → `删除任务` 确认文案 → 确认 → toast `任务已删除`、条目从所有分区消失、URL 无 `?session=`、欢迎态可见、页面请求上下文 `GET /api/sessions/<id>/messages` 404（顶栏 `重命名` 入口由 7.2a 的 jsdom 覆盖，不进走查）。验证：`make ui-walk` 两个 project 全绿

Suggested fixture level: expanded - 真实 HTTP 与真实浏览器证据，CI 既有 smoke/ui-walk job；8.1 触碰受保护 Make 配方、oracle 与 AGENTS.md 证据行需同步
Minimal mergeable slice: 8.1（atomic：hurl 文件、Make 配方、oracle 变异断言与 AGENTS.md 证据行是同一 Hurl 文件的定义端与三处消费端，oracle 精确钉配方与证据行使其必须同 PR）依赖 4.1、4.2、4.3b（done 会话删除为非 running 路径）、3.3（`thinking` 落库）、5.1（快照 `thinking` 投影）、6.3（`WORKBUDDY_THINK`）、1.2（models.yml reasoning 声明随服务启动写出）、change A #481（smoke 配方与审批作答前置）；8.2a（atomic：选场景 → 选空间 → 发送 → 折叠块 → 写入卡 → 预览 → 产物面板 → 侧栏归位是首次走查的一段连续交互，任何前缀都不构成绑定会话的呈现证据；`testMatch`/`globalTimeout` 改动是让该文件被选中并跑完的前提；`finally` 清理需要 DELETE 路由）依赖 7.1、7.3、7.4、7.5b、7.6、3.3、3.4、2.2、6.3、4.3b、change A #481、#484、#485、#492（A 的 ui-walk 步骤全部合入，共享 `ui-walk-layout.ts` 辅助与 gate，超时预算按完整 A 走查实测）；8.2b（atomic：置顶 → 重命名 → 搜索 → 删除作用于 8.2a 创建的同一会话，是一条连续走查）依赖 8.2a、7.2a、7.2b、7.7、4.2、4.3b
Width exception: multi-path - 8.1 `make test-guardrails` 只能证明 Make 配方/oracle/AGENTS.md 证据行的形状，`make smoke` 只能证明 `session-meta.hurl` 的真实 HTTP 行为；oracle 精确钉住配方与证据行使 hurl 与三处消费端必须同 PR，任一侧单独合入都不构成可签收证据，故两条验证路径

## 9. 收尾

- [ ] 9.1 文档：`docs/architecture/system.md` §3.1 `sessions` 行补「空间绑定 cwd / 元数据 PATCH·DELETE / thinking 与 files.changed 事件」一句；`IMPLEMENTATION_PLAN.md:207-216` S1c 节标注 change B 归属与偏差留痕引用（proposal「功能覆盖声明」）；`CONTEXT.md` 增「任务」一条：UI 对会话的用户可见称谓（`重命名任务`/`删除任务`/`筛选任务`），侧栏「任务」分区特指未置顶且未绑定空间的会话（与 session-sidebar spec 一致）；proposal 中 `查看详情` 落点等 Stage 2 事实核对项已回写，核对无遗漏。验证：`make check` 全绿
- [ ] 9.2 规格同步（design Migration Plan「规格次序」、Open Questions 4）：确认 change A 已归档（`openspec/specs/turn-control/spec.md` 含「从此处分叉 REST」）后，新建 `openspec/changes/s1c-session-metadata-presentation/specs/turn-control/spec.md`，以 MODIFIED 整段重述该 Requirement，把场景中的五键会话视图改为八键并写入继承规则（`workspaceId`/`scene` 继承、`pinnedAt` null）。验证：`openspec validate s1c-session-metadata-presentation --strict --no-interactive` 通过；session-metadata 的 fork 场景与之逐字一致
- [ ] 9.3 Open Questions 关闭（design Open Questions 1、2、3）：(1) 本机以真实端点（`MODEL_REASONING=on`）跑一次 `make smoke-live` 并读回一条消息快照的 `thinking`：接受且返回 → 维持默认 on；拒绝请求 → 部署设 `off` 并记录；接受但不返回 → 记录「该模型无 reasoning，折叠块不渲染」；结论只改部署配置与 design 本节，凭据只在 shell env。(2) B 自己的真二进制手工核对：本机真实 omp v18.0.10（官方 release，按 SHA256SUMS 校验）+ 真实端点（与 `smoke-live` 同样只经 shell env 提供凭据），在一个已绑定工作空间的会话中 prompt 模型编辑该空间内一个已存在的文件，从该会话的 omp 会话 `.jsonl` 读取对应 `tool_execution_end` 的 `result.details`（或读宿主已持久化的 `chat_steps.changes` 佐证），记录 hashline 模式 edit 的 `details.path` 是否为绝对路径；结论写回 design Open Questions 2 与 `file-changes.ts` 注释（D6 已同时处理绝对与相对路径，结论只作记录不改行为）。(3) Open Questions 3：4.3b 的 uid-isolation 用例在 CI Linux job 绿后，加测试 VPS 上 sudo 模式一次部署演练（删除会话后 `.jsonl` 不存在），结论写回 design Open Questions 3。验证：design Open Questions 1、2、3 标「已关闭」并注明日期与二进制 SHA256；`make check` 全绿

Suggested fixture level: none - 文档、规格同步与手工验证记录，无运行时行为（9.3 若触发默认值变更，走 1.2 的 fixture）
Minimal mergeable slice: 9.1（atomic：三处文档各一句话，描述的是组 1–8 全部合入后的同一事实，拆开无独立交付价值）依赖组 1–8 全部 task（随末尾 PR 或独立 docs PR）；9.2（atomic：一份 delta 文件与一次 validate）依赖 change A 归档（无 issue 编号；执行者以 `openspec/specs/turn-control/spec.md` 已含该 Requirement 为开工条件）、4.4；9.3（atomic：三项结论各一行写回同一节，拆开无独立交付价值）依赖 1.2、3.2、3.3、3.4（持久化 `changes` 佐证）、4.3b（uid-isolation 用例）
