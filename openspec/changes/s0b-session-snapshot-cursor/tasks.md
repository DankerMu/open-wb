## 1. Contract and protected baseline
- [x] 1.1 Expanded fixture first-round PASS;138 pre-change source/test/config identities frozen and LSP consumer references checked. Parent real app/SQLite and native fake-omp counterexamples return semantic RED:1000 pending characters read as empty, without forced writes. Strict validation is required again after final fixture edits.
- [x] 1.2 Downstream103/92/93 DTO/filtering scope and parent S0b design/spec synchronized; numeric-live versus null-sealed epoch semantics and exact2048+Z overlap expectation frozen.

## 2. Complete history and sequence ownership
- [x] 2.1 Complete-view RED→GREEN; getMessages overlays only owned pending text without altering writes/timers/fault retention or lossless decoding. Direct-DB flush-boundary assertions preserved.
- [x] 2.2 RingBuffer.sequence is non-advancing; Supervisor records after persistence/buffering before optional observation, including without an observer;204 return guard and captured generation identity preserved.
- [x] 2.3 Generation-bound dispatch/pump counts retain publication ownership separately from native revocation. Parent found and repaired a boolean-ownership counterexample: old pump completion cleared the next held dispatch; native exit falsely sealed1:3. Protected counterexample now GREEN and kept as a permanent regression, without changing219 claim bookkeeping.
- [x] 2.4 Complete tree and named StreamCursor captured in the same authorized preParsing stack; current DTO expectations and required port fakes migrated,401/404/no-store/compensation preserved.

## 3. Discriminating integration proof
- [x] 3.1 Pending/fault-retained tails, unchanged flush policy, Unicode/NUL/BOM and response-hook overlap covered. Eight parent compiled app/SQLite/native cases pass, including actual2048+queued overlap→2049 and no-observer step/event sequence; browser behavior is not claimed.
- [x] 3.2 Native-exit-before-stdout-drain, healthy idle numeric versus retired sealed cursor, reuse/new epoch/restart, failed token issue without revoke, pending dispatch, replacement isolation and existing204/91 cases covered. Controlled-child and actual-native evidence are distinguished in the proof records.
- [ ] 3.3 Parent runs protected native HTTP/SQLite overlap oracle and wrong-candidate qualification for missing overlay/late cursor/skipped recording/early sealing, restoration and dedicated stability; validate exact scope, scoped static/types/build/fullserver coverage/anti-drift/strict, with unchanged discovery/thresholds.

## 4. Independent delivery
- [ ] 4.1 Expanded correctness, test-evidence+spec-compliance and invariant-state review; bounded two fix passes, same-head source CI/merge, then separate additive/modification-preserving canonical archive PR and its own CI before103.

## Core risk mapping
Selected entry/API→2.4/3.1 owner-before-parse and newDTO; schema/types→2.2/2.4 structurednullablecursor and requiredport; concurrency/state→2.3/3.2 native/pump/dispatch identities; resources→2.3/3.2 ring/native teardown; compatibility→2.1/2.4 fullcurrentview vsunchangeddurability; errors→2.1/3.1 fault-retainedtail and204 ownership; auth/secrets→2.4 ownerfilter/privatefields; tests/evidence→1.1/3.3 semanticbaseline and mutation sensitivity; release/docs→1.2/4.1 downstreamcontracts/source+archiveCI. Not selected config (no keys), fileIO (no new production filesystem path; native lifecycle covered by resources).

## Domain risk mapping
Selected tenant isolation (owner snapshot/cursor separation,2.4/3.1; no sandbox enforcement change); auth/session lifecycle (generation/turn status,2.3/3.2); process/child-environment isolation (native drain/cleanup,3.2/3.3; allowlist unchanged); server/web HTTP-envelope compatibility (new DTO,1.2/2.4); offline deployability (no dependencies/network changes,3.3). Not selected SQLite migration/catalog (no schema change; directDB durability preserved), browser runtime/navigation/persistence (contract sync only, realweb later92/93), cross-service network (no newservicecall).

## Ownership / Write Scope
One implementer/current checkout/no worktrees/no nesting. Parent owns fixture, docs/issue synchronization, protected /tmp/open-wb-issue214-evidence, acceptance and Git/PR. Source allowed: server/src/sessions/store.ts, supervisor.ts, stream/ring-buffer.ts, rest.ts; sessions/index.ts only if the required port assembly needs it. Tests: existing session-store/store-stream/store-reconcile, session-ring-buffer, session-rest and session-rest-helpers, session-supervisor-helpers; new focused session-snapshot.test.ts and session-supervisor-stream.test.ts permitted. Preserve all unaffected assertions; exactDTO changes follow this approved contract, not test weakening. No runtime/process/token/config/dependency/CI/threshold change without an evidenced blocker and parent authorization. No new production files by default: store projection can use a local view object and append pending before its existing views.push, avoiding extra decoding or a duplicate store. Every exported-symbol modification requires LSP references; imports/fixes use available LSP actions. Writer skips formatting/lint/types/build/full suites/Git, may run only the named focused regression files during vertical TDD; raw commands/exits/RED classifications must be saved to /tmp/open-wb-issue214-writer. Reviewers skip all validation.
Parent anti-drift found nine copied test blocks. Authorized test-only scope extension: session-supervisor-faults.test.ts and session-supervisor-admission.test.ts may gain the new cursor assertions in their existing equivalent scenarios; move matching new snapshot assertions into the existing store-reconcile failure case, then remove only duplicate new scenarios. Preserve every pre-existing assertion and input; use existing shared helper modules for repeated HTTP reads/cursor assertions. Do not rename literals to evade clone detection or change any gate. Concrete function contracts use owner-named types (SessionMessageTree, StreamCursor), not ReturnType projections.

## Evidence Limits and Follow-ups
Single app/store synchronous owner is the snapshot atomicity domain; no multi-process SQLite writer consistency claim. Complete in-memory content is not crash-durable until the unchanged flush/terminal path commits. Browser SSE behavior remains103/93 work; the parent reducer oracle validates protocol arithmetic, not UI. Existing reentrant claim-retention observation is tracked in #219 and is not silently folded into214; if the selected lifecycle implementation cannot preserve correct snapshot fences without resolving it, report that concrete dependency before edits. User-approved#91 min−1 and empty-id gap decisions remain fixed.

## Verification record and deviations
Parent scoped Biome/types/build exit0; full server65files1174tests exit0,92.40% statements/88.18% branches; anti-drift0clones and file caps pass.138 frozen inputs changed only in11 authorized existing source/test files; two focused test files added, no config/dependency/threshold changes. Writer original RED logs01/03/05/07 and GREEN logs are retained. Later lifecycle edges first ran GREEN; initial native-tail assertion after sealing and a missing test delimiter were authoring/setup failures, not product RED. Parent caught missing OwnedHistory/unknown child types, then enforced owner-named SessionMessageTree/StreamCursor types. Three anti-drift repair cycles resolved unused nested exports, copied test scenarios and the REST test file cap; new assertions were folded into existing scenarios with all original assertions/inputs retained, reducing duplicate test count rather than dropping behavior. Redundant length/constant-concatenation checks and generated README scaffold were removed after positive smoke. Independent held-pipe shutdown probes did not reproduce an early-slot-deletion suspicion; the distinct held-dispatch reservation failure did reproduce and was repaired. Raw evidence: /tmp/open-wb-issue214-evidence and /tmp/open-wb-issue214-writer.
