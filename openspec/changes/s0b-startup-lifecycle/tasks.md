## 1. Fixture and tracer baseline
- [x] 1.1 Expanded fixture review first-round PASS and strict validation exit0;134 existing source/test/config identities frozen. Actual compiled baselines retained: missing models.yml at server_started, SIGTERM trace listener-close→runtime-exit→listener-close→DB-close, and full fake call-proxy turn failed instead of done. #101 approvedidlecap and #7 stickyfailure semantics preserved.
- [x] 1.2 One real compiled wildcard-listen tracer reached semantic RED: exact server_started observed but models.yml absent. Initial existsSync import failure classified SETUP; before source authorization, parent corrected helper compiler geometry, unsupported Promise.withResolvers, record normalization that hid extra keys, catch-all absence, exit-vs-close/cleanup handling and signal ownership. Originals retained; corrected tracer hashes frozen and all134 baseline inputs unchanged. B is authorized vertically: tracer GREEN, then one next lifecycle behavior at a time.
## 2. Lifecycle implementation
- [x] 2.1 Existing model writer now runs after actual listen and before success. Parent real production/fake call-proxy/local-upstream probe verifies exact persisted content/done and absence of the actual upstream sentinel and runtime bearer from complete stdout/stderr/YAML; actual wildcard port and no eager runtime directories are independently exercised.
- [x] 2.2 Pending-bind versus bound-listener cancellation and repeated signals preserve runtime→listener→DB order. Parent probes cover held model writes, post-bind write failure, pre-bind cancellation, repeated mixed signals, sticky real failure and cleanup failure status. Permanent real-process regressions cover model publication and native exit order; further race cases remain executable acceptance probes rather than duplicate permanent fixture machinery.
## 3. Independent acceptance and delivery
- [x] 3.1 Parent replayed original models/order counterexamples plus independent full proxy/secret, write-failure, held-write signal, pre-bind cancellation/port+DB reuse, repeated signals, sticky failure and cleanup-failure cases. Four reached bad candidates failed, restoration and seven stability cases passed. Scoped Biome/build/types/full server coverage/anti-drift and strict fixture pass without discovery/threshold changes.
- [ ] 3.2 Expanded correctness/test-evidence+spec/invariant-security lifecycle seats, bounded two fix passes, exact-head CI and source merge. User waived only per-issue human wait, not agent review/CI/finalEpicacceptance.
- [ ] 3.3 Separate full canonical lifecycle/shared-assembly promotion/archive and parent3.8 completion with its own CI. No SSE/web/realomp productionrelease claim.
## Core risk mapping
Entry/API→1.2/2.1 real production main and no import effects; config→1.1 #101elevenkeys/root/nativecap unchanged; fileIO/publication→2.1/2.2 actual managedmodels success/failure/inflightwork; schema/units→2.1 wildcard/IPv6 connectable actualport, modelbytes, exactrecords; auth/secrets→2.1/3.1 unique64-character upstreamkey and actualruntimebearer, no rawoutput/secretYAML; concurrency/state→2.2 prebind/bound/modelwrite/signal/failure phases and idempotence; resources→2.2 nativeexit beforelistener event beforeDBclose plus all-path test cleanup; compatibility→1.1/2.2 old entry/main/abort/sticky/error/auth guarantees; errors/partialoutputs→2.2 managedwriter/realIOerrors/failuretruth; release/packaging→1.2/3.1 real declaredcompiler+trackedmigrationtree andsameheadCI; docs→3.3 complete requirement replacement, not a partial overwrite.
## Project domain packs
Selected process/child-environment isolation, cross-service credential boundary, SQLite close/recovery compatibility, auth/session lifecycle, offline deployment and HTTP contract map to2.1–3.1. No new tenant/sandbox enforcement, schema migration, browser navigation or KB surface; parent same-uid security limitation remains and no OS-enclave/release claim is made.
## Ownership and allowed scope
One writer/currentcheckout/no newworktree/no nesting. Parent owns fixture, protectedbaselines/oracles, verification, commits/PR/review gates. Production scope server.ts only unless an actual interface blocker is reported before widening; reuse app.sessions and models-yml helpers, do not modify runtime grace periods or fake script semantics. Test scope server-startup-order.test.ts plus minimal shared startup helper/fixture under server/test if existing helpers cannot provide the real compiler/process boundary. No new dependency/CI/Make/config/threshold edits. Existing fake-upstream/recording HTTP utilities and RPC helpers are reference implementations, never copy a second protocol engine.
Writer skips format/lint/build/typecheck/fullsuite/git; focused startup tests may invoke their own declared compiler into private scratch as part of executing the real tested surface, not a project-wide verification loop. For each behavior: one RED→minimum GREEN before the next uncertain test. New tests earn their place with observable contracts and plausible failure modes; no source-text, noop, only-nonempty or mock-echo assertions. Reviewers read-only/no validation/nesting. Parent can make scoped formatter and documentation updates only.
## Reconnaissance evidence
Fastify preClose itself retires sessions before rawserver.close, but Node listens with the sameAbortSignal and can independently close a boundserver first. Parent actual trace confirms this, so changing the bound-signal decision is required, not speculative. Fastify cancellation duringready resolves withoutAbortError; earlier review's unconditional-abort-failure inference was refuted. Do not weaken clean-cancellation no-record or true-failure sticky-nonzero contracts to reconcile them: they describe different states. Inflight models IO introduces a new ownership boundary to test explicitly. Evidence root /tmp/open-wb-issue102-evidence.

## Evidence governance
Parent protected actual-process probes independently reproduced all three initial failures (models absent, listener before native exit, call-proxy failed vs done). Original probe versions are retained; fail-stop cleanup was strengthened to kill/reap only the owned detached group, without changing expected behavior, and the first two semantic REDs were rerun. Writer's initial report included an unauthorized git status and mentioned an unrelated long-lived process; protected hashes prove no production edit, the writer reported leaving that process untouched, and subsequent git/foreign-PID inspection is explicitly prohibited.

## Implementation and oracle corrections
The initial native-order fixture incorrectly inserted a grandchild observer and concatenated two separate logs into a predetermined order. Its timeout was not accepted as a semantic regression. The corrected private preload observes the runtime-owned ChildProcess exit, listener close and successful database close into one shared trace. The temporary fake scenario environment extension was rejected and restored byte-for-byte to baseline; the private launcher imports the unchanged fake with its existing scenario argument. A handwritten HTTP parser was replaced by Node fetch. Production native-order changes preceded the corrected permanent regression, so its TDD evidence is the parent's previously recorded real baseline counterexample, not a claimed RED from the invalid observer.

Independent candidate probes and seven dedicated stability runs pass. Four reached bad candidates fail semantically and restore to GREEN: re-aborting the bound listener on repeated signals, late publication after held-write cancellation, zero exit after model-write failure, and false failure publication during pre-bind cancellation. An earlier post-listen cancellation mutant survived because that continuation was not reached; it is retained as an inadequate qualification experiment, not counted as fault detection. Full server verification passed 61 files / 1125 tests with 91.87% statements and 87.86% branches. Static checks exposed an optional-body type error and duplicated cleanup, then an accidentally removed TCP helper during consolidation; all corrected without weakening assertions. Final scoped Biome, server typecheck, anti-drift (zero clones), strict fixture, both production-process regression cases and production build exit0. The full suite predates these test-helper-only repairs; the touched tests were rerun afterwards.
