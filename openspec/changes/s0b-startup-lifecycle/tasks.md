## 1. Fixture and tracer baseline
- [x] 1.1 Expanded fixture review first-round PASS and strict validation exit0;134 existing source/test/config identities frozen. Actual compiled baselines retained: missing models.yml at server_started, SIGTERM trace listener-close→runtime-exit→listener-close→DB-close, and full fake call-proxy turn failed instead of done. #101 approvedidlecap and #7 stickyfailure semantics preserved.
- [ ] 1.2 Build one real-process tracer test first (wildcard bind→actual-port model file before success); observe semantic RED on the current callable compiled entry, then authorize the minimum integration. Missing module/test-loader failures are SETUP, never substitutes for observed wrong behavior. Test-owned compiler fixture must not rely on an old dist or undeclared jiti.
## 2. Lifecycle implementation
- [ ] 2.1 Wire existing model writer after actual listen and before success; cover complete real fake call-proxy/local-upstream turn with actual persisted content/done and sentinel+runtime-bearer absent from stdout/stderr. Reuse serializer/host mapping/env-name credential policy and preserve no eager sandbox/bin/session directories.
- [ ] 2.2 Pin the original native-order RED in a regression, then fix pending-bind versus bound-listener cancellation, repeated signals, runtime→listener→DB order and cleanup failure truth. Add signal-during-write and post-bind writer failure cases incrementally; preserve no late record/handle access/await cycle, pure import and generic managed output.
## 3. Independent acceptance and delivery
- [ ] 3.1 Parent replays original models/order counterexamples plus independent full proxy/secret, write-failure, held-write signal and pre-bind cancellation/port+DB reuse cases. Qualify major failure classes using real controlled faults or isolated bad candidates, restoration and dedicated stability. Scoped format/build/types/full server coverage/anti-drift and strictfixture pass without discovery/threshold changes.
- [ ] 3.2 Expanded correctness/test-evidence+spec/invariant-security lifecycle seats, bounded two fix passes, exact-head CI and source merge. User waived only per-issue human wait, not agent review/CI/finalEpicacceptance.
- [ ] 3.3 Separate full canonical lifecycle/shared-assembly promotion/archive and parent3.8 completion with its own CI. No SSE/web/realomp productionrelease claim.
## Core risk mapping
Entry/API→1.2/2.1 real production main and no import effects; config→1.1 #101elevenkeys/root/nativecap unchanged; fileIO/publication→2.1/2.2 actual managedmodels success/failure/inflightwork; schema/units→2.1 wildcard/IPv6 connectable actualport, modelbytes, exactrecords; auth/secrets→2.1/3.1 unique64-character upstreamkey and actualruntimebearer, no rawoutput/secretYAML; concurrency/state→2.2 prebind/bound/modelwrite/signal/failure phases and idempotence; resources→2.2 nativeexit beforelistener event beforeDBclose plus all-path test cleanup; compatibility→1.1/2.2 old entry/main/abort/sticky/error/auth guarantees; errors/partialoutputs→2.2 managedwriter/realIOerrors/failuretruth; release/packaging→1.2/3.1 real declaredcompiler+trackedmigrationtree andsameheadCI; docs→3.3 complete requirement replacement, not a partial overwrite.
## Project domain packs
Selected process/child-environment isolation, cross-service credential boundary, SQLite close/recovery compatibility, auth/session lifecycle, offline deployment and HTTP contract map to2.1–3.1. No new tenant/sandbox enforcement, schema migration, browser navigation or KB surface; parent same-uid security limitation remains and no OS-enclave/release claim is made.
## Ownership and allowed scope
One writer/currentcheckout/no newworktree/no nesting. Parent owns fixture, protectedbaselines/oracles, verification, commits/PR/review gates. Production scope server.ts only unless an actual interface blocker is reported before widening; reuse app.sessions and models-yml helpers, do not modify runtime grace periods or fake script semantics. Test scope server-startup-order.test.ts plus minimal shared startup helper/fixture under server/test if existing helpers cannot provide the real compiler/process boundary. No new dependency/CI/Make/config/threshold edits. Existing fake-upstream/recording HTTP utilities and RPC helpers are reference implementations, never copy a second protocol engine.
Writer skips format/lint/build/typecheck/fullsuite/git; focused startup tests may invoke their own declared compiler into private scratch as part of executing the real tested surface, not a project-wide verification loop. For each behavior: one RED→minimum GREEN before the next uncertain test. New tests earn their place with observable contracts and plausible failure modes; no source-text, noop, only-nonempty or mock-echo assertions. Reviewers read-only/no validation/nesting. Parent can make scoped formatter and documentation updates only.
## Reconnaissance evidence
Fastify preClose itself retires sessions before rawserver.close, but Node listens with the sameAbortSignal and can independently close a boundserver first. Parent actual trace confirms this, so changing the bound-signal decision is required, not speculative. Fastify cancellation duringready resolves withoutAbortError; earlier review's unconditional-abort-failure inference was refuted. Do not weaken clean-cancellation no-record or true-failure sticky-nonzero contracts to reconcile them: they describe different states. Inflight models IO introduces a new ownership boundary to test explicitly. Evidence root /tmp/open-wb-issue102-evidence.
