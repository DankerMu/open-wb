# Design: ui-popup-primitives（#278）

Change surface: 新 `web/src/ui/{menu,popover,tooltip,segmented-control}.tsx` 与同名 `.css`；`web/src/ui/ui.css`（+4 `@import`）；`web/src/ui/index.ts`（+`Menu`、`type MenuItem`、`Popover`、`Tooltip`、`SegmentedControl`）；`web/test/ui-support.ts`（+`yieldMacrotask`、`waitMs`、`pressPointer`、`ruleBody`）与 `web/test/ui-dialog.test.tsx`（三 helper 改导入，行为不变）；新 `web/test/ui-menu.test.tsx`、`web/test/ui-popover-tooltip.test.tsx`、`web/test/ui-segmented-control.test.tsx`；`web/package.json`/`package-lock.json`；`ATTRIBUTION.md:37`。

Must preserve: `web/src/features/files/dialogs.tsx:84-190`（`CreationMenu` 手写菜单）、`web/src/features/files/page.tsx:94-165`（切换器）、`web/src/features/settings/page.tsx:6-38`（radio 卡）及其测试 `files-page.test.tsx`/`files-errors.test.tsx`/`settings-footer.test.tsx` 逐字不动（迁移归 #302/2.5）；`web/src/ui/{button,input,switch,tag,chip,dialog,confirm-dialog,drawer,icon,brand-mark}.*` 不动（Menu 只消费 `Icon`）；`web/test/radix-platform.ts` 不动（头注释已覆盖 Menu/Popover）；`ui-dialog.test.tsx` 49 个用例语义不变（只把三个 helper 改为从 `ui-support.ts` 导入）；`motion.css` 不动（复用 `wb-pop` 关键帧名）；`styles.css` `@import` 列表不变；`make ui-walk` 全 journey 不变（无页面消费者）。

Must add/change:
- 依赖：`@radix-ui/react-dropdown-menu@^2.1`、`@radix-ui/react-popover@^1.1`、`@radix-ui/react-tooltip@^1.2`、`@radix-ui/react-radio-group@^1.4`（MIT；dismissable-layer/focus-scope/presence/portal 已随 react-dialog 进入 lock，popper/roving-focus/collection/direction/visually-hidden 与 `@floating-ui/*` 为本切片新引入，lock diff 审查时留意）。
- `Menu`（`menu.tsx`）：`export type MenuItem = { label: string; onSelect: () => void; danger?: boolean | undefined; icon?: IconName | undefined }`；`MenuProps = { trigger: ReactElement; items: readonly MenuItem[] }`。结构：`<DropdownMenu.Root modal={false}><DropdownMenu.Trigger asChild>{trigger}</DropdownMenu.Trigger><DropdownMenu.Portal><DropdownMenu.Content className="ui-menu" loop sideOffset={6} collisionPadding={8}>{items.map(item => <DropdownMenu.Item className={item.danger ? "ui-menu-item ui-menu-item--danger" : "ui-menu-item"} key={item.label} onSelect={() => item.onSelect()}>{item.icon && <Icon name={item.icon} size={14} />}<span className="ui-menu-item-label">{item.label}</span></DropdownMenu.Item>)}</DropdownMenu.Content></DropdownMenu.Portal></DropdownMenu.Root>`。Radix 提供：trigger `aria-haspopup="menu"`/`aria-expanded`/`data-state`、pointerdown（左键、非 ctrl）或 Enter/Space/ArrowDown 打开、content `role="menu"` + `aria-labelledby=triggerId`、item `role="menuitem"`、roving focus（上下/Home/End/typeahead，`loop` 首尾环绕——既有手写菜单 `dialogs.tsx:110-125` 取模环绕，迁移不得退化）、Enter/Space/click 触发 `onSelect` 并关闭、Escape/外点关闭、关闭后焦点归还 trigger（`onCloseAutoFocus` 默认）。`modal={false}`：不 `hideOthers`、不锁滚动、body 不加 `pointer-events:none`，外点关闭且点击到达目标（demo:1056）。键盘打开时 Radix 经 `isUsingKeyboardRef`（document keydown 监听）允许 `RovingFocusGroup` 的入场聚焦 → 首项获焦；pointer 打开时入场聚焦被阻止，焦点留在 content。不写任何 onKeyDown。
- `Popover`（`popover.tsx`）：`PopoverProps = { trigger: ReactElement; children: ReactNode; open?: boolean | undefined; onOpenChange?: ((open: boolean) => void) | undefined; contentRole?: string | undefined; contentLabel?: string | undefined }`。结构：`<Popover.Root open={open} onOpenChange={onOpenChange}><Popover.Trigger asChild>{trigger}</Popover.Trigger><Popover.Portal><Popover.Content className="ui-popover" sideOffset={6} collisionPadding={8} {...(contentRole ? { role: contentRole } : {})} {...(contentLabel ? { "aria-label": contentLabel } : {})}>{children}</Popover.Content></Popover.Portal></Popover.Root>`。**必须条件展开**：Radix `PopoverContentImpl` 把 `role="dialog"` 写在 `{...contentProps}` 之前，传 `role={undefined}` 会把默认角色覆盖成无角色。`open`/`onOpenChange` 直接透传（`undefined` = 非受控，Radix `useControllableState`）。Radix 提供：trigger `aria-haspopup="dialog"`/`aria-expanded`、非模态 `FocusScope`（打开聚焦 content 内首个可聚焦元素、关闭归还 trigger）、Escape/外点关闭（外点判定沿用 DismissableLayer：pointerdown 登记、click 到达才关闭——复用 `pressPointer`）。
- `Tooltip`（`tooltip.tsx`）：`TooltipProps = { label: string; children: ReactElement; side?: "top" | "right" | "bottom" | "left" | undefined }`。结构：`<Tooltip.Provider delayDuration={300} disableHoverableContent><Tooltip.Root><Tooltip.Trigger asChild>{children}</Tooltip.Trigger><Tooltip.Portal><Tooltip.Content className="ui-tooltip" side={side ?? "top"} sideOffset={6} collisionPadding={8}>{label}</Tooltip.Content></Tooltip.Portal></Tooltip.Root></Tooltip.Provider>`。Radix 提供：trigger `data-state`、聚焦立即打开（`onFocus`，React 映射 `focusin`，测试用 `act(() => el.focus())` 而非 `fireEvent.focus`）、`pointermove`（非 touch）经 `delayDuration` 后打开、blur/Escape 关闭、`pointerleave` 关闭（**仅当 `disableHoverableContent`**：Radix 1.2 默认可悬停内容，离开 trigger 只清计时器并建宽限区，要等下一次落在宽限区外的 document `pointermove` 才关闭；RTL `fireEvent.pointerLeave` 不派发 pointermove，故默认配置下 T6 过不了，且真实用户离开后 tooltip 会残留到下一次移动）、打开期间 trigger `aria-describedby=contentId`；Content 未传 `aria-label` 时可见 content 本身就是 `role="tooltip" id=contentId`（Radix 只在传 `aria-label` 时才渲染 VisuallyHidden 隐藏副本），本组件不传 `aria-label`。Provider 每实例一份（无应用根改动、无额外导出）。
- `SegmentedControl`（`segmented-control.tsx`）：`SegmentedControlProps<T extends string> = { value: T; onValueChange: (value: T) => void; options: readonly { value: T; label: string }[]; label: string }`。结构：`<RadioGroup.Root aria-label={label} className="ui-seg" onValueChange={v => onValueChange(v as T)} value={value}>{options.map(o => <RadioGroup.Item className="ui-seg-item" key={o.value} value={o.value}>{o.label}</RadioGroup.Item>)}</RadioGroup.Root>`。Radix 提供：root `role="radiogroup"`、item `<button type="button" role="radio" aria-checked data-state="checked|unchecked">`、roving focus（`RovingFocusGroup` 初始 `currentTabStopId` 为 null：首次渲染根元素 `tabIndex=0`、所有项 `-1`；根元素获焦时入场聚焦跳到选中项，此后选中项 `0`、其余 `-1`）、方向键移动焦点并选中（Radix 在 document keydown 记录箭头键、item `onFocus` 时 `click()` → `onValueChange`；`loop` 默认 true）、表单内 `BubbleInput`（构造 ResizeObserver → 测试引入 shim）。样式挂 `[data-state="checked"]`，不写 `.on` 类。
- CSS（颜色只经 token；z-index 写在 Radix Content 元素上——Popper 会把 content 的计算 z-index 复制到定位包装层）：
  - `menu.css`（demo:761-770，去掉 `position:fixed`）：`.ui-menu{z-index:1600;min-width:180px;max-height:min(70vh,480px);padding:4px;overflow-y:auto;border:1px solid var(--wb-border-default);border-radius:10px;background:var(--wb-bg-primary);box-shadow:var(--wb-shadow-popover);animation:wb-pop .15s ease}`；`.ui-menu-item{position:relative;display:flex;align-items:center;gap:8px;width:100%;padding:7px 10px;border-radius:6px;color:var(--wb-text-primary);font-size:13px;text-align:left;cursor:pointer;user-select:none;outline:none;transition:background .12s}`；`.ui-menu-item:hover,.ui-menu-item[data-highlighted]{background:var(--wb-bg-hover)}`；`.ui-menu-item:focus-visible{outline:none}`（roving focus 的高亮底色即焦点指示，压制全局 4px 圆角环）；`.ui-menu-item .ui-icon{color:var(--wb-icon-secondary)}`；`.ui-menu-item--danger,.ui-menu-item--danger .ui-icon{color:var(--wb-status-error-text)}`；`.ui-menu-item-label{flex:1;min-width:0}`；`@media (prefers-reduced-motion: reduce){.ui-menu{animation:none}.ui-menu-item{transition:none}}`。
  - `popover.css`（demo:779-780，去掉 `position/bottom/display`）：`.ui-popover{z-index:1300;width:300px;max-height:min(70vh,520px);padding:8px;overflow-y:auto;border:1px solid var(--wb-border-default);border-radius:12px;background:var(--wb-bg-primary);box-shadow:var(--wb-shadow-popover);animation:wb-pop .15s ease}`；`.ui-popover:focus-visible{outline:none}`（content 可获焦时不画环）；reduced-motion `animation:none`。`.pop-item/.pop-search/.pop-sep`（demo:781-792）不进本切片。
  - `tooltip.css`（无 demo 锚点，见 proposal 偏差 2）：`.ui-tooltip{z-index:1700;max-width:240px;padding:5px 8px;border-radius:6px;background:var(--wb-palette-black-90);color:var(--wb-text-white);font-size:12px;line-height:18px;box-shadow:var(--wb-shadow-popover);animation:wb-pop .15s ease}`；`[data-theme="dark"] .ui-tooltip{background:var(--wb-palette-white-90);color:var(--wb-palette-black-90)}`；reduced-motion `animation:none`。
  - `segmented-control.css`（demo:558-562）：`.ui-seg{display:inline-flex;padding:3px;border-radius:99px;background:var(--wb-bg-hover-light, var(--wb-color-bg-input))}`（demo 原样含回退；两名皆在 tokens.css 定义。`--wb-bg-hover-light` 只在 `:root` 定义为 `var(--wb-palette-gray-3)`，而 `--wb-palette-gray-3` 在深色块被重定义，`data-theme` 挂在 `documentElement` 上，故深色下它仍解析出深色值、回退分支实际不触发；回退保留只为与 demo 逐字一致）；`.ui-seg-item{padding:6px 15px;border:none;border-radius:99px;background:none;color:var(--wb-text-secondary);font-size:12.5px;white-space:nowrap;cursor:pointer}`；`.ui-seg-item:hover{color:var(--wb-text-primary)}`；`.ui-seg-item[data-state="checked"]{background:var(--wb-palette-black-90);color:var(--wb-text-white)}`；`[data-theme="dark"] .ui-seg-item[data-state="checked"]{background:var(--wb-palette-white-90);color:var(--wb-palette-black-90)}`；`.ui-seg-item:focus-visible{border-radius:99px}`；reduced-motion 块（`transition:none`，对称于其它组件 css，守卫要求每个可聚焦组件 css 都有）。
- `ui-support.ts` 新增：`yieldMacrotask()`（原文）、`pressPointer(el)`（原 `pressOverlay` 原文，注释改为通用"DismissableLayer 外点序列"）、`ruleBody(css, selector)`（原文）；`ui-dialog.test.tsx` 删除本地定义改导入，`pressOverlay` 调用点改名。
- `ATTRIBUTION.md:37`：已安装列表追加 `@radix-ui/react-dropdown-menu`、`@radix-ui/react-popover`、`@radix-ui/react-tooltip`、`@radix-ui/react-radio-group`（#278，`Menu`/`Popover`/`Tooltip`/`SegmentedControl`）；"后续切片安装"只剩 toast（#279）。

Governing invariant: 弹出层与分段控件的全部交互语义（打开/关闭触发、roving focus 与方向键、typeahead、Escape/外点、焦点归还 trigger、`aria-haspopup`/`aria-expanded`/`aria-checked`/`aria-describedby`、portal 与碰撞定位）只由 Radix 提供，本仓不写 onKeyDown/焦点管理；本仓只做四件事：`Menu modal={false}` + `loop`、Popover 的 `role`/`aria-label` 条件展开、Tooltip 每实例 Provider + `side`、Tooltip `disableHoverableContent`（离开 trigger 即关）。组件 tsx 无 `style={`（Radix Popper 注入的包装层内联定位样式不在源码内）；颜色只经 token；z-index 分层 popover 1300 < drawer overlay/panel 1350/1360 < dialog 1400 < menu 1600 < tooltip 1700（Popover 不能开在 Dialog/Drawer 之上，S1e 无此消费者）。

Sibling surfaces: `web/src/features/files/dialogs.tsx`（#302 迁移：`CreationMenu` → `Menu`，Radix Trigger 自带 `aria-haspopup`/`aria-expanded`，`dialogs.tsx:139-140` 的手写 `aria-expanded`/`aria-haspopup` 随之消失，`:141` 的 `aria-label="新建"` 必须留在 trigger `Button` 上（Slot 透传；`ui-walk.spec.ts:426` 按名 `新建` 定位）；**焦点归还注意**：Radix 先同步跑 item `onSelect` 再关菜单，若 `onSelect` 直接打开 `Dialog`，`useFocusHandoff` 在 `onOpenAutoFocus` 记录到的打开者是即将卸载的 menuitem，关闭对话框后焦点会落到 body——#302 须给由菜单项打开的 `Dialog` 传 `returnFocus`（指向 `＋` trigger 的 ref），并在 jsdom 断言关闭后 `document.activeElement === trigger`；`ui-walk.spec.ts:427` `files.getByRole("menuitem")` 因 portal 改页面范围）；`web/src/features/files/page.tsx`（#302：切换器 → `Popover contentRole="dialog" contentLabel="工作空间切换器"`，`ui-walk.spec.ts:387` 改页面范围；trigger 的 `aria-label="选择工作空间"` 保留（ui-walk:386）；搜索框为 content 内首个可聚焦元素即自动获焦；`＋ 新建工作空间` 从 Popover 内打开的 `Dialog` 同样须传 `returnFocus` 指向切换器 trigger）；`web/src/features/settings/page.tsx` 与 `web/test/settings-footer.test.tsx:154-178`（2.5：`radiogroup` 名 `主题` 与 `radio` 名沿用；`:158,:174` 的 `HTMLInputElement.checked` 断言改 `aria-checked="true"`；`ui-walk.spec.ts:90,:365` 的 `check()`/`toBeChecked()` 对 `role=radio` + `aria-checked` 的按钮同样有效，无需改）；`web/src/routes/shell/sidebar.tsx`（由 #281 新建，当前不存在；#281：折叠态 `Tooltip side="right"`；用户菜单 `Menu` 的 trigger 为 footer 按钮，关闭后焦点回该按钮；`退出登录` 项打开的 `ConfirmDialog` 须传 `returnFocus` 指向该按钮，理由同 #302 注意）；`web/src/ui/icon.tsx`（`Menu` 消费 `size={14}`）；`web/test/ui-guardrails.test.ts`（新 css/tsx 自动纳入字面量与 `style={` 扫描；Radix 包 ATTRIBUTION 断言逐包 `toContain`）；`web/test/ui-tokens.test.ts`（未定义引用守卫覆盖四个新 css，含 `var(a, var(b))` 回退形态）；`web/test/ui-dialog.test.tsx`（helper 上收后行为不变）；`styles.css:79-83` 全局 `:focus-visible`。

Seams under test（三文件顶部均 `import "./radix-platform.js"`；焦点移动与关闭归还发生在 Radix 的 `setTimeout`/事件链里，断言一律 `waitFor`；`afterEach` 沿用 `cleanup + yieldMacrotask + restoreAllMocks`）:
- `ui-menu.test.tsx`：(M1) 关闭态：无 `menu`，trigger `aria-haspopup="menu"`、`aria-expanded="false"`；(M2) 键盘打开：`fireEvent.keyDown(trigger,{key:"Enter"})` → `findByRole("menu")` 类名含 `ui-menu`、位于 `document.body` 直接 portal 子树、`aria-labelledby` 等于 trigger `id`、`menuitem` 数 = items 数、`waitFor` 首项获焦；(M3) 方向键：首项上 ArrowDown → 第二项；End → 末项；Home → 首项；首项上 ArrowUp → 末项、末项上 ArrowDown → 首项（`loop`）；(M4) 焦点项上 Enter → 该项 `onSelect` 恰一次、`waitFor` 无 `menu`、焦点回 trigger；(M5) Escape → 关闭、无 `onSelect` 调用、焦点回 trigger；(M6) 指针打开：`fireEvent.pointerDown(trigger,{button:0,ctrlKey:false,pointerType:"mouse"})` → 打开；`fireEvent.click(item)` → `onSelect` 一次并关闭；(M7) `danger` 项类名 `ui-menu-item ui-menu-item--danger`、`icon` 项内含 `aria-hidden` svg 且类名含 `ui-icon-14`、普通项无 `--danger`；(M8) 非模态：打开期间 `document.body.style.pointerEvents === ""`、页面上另一个按钮 `getByRole("button",{name:"其他"})` 无需 `hidden:true` 即可找到（未被 `aria-hidden`）；`pressPointer(document.body)` 让出宏任务后 → 关闭。
- `ui-popover-tooltip.test.tsx`：(P1) 非受控默认：`fireEvent.click(trigger)` → `findByRole("dialog")`（Radix 默认角色，未传 `contentRole`）在 body portal 内、无 `aria-label`、trigger `aria-expanded="true"`；(P2) `contentRole="dialog" contentLabel="工作空间切换器"` → `getByRole("dialog",{name:"工作空间切换器"})`；`contentRole="group" contentLabel="模型"` → `getByRole("group",{name:"模型"})` 且无 `dialog`；(P3) 打开后 `waitFor` content 内首个 `input` 获焦；Escape → 关闭、`waitFor` 焦点回 trigger；(P4) `pressPointer(document.body)`（先让出宏任务）→ 关闭；`pressPointer(content 内元素)` → 仍打开；(P5) 受控：`open` 为 true 直接渲染；Escape → `onOpenChange(false)` 一次且仍渲染（受控未翻转）；rerender `open=false` → 消失；(P6) content 带 `data-side`/`data-align` 属性（Radix Popper 输出；真实翻转不在 jsdom 断言）；(T1) 初始无 `tooltip`、trigger 无 `aria-describedby`；(T2) `act(() => trigger.focus())` → `findByRole("tooltip")` 文本 = label、`trigger.getAttribute("aria-describedby") === tooltip.id`、该元素类名含 `ui-tooltip`（content 本身即 tooltip，无隐藏副本）；(T3) `act(() => trigger.blur())` → `waitFor` 无 `tooltip` 且无 `aria-describedby`；(T4) 打开后 `fireEvent.keyDown(document,{key:"Escape"})` → 关闭；(T5) `side="right"` → `.ui-tooltip` `data-side="right"`，默认 `data-side="top"`；(T6) hover：`fireEvent.pointerMove(trigger,{pointerType:"mouse"})` → `waitMs(250)` 后仍无 `tooltip`（钉住 300ms 下界；Radix 总经 setTimeout 打开，同步判空恒真）→ 再 `waitMs(100)` 后有；`fireEvent.pointerLeave(trigger)`（不派发任何 pointermove）→ `waitFor` 无（依赖 `disableHoverableContent`）；(S) 静态：四个 css 各含 `prefers-reduced-motion: reduce` 块；`ruleBody(menu.css,".ui-menu")` 含 `z-index: 1600`、`.ui-popover` 含 `z-index: 1300`、`.ui-tooltip` 含 `z-index: 1700`；`menu.css` 含选择器 `.ui-menu-item[data-highlighted]`；`segmented-control.css` 含 `[data-theme="dark"] .ui-seg-item[data-state="checked"]` 与 `.ui-seg-item:focus-visible`（含 `border-radius`）；`tooltip.css` 含 `[data-theme="dark"] .ui-tooltip`；`ui.css` 含四个 `@import`；(X) 出口：`Menu`/`Popover`/`Tooltip`/`SegmentedControl` 可从 `../src/ui/index.js` 导入，`MenuItem` 类型可用于标注 items 数组（编译期消费，满足 knip）。
- `ui-segmented-control.test.tsx`：(G1) `getByRole("radiogroup",{name:"主题"})`、三个 `radio` 名 `浅色`/`深色`/`跟随系统`、选中项 `aria-checked="true"` + `data-state="checked"`、其余 `"false"`/`unchecked`；root 类名 `ui-seg`、项类名 `ui-seg-item`、项为 `button` 且 `type="button"`；(G2) 点击未选中项 → `onValueChange("dark")` 恰一次，受控下 rerender 前 `aria-checked` 不变；(G3) 选中项 `focus()` 后 `fireEvent.keyDown(item,{key:"ArrowRight"})` → `waitFor(onValueChange 被以下一项调用)`（不得在 `waitFor` 前派发 keyUp——Radix 在 keyup 清除箭头标记）；首项上 ArrowLeft → 末项（默认 `loop`）；(G4) 初始渲染：root `.ui-seg` `tabIndex=0`、三项均 `-1`；`act(() => root.focus())` → `waitFor` 焦点落在选中项、选中项 `tabIndex=0`、其余 `-1`；(G5) 包在 `<form>` 内渲染不抛错、`radio` 仍为 3 个。

Required evidence:
- 三份测试先红（导入失败）后绿；每个 seam 各≥1 用例；涉及焦点移动/归还/关闭的断言经 `waitFor`，不得因假红改为手写处理器。反向注入八项各红并回退：`Menu` 去掉 `modal={false}` → (M8) 红；去掉 `loop` → (M3) 环绕红；`Popover` 改为无条件 `role={contentRole}` → (P1) 默认角色红；`Tooltip` 去掉 `Provider` → (T2) 抛错红；不透传 `side` → (T5) 红；`SegmentedControl` 去掉 `aria-label` 映射 → (G1) 红；删除 `[data-theme="dark"] .ui-seg-item[data-state="checked"]` 规则 → (S) 红；`Tooltip.Provider` 去掉 `disableHoverableContent` → (T6) `pointerLeave` 后仍显示红。
- `make check` exit 0（含 knip：四导出 + `MenuItem` 由测试消费；jscpd：helper 上收后 `ui-dialog.test.tsx` 与新测试无克隆；size-guard：每文件 < 800）；`npm run build --workspace web` exit 0；`ci-compiled-server.sh ui-walk`（CI 环境变量）1 passed。
- 守卫（既有）：`web/src/ui/**` 无字面颜色、无 `style={`；ATTRIBUTION 含四个新包全名。
- 视口翻转/钳位不在 jsdom 取证（零尺寸下 floating-ui 不产生溢出）：由迁移切片的 ui-walk 与 `make ui-shots`（#297/#298）覆盖。

Non-goals: 迁移既有菜单/切换器/radio 卡；Toast；Menu 分隔线/分组/选中勾/`align`/`side`；Popover `initialFocus`/`modal`/arrow；Tooltip 应用根 Provider 与 arrow；SegmentedControl `name`/`disabled`；jsdom 内的翻转断言。

Review focus: (1) `Menu` 是否 `modal={false}` 且 `loop`，且没有任何手写 onKeyDown/焦点代码；(2) `Popover` 的 `role`/`aria-label` 是否条件展开（未传时 DOM 仍为 `role="dialog"`）；(3) `Tooltip` Provider 位置、`disableHoverableContent` 在位，且 `aria-describedby` 指向可见 content（content 本身 `role="tooltip"`；Content 不传 `aria-label`，否则 Radix 会改渲染隐藏副本并把可见层降为无 role）；(4) `SegmentedControl` 的 `aria-label` 与 `[data-state="checked"]` 样式挂点，泛型 `T` 的 `as T` 只出现一次；(5) 四个 css 的 z-index 写在 Radix Content 元素的类上、reduced-motion 块齐全、`:focus-visible` 处理（seg 重申 99px；menu/popover 明确 `outline:none` 的理由成立——roving 高亮或 content 本身不应画环）；(6) `ui-support.ts` 上收后 `ui-dialog.test.tsx` 无重复定义、`pressPointer` 语义未变；(7) ATTRIBUTION 四包全名逐个出现；(8) 测试里所有焦点/关闭断言都经 `waitFor`，SegmentedControl 箭头用例未在断言前派发 keyUp；(9) `web/src/ui/**/*.tsx` 无 `style={` 与颜色字面量。
