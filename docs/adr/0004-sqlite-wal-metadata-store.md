# app-server 元数据用 SQLite（WAL）

账号属性、会话索引、工作空间与挂载配置、权限规则、审计事件、KB 可见范围、模型注册表统一存
SQLite WAL 单文件，schema 由迁移工具管理。P4 目标是内网单机部署包，多一个 PG 进程是纯运维成本；
内网百人级的元数据+审计追加写负载在 WAL 能力内。将来出现多实例/HA 需求时再迁移，迁移工具保证
schema 不锁死。审计表只追加（应用层禁 UPDATE/DELETE）。
